# สเปกการเขียน Thai BASIC ROM ใหม่ทั้งหมด (clean-room rewrite)

เอกสารนี้เป็นสเปกสำหรับเขียน MSX Thai BASIC cartridge ใหม่ทั้งหมดเป็น Z80 assembly source (ไม่ใช่การแพตช์
ไบต์แบบเดิม) ตามที่ตกลงกันไว้:

- เขียนเป็น **Z80 assembly source ใหม่ทั้งหมด** ประกอบด้วย `pasmo`
- นำมาใช้ซ้ำเฉพาะ **font** (bitmap) และ **logic** (พฤติกรรมที่ควรจะเป็น) จากตัวเดิม
- เป้าหมายพฤติกรรม: **ถูกต้องตามที่ควรจะเป็น** (ไม่ใช่ byte-identical กับต้นฉบับ) — แก้บั๊กทั้งหมดที่เจอ
  ตลอดโครงการนี้ไปเลย: MSX1 hang เดิม, MSX2 character corruption, DSPFNK bug, MSX2+ stack-search bug,
  และการที่ระบบ VRAM compositing เดิมไม่พอร์ตไป MSX2/2+ เลย

## 0. หลักการออกแบบที่ต่างจากต้นฉบับโดยเจตนา

ต้นฉบับ (MSXTHA102.ROM) เขียนขึ้นสำหรับ MSX1 ล้วน ๆ แล้วพยายามพอร์ตไป MSX2 ด้วยการแพตช์ทีละจุด ทำให้เกิด
บั๊กสะสมหลายชั้น (ตามที่เอกสาร `MSXTHA102_analysis_TH.md` และ session ก่อนหน้าบันทึกไว้) ต้นตอบั๊กเกือบทั้งหมด
มาจาก 2 pattern ที่อันตรายในตัวเอง:

1. **เรียก BIOS internal address ตรง ๆ** (ไม่ใช่ official jump table ที่ 0x0000-0x0141 ต้น ROM) — internal
   address ต่างกันทุก generation (MSX1/MSX2/MSX2+) เพราะ layout ภายในของ BIOS แต่ละรุ่นไม่เหมือนกัน
2. **ค้นหา/แก้ return address บน call stack แบบไม่มีขอบเขต** (unbounded stack search) เพื่อ hijack ให้ BIOS
   ข้ามขั้นตอนบางอย่าง — ถ้าค่าที่ค้นหาไม่ตรงกับที่คาดไว้ (เช่นกรณี MSX2+ ที่ layout เปลี่ยนไป) จะไปเขียนทับ
   stack ของฟังก์ชันอื่นโดยไม่ได้ตั้งใจ ผลลัพธ์คาดเดาไม่ได้

**กติกาการออกแบบใหม่ (บังคับใช้ทั้งไฟล์)**:

- ใช้ **official BIOS jump table เท่านั้น** (0x0000-0x0141, เช่น `CHPUT`=0x00A2, `RDVRM`=0x004A,
  `WRTVRM`=0x004D, `CALPAT`=0x0084/`CALATR`=0x0087/`CALADR`=0x008A บน MSX2 ฯลฯ) หรือ **system variable ที่มี
  เอกสารรองรับ** (เช่น `CGPNT` 0xF91F, `LINL40`/`LINL32`, `CSRY`/`CSRX`, `EXPTBL`, `SCRMOD`) — ห้ามเรียก
  internal address ที่ไม่ได้อยู่ใน jump table เด็ดขาด
- ตรวจ generation ผ่าน **`EXPTBL[0]` bit 7 หรือ `VDP(1)` / `MSXVER` (0x002D)** ซึ่งเป็นวิธีมาตรฐานที่มีเอกสาร
  รองรับ ไม่ใช่การเดาจาก layout ภายใน
- **ไม่ใช้เทคนิค stack-search-hijack เลย** — การแทรก hook จะใช้กลไก `H.xxx` (H.CHGE=0xFDC2, H.KEYC=0xFDCC)
  ตามมาตรฐานเท่านั้น และถ้า hook ต้องการ "ทำงานแทน" BIOS บางส่วน จะออกแบบให้ hook คืนค่าแบบที่ BIOS คาดหวังไว้
  ปกติ (เช่นคืน carry flag / register ตามสเปกของจุด hook นั้น) แทนการไปแก้ stack ของผู้เรียก
- การวาดตัวอักษร Thai 3-ชั้น (glyph compositing) จะไม่อ่าน/เขียน VRAM ตรง ๆ ด้วย address ภายใน แต่จะ**คำนวณ
  ตำแหน่ง VRAM เองจาก CSRX/CSRY + LINLEN + ฐาน pattern-table ที่ถูกต้องของแต่ละ generation** แล้วเรียก
  `RDVRM`/`WRTVRM` ผ่าน jump table — สูตรคำนวณตำแหน่งเป็นคณิตศาสตร์ตรงไปตรงมา (ไม่ใช่ internal BIOS
  behavior ที่เปลี่ยนไปมาระหว่างรุ่น) จึงพอร์ตได้ทันทีโดยไม่ต้องพึ่ง internal function ที่ไม่มีเอกสาร

## 1. สินทรัพย์ที่ดึงมาจากต้นฉบับ (ไม่ต้องเข้าใจความหมายทุกไบต์ — ใช้เป็น binary asset)

| ไฟล์ | จาก ROM เดิม (address) | ขนาด | เนื้อหา |
|---|---|---|---|
| `assets/font_raw.bin` | 0x5867-0x605E | 2040 ไบต์ (255 glyph x 8) | ฟอนต์ไทย รวม glyph piece สำหรับ OR-compositing |
| `assets/kbtables/kb_dispatch_0x4c7a.bin` | 0x4C7A-0x4C9E | 36 ไบต์ | ตาราง dispatch หลักตาม scan code (12 triplet) |
| `assets/kbtables/kb_shift_symbols_0x4c9e.bin` | 0x4C9E-0x4CA8 | 10 ไบต์ | สัญลักษณ์แถวเลขตอนกด Shift `)!@#$%^&*(` |
| `assets/kbtables/kb_shift4way_0x4ca8.bin` | 0x4CA8-0x4CB0 | 8 ไบต์ | ตาราง 4-way (shift state) สำหรับคีย์แถวหลัก |
| `assets/kbtables/kb_shift4way_0x4cb0.bin` | 0x4CB0-0x4CB8 | 8 ไบต์ | ตาราง 4-way สำหรับคีย์อีกแถว |
| `assets/kbtables/kb_symbolpairs_0x4cb8.bin` | 0x4CB8-0x4CCE | 22 ไบต์ | คู่สัญลักษณ์ (ไม่ shift/shift) 10 คู่ |

รายละเอียดการถอดรหัสตารางเหล่านี้อยู่ใน `notes/keyboard_extraction.md` — ยังมีบาง handler ปลายทาง
(0x4DE1, 0x4E06, 0x4E16, 0x4DD6, ตาราง 0xFB99) ที่ต้อง disassemble เพิ่มตอนลงมือ Phase 3 จริง

**ฟอนต์**: code 0-97, 99-127, 219-222 (131 ตัว) เหมือน MSX BIOS ปกติทุกไบต์ (ไม่ต้องฝังซ้ำ ใช้ font BIOS
เดิมได้ถ้าต้องการประหยัดพื้นที่ ROM แต่แนะนำให้ฝังเต็มไปเลยเพื่อความง่ายและกันปัญหา font BIOS ต่างกันระหว่าง
generation) code 98, 128-218, 223-254 (124 ตัว) เป็นของไทยโดยเฉพาะ (98 เป็นแค่ font-style tweak เล็กน้อย
ไม่เกี่ยวกับไทย)

## 2. Header และ hook ที่จะติดตั้ง

```
0x4000: "AB"
0x4002: dw INIT
0x4004: dw STATEMENT
0x4006: dw 0          ; DEVICE ไม่ใช้
0x4008: dw 0          ; TEXT ไม่ใช้
```

Hook ที่ติดตั้งตอน INIT (ผ่าน H.xxx table เท่านั้น, มาตรฐาน portable ทั้ง MSX1/2/2+):

- `H.CHGE` (0xFDC2) → routine ของเรา: รอคีย์ + กะพริบ cursor (เขียนใหม่จากศูนย์โดยใช้ jump table
  `CHGET`/cursor-on/cursor-off ที่ถูก generation — **ห้ามพึ่ง exact cursor-blink internal address แบบ
  ต้นฉบับ**)
- `H.KEYC` (0xFDCC) → routine ประกอบอักษรไทยของเรา (ใช้ตาราง section 1 + state machine ที่เขียนใหม่)
- `H.READ` (ใช้เฉพาะตอน `SYSTEM` statement ถูกเรียก, ตามต้นฉบับ)

## 3. ตัวแปร flag (RAM) — เลือกพื้นที่ใหม่ ไม่ชนกับ BASIC work area

ต้นฉบับใช้ 0xFCAC/0xFD09/0xFD0A/0xFBEB(read-only)/0xFCAB โดยไม่มีเอกสารยืนยันว่าพื้นที่เหล่านี้ปลอดภัย
100% ข้าม generation (ส่วนหนึ่งของบั๊กเดิมอาจมาจากตรงนี้) เพื่อความปลอดภัย เวอร์ชันใหม่จะ:

- ประกาศ flag ทั้งหมดเป็น `EQU` ในไฟล์เดียว (`src/equates.asm`) แยกจาก logic เพื่อย้ายง่ายถ้าพบว่าชนกับ
  ตัวแปรระบบตอนทดสอบจริง
- ใช้พื้นที่ใน page-3 RAM ที่เอกสาร MSX Technical Handbook ระบุว่าเป็น "free RAM for machine-language
  programs" (ต้องยืนยันช่วงที่แน่ชัดอีกครั้งตอน implement — ทำเป็น TODO ที่ทำเครื่องหมายไว้ใน equates.asm)
  แทนการเดาแบบต้นฉบับ
- flag ที่ต้องมี: `THAI.MODE` (THAION/THAIOFF), `PRINT.MODE` (PRINTON/PRINTOFF, 3-ระดับ vs 1-ระดับตอนแสดงผล),
  `INPUT.MODE` (INPUTON/INPUTOFF, ประกอบอักษรตอนพิมพ์เข้า vs ไม่ประกอบ), `PLOCK.MODE` (PLOCKON/PLOCKOFF)

## 4. ตาราง BASIC statement (STATEMENT vector ที่ 0x41EE เดิม)

คัดลอกชื่อคำสั่ง+นามแฝงจากต้นฉบับ (ข้อความ ASCII ธรรมดา ไม่มีปัญหาลิขสิทธิ์เชิงโค้ด เป็นแค่ชื่อคำสั่ง) แต่
เขียน handler ใหม่ทั้งหมด:

| คำสั่ง | นามแฝง | พฤติกรรมที่ถูกต้อง (แก้จากต้นฉบับ) |
|---|---|---|
| `THAION` | — | โหลดฟอนต์ไทยเข้า CGPNT (ใช้ VDP RAM ที่ถูกต้องตาม generation ผ่าน jump table), reset flag ทั้งหมด, ติดตั้ง hook — **ต้องทำงานถูกต้องบน MSX1 ด้วย** (บั๊กเดิมที่ผู้ใช้รายงานคือ PRINTON ใช้ไม่ได้บน MSX1 — ในเวอร์ชันใหม่ THAION/PRINTON จะไม่พึ่ง path ที่ mix กับ MSX2-only code เลย เพราะ compositing ใหม่เป็น generation-aware ตั้งแต่ต้น) |
| `THAIOFF` | — | คืนฟอนต์เดิม, ถอด hook (เขียนค่า default ที่ H.xxx กลับ), reset flag |
| `PRINTON` | `?ON` | เปิดโหมดแสดงผล 3 ระดับ — ทำงานเหมือนกันทุก generation (ตาม section 0) |
| `PRINTOFF` | `?OFF` | ปิด กลับ 1 ระดับ |
| `INPUTON` | `@ON` | เปิดโหมดประกอบอักษรตอนพิมพ์เข้า |
| `INPUTOFF` | `@OFF` | ปิด |
| `PLOCKON` | — | ล็อก PRINTON/PRINTOFF ไม่ให้เปลี่ยน |
| `PLOCKOFF` | — | ปลดล็อก |
| `SYSTEM` | — | ติดตั้ง H.READ hook ตามต้นฉบับ (ฟีเจอร์นี้ไม่เกี่ยวกับ Thai-specific bug ใด ๆ ที่เจอ คงพฤติกรรมเดิม) |
| `LPRINT` | — | พิมพ์ผ่านเครื่องพิมพ์ พร้อมตาราง dot-matrix ไทย (จะดึงตารางแปลงจากต้นฉบับถ้าหาเจอ ไม่งั้นสร้างจาก font_raw.bin เอง เพราะเป็นข้อมูลเดียวกัน) |
| `ANSTR(s,var)` | — | ตามเอกสาร msx.org: คืนอักษรไทยที่มีการปรับเสียง (accented) พร้อมข้อมูลปรับเสียง |
| `TNSTR(s,var)` | — | คืนสตริงอักษรไทยแบบเต็มที่มีการปรับเสียง |
| `MLSTR(s,var)` | — | คืนส่วนกลางของอักษรไทยที่มีการปรับเสียง |

(ที่มา: หน้า msx.org wiki `CALL_ANSTR`, `CALL_TNSTR`, `CALL_MLSTR` ที่ค้นระหว่างช่วงวางแผน)

## 5. การวาดตัวอักษร 3 ระดับ (glyph compositing แบบ portable) [แก้ไขแล้ว — ดู 9.17]

*** อัปเดตสำคัญ: ฉบับร่างแรกของหัวข้อนี้ (เก็บไว้เป็นประวัติด้านล่าง) เข้าใจผิดว่า PRINTON ทำงาน
ด้วยการ OR บิตแพตเทิร์นที่ระดับ pattern-generator table (ทีละ 8 แถวพิกเซลต่อ glyph) — การ
disassemble ROM ต้นฉบับจริงจัง (`printon_algorithm_report.md`) บวกกับคำยืนยันของผู้ใช้เอง
("24 บรรทัดจะเหลือ 8 บรรทัดโดยใช้ 3 line ต่อบรรทัด") พิสูจน์ว่ากลไกจริงคือการซ้อนตัวอักษร
**ข้าม 3 แถวจริงของ name table** (ไม่ใช่ pattern table) ต่อ 1 บรรทัดตรรกะ — พยัญชนะอยู่แถวของ
ตัวเอง, สระบนอยู่แถวเหนือขึ้นไป 1 แถวคอลัมน์เดียวกัน, วรรณยุกต์อยู่เหนือสระอีก 1 แถว (หรือเหนือ
พยัญชนะแถวเดียวถ้ายังไม่มีสระ) — implement จริงอยู่ที่ `src/printon.asm` ดูรายละเอียดสถาปัตยกรรม
เต็ม (รวมปัญหา "BIOS ไม่มีสัญญาณให้ hook บอกว่าจัดการเองแล้ว" ที่ต้นฉบับแก้ด้วย stack-hijack แต่
โปรเจกต์นี้แก้ด้วยการยักย้าย CSRY/CSRX ชั่วคราวแทน) ใน **9.17** ด้านล่าง

### 5.0 (ประวัติ — ผิด ไม่ได้ใช้จริง) ร่างแรก: glyph compositing ระดับ pattern table

เก็บไว้เป็นหลักฐานว่าทำไมถึงเปลี่ยนทิศทาง — **ไม่ใช่สิ่งที่ implement จริง**:

```
CALC_VRAM_ADDR:
  ; input: CSRX, CSRY (หรือพารามิเตอร์ที่ส่งมา)
  ; ต้องรู้ pattern-table base ปัจจุบัน (จาก CGPNT ไม่ใช่จาก TXTNAM ตรง ๆ — เพราะเราวาดที่ตำแหน่ง
  ;   pattern-table ของ glyph ที่ char code ปัจจุบันชี้ไป ไม่ใช่ name-table)
  ; MSX1 (TMS9918, VRAM 16K): addr = pattern_base + char_code*8 (14 บิต พอ)
  ; MSX2/2+ (V9938): เหมือนกัน แต่ RDVRM/WRTVRM ของ MSX2 BIOS เองรองรับ VRAM ได้ถึง 128K ผ่าน
  ;   mechanism มาตรฐานของมันเอง (ไม่ต้องคำนวณ extra bank bit เอง — ปล่อยให้ RDVRM/WRTVRM ทางการจัดการ)

READ_GLYPH_ROW:   ; CALL RDVRM (0x004A) ตรง ๆ
WRITE_GLYPH_ROW:  ; CALL WRTVRM (0x004D) ตรง ๆ
COMPOSITE_GLYPH:  ; อ่านทีละแถว (8 แถว), OR กับ glyph ใหม่, เขียนกลับ
```

(ปัญหาของแนวทางนี้: มันไม่ตรงกับพฤติกรรมจริงของต้นฉบับเลย — ต้นฉบับซ้อนที่ระดับ "ตัวอักษรทั้งตัว
คนละแถว name table" ไม่ใช่ "บิตแพตเทิร์นซ้อนกันในตัวอักษรเดียว")

## 6. คีย์บอร์ดไทย (Phase 3)

**สถานะ: ทำเสร็จ 2 ใน 2 ส่วนที่ไม่ต้องพึ่ง Phase 2 — ดูรายละเอียดเต็มใน section 9.8 (ปุ่มสลับโหมด) และ
9.9 (พิมพ์อักษรไทยแบบเรียบ)**

ส่วนที่ 1 — ปุ่มสลับโหมดไทย/อังกฤษ (`src/keyboard.asm`) ผ่านการติดตั้ง `H.KEYC` hook (0xFDCC)
ตอน `THAION`/ถอดตอน `THAIOFF` โดยใช้ contract ทางการของ `H.KEYC` (อ่าน scan code จาก `C`, คืนค่าใน `A`)
ตรง ๆ ไม่ใช้กลไก stack-hijack แบบต้นฉบับ (0x4C57) เลย — ทดสอบผ่านจริงบน real MSX1 BIOS ผ่าน openMSX

ส่วนที่ 2 — พิมพ์อักษรไทยแบบเรียบ/ไม่จัดระดับ (26 คีย์แถวอักษรหลัก + 12 คีย์ row1 = 21 พยัญชนะ +
14 สระ/วรรณยุกต์) ผ่านตาราง `MAIN_ROW_TABLE`/`ROW1_TABLE` ที่ดึงค่าจริงจาก ROM ต้นฉบับ (0x4EC1/0x4EB5)
ผสาน `QUEUE_PUSH_CHAR` (เลียนแบบ push-queue routine เดิมที่ 0x4E25-0x4E33 ตรง ๆ) — ดัน font code เข้าคิว
คีย์บอร์ดเหมือนอักษรอังกฤษปกติ ไม่ต้องพึ่ง VRAM compositing เลย (ดู 9.9 สำหรับหลักฐาน/การยืนยันเต็ม)

ส่วนที่ยังไม่ทำ: การ "ซ้อน" พยัญชนะ+สระ+วรรณยุกต์หลายตัวลงช่องเดียวกันแบบ 3 ระดับ (นี่คือสิ่งที่ `PRINTON`
ควบคุมจริง ๆ) ต้องรอ Phase 2 (`COMPOSITE_GLYPH`, section 5) เสร็จก่อนจึงจะออกแบบส่วนนี้ได้ถูกต้อง (ดู 9.9
หัวข้อ "ยังไม่ได้ทำ" สำหรับขอบเขตที่เหลือแบบละเอียด รวมถึง shift-state ตัวที่ 3 และแถวตัวเลข)

## 7. F-key label (Phase 4)

ต้นฉบับมีบั๊ก DSPFNK (เรียก jump table แบบไม่มีเงื่อนไข ทั้งที่ MSX2 DSPFNK ใช้ state ภายในคนละแบบกับ MSX1
โดยสิ้นเชิง) — เวอร์ชันใหม่จะ**เขียน F-key label routine ของตัวเองทั้งหมด** (ไม่เรียก DSPFNK ของ BIOS เลย)
โดยวาดผ่าน `COMPOSITE_GLYPH`/`WRITE_GLYPH_ROW` เดียวกับ section 5 ตรง ๆ ไปที่บรรทัดล่างสุดของจอ — วิธีนี้
portable โดยธรรมชาติเพราะไม่พึ่ง BIOS internal DSPFNK เลย

## 8. แผนการ implement (อ้างอิง TaskList ใน session)

1. **Phase 1** — skeleton + THAION/THAIOFF (font install ผ่าน CGPNT) + flag statement ทั้งหมด (ไม่มี
   glyph compositing ก่อน แค่เปิด/ปิด flag ให้ถูก) → ทดสอบ boot ผ่าน emulator
2. **Phase 2** — glyph compositing (section 5) → ทดสอบวาดตัวอักษรไทย 3 ระดับบน MSX1 emulator ก่อน
   (ง่ายสุด) แล้วค่อยยืนยัน MSX2/MSX2+
3. **Phase 3** — คีย์บอร์ด (section 6)
4. **Phase 4** — F-key label, LPRINT, ANSTR/TNSTR/MLSTR (section 7 + 4)
5. **verification** — ทดสอบข้าม 3 generation ด้วย `emutest.py`

## 9.5 สถานะปัจจุบัน (อัปเดตหลัง Phase 1 เสร็จและทดสอบผ่านจริง)

- **Phase 1 เสร็จและยืนยันด้วย Z80 emulation จริงแล้ว** บน **ทั้ง MSX1 / MSX2 / MSX2+ BIOS** (ไฟล์
  `test_phase1.py`, รันผ่านหมดทั้ง 3 รุ่น): INIT, STATEMENT dispatcher (รวม alias และ PLOCKON บล็อก
  PRINTON ได้ถูกต้อง), PRINTON/PRINTOFF/INPUTON/INPUTOFF/PLOCKON/PLOCKOFF, และ **THAION/THAIOFF แบบเต็ม
  รวมการโหลดฟอนต์จริงผ่าน `LDIRVM`+`CGPNT`** (ไม่ใช่แค่ toggle flag เฉยๆ ตามที่ระบุไว้เดิมใน section 3/4 —
  ปรับแผนระหว่าง implement ให้ดีขึ้น: **ไม่ต้องมี RAM buffer สำรองฟอนต์เดิมของเราเอง** ใช้วิธีเรียก
  `INITXT` (jump table 0x006C) ซ้ำตอน THAIOFF แทน ซึ่ง BIOS จะ reload ฟอนต์มาตรฐานเข้า VRAM ให้เองเป็น
  ผลข้างเคียงของการ init หน้าจอ — ปลอดภัยกว่าการเดาตำแหน่ง RAM ว่างเอง)
- **บั๊ก harness ที่เจอและแก้แล้วระหว่างทดสอบ** (บันทึกไว้ใน `emutest.py` เป็นคอมเมนต์ด้วย):
  1. `m.halted` ค้างข้ามการเรียก `run()` หลายรอบบน machine เดิม — ต้อง reset `m.halted=False` ก่อนทุกครั้ง
  2. `run()` คืนค่าเมื่อถึง "event" (เช่น จบ 1 เฟรมวิดีโอ ~60Hz) ไม่ใช่แค่ตอน HALT หรือหมด
     `ticks_to_stop` — งานที่กิน tick นานกว่า 1 เฟรม (เช่น copy ฟอนต์ 2040 ไบต์, `INITXT`) ต้องเรียก `run()`
     วนซ้ำจนกว่า `m.halted` หรือ event กลับเป็น 0
  3. หน่วยความจำ RAM ที่สร้างขึ้นมาเปล่าๆ (ไม่ผ่าน boot จริง) ทำให้ตาราง hook `H.xxx` เป็นศูนย์ล้วน — ถ้า
     routine ที่ทดสอบกินเวลาข้ามเฟรมและมี interrupt (RST 0x38) เกิดขึ้นจริงระหว่างนั้น จะ "ไถล" ผ่าน
     ตาราง hook ที่เป็นศูนย์(ตีความเป็น NOP) จนหลุดไปรันข้อมูลฟอนต์เป็นโค้ดมั่ว ๆ — แก้โดยเติม `RET`
     (0xC9) ลงในช่วงตาราง hook มาตรฐาน (0xFD9A-0xFFFF) ให้เหมือนสภาพ RAM หลัง boot จริง
- ไฟล์ที่เกี่ยวข้อง: `src/*.asm` (source ทั้งหมด), `build.sh`, `test_phase1.py`, `../emutest.py` (harness
  ที่แก้ปรับปรุงแล้ว, ใช้ร่วมกับโปรเจกต์เก่าได้ด้วย)
- **ยังไม่ได้ทำ**: Phase 2 (glyph compositing วาดตัวอักษรไทย 3 ระดับจริง), Phase 3 (คีย์บอร์ด), Phase 4
  (F-key label, LPRINT, ANSTR/TNSTR/MLSTR)

### 9.6 (ประวัติ) การไล่หาตัวแปรระบบก่อนมี real-hardware test — ดู 9.7 สำหรับผลสรุปจริง

ช่วงแรกไล่หา CSRX/CSRY/CGPNT ผ่าน `emutest.py` (harness เบา ๆ ไม่ได้ boot เครื่องจริง) เจอว่า CSRX/CSRY
(0xF3DD/0xF3DC) ถูกต้องอยู่แล้ว แต่ CGPNT อ่านได้ค่า 0 ตลอด (สรุปตอนนั้นว่าเป็นข้อจำกัดของ harness ไม่ใช่
บั๊กจริง) — **ผลจากการทดสอบบนเครื่องจริงใน 9.7 ด้านล่างยืนยันว่าที่อยู่ `CGPNT=$F91F` ที่ใช้อยู่ตอนนั้น
ผิดจริง** (ไม่ใช่แค่ข้อจำกัด harness) ที่ถูกคือ `$F924` — แก้ไปแล้ว รายละเอียดเต็มอยู่ใน 9.7

### 9.7 บั๊กจริงที่ผู้ใช้เจอบน blueMSX จริง + วิธีตามหาและแก้ (สำคัญมาก — อ่านก่อนแก้ต่อ)

ผู้ใช้พิมพ์ `call thaion` บน blueMSX จริงแล้วจอเพี้ยนทั้งจอ (เห็นตัวอักษรซ้อนกันมั่ว ๆ เต็มจอ พร้อม
"Syntax error" ปน) — ไล่ตามด้วย **openMSX จริง** (ใช้ `-machine` config ที่ชี้ไปที่ `MSX1_bios.rom` จริง
ของโปรเจกต์นี้ ไม่ใช่ C-BIOS ฟรีที่มากับ openMSX เอง เพราะ C-BIOS ปฏิเสธไม่ยอมบูต ROM ที่มี STATEMENT
vector ที่ไม่ใช่ศูนย์ — ข้อจำกัดของ C-BIOS เอง ไม่เกี่ยวกับ ROM ของเรา) พิมพ์ `call thaion` จริงแล้ว
**reproduce อาการเดียวกับที่ผู้ใช้เจอได้ 100%** ผ่าน screenshot จริง เจอ 2 บั๊กจริงซ้อนกัน:

**บั๊กที่ 1 — `CGPNT` ที่อยู่ผิด (สาเหตุจอเพี้ยน)**: โค้ดใช้ `$F91F` แต่ตัวจริงคือ **`$F924`** (ยืนยันด้วย
วิธี: หา byte pattern ของฟอนต์ smiley มาตรฐาน code 1 = `3c42a581a599423c` ใน VRAM จริงหลัง boot เจอที่
VRAM 0x0808 → pattern table base จริง = 0x0800 → ไล่หาคู่ไบต์ใน RAM ที่เก็บค่า 0x0800 เจอที่ 0xF924(lo)/
0xF925(hi) พอดี) ค่าเดิม 0xF91F ดันเก็บค่าอย่างอื่นอยู่ (0xBF00 ซึ่งไม่ใช่ VRAM address ที่ใช้ได้เลยสำหรับ
MSX1 ที่มี VRAM 16KB) ทำให้ `LDIRVM` เขียนฟอนต์ 2040 ไบต์ทับตำแหน่งผิด จน wrap เกิน VRAM ไปทับ name table
ที่ VRAM 0x0000 — **นี่คือสาเหตุจอเพี้ยนทั้งจอที่ผู้ใช้เห็น**

**บั๊กที่ 2 — carry flag กลับด้าน (สาเหตุ "Syntax error")**: แก้บั๊กที่ 1 แล้วจอไม่เพี้ยนแต่ยังขึ้น
"Syntax error" ทุกครั้ง ทั้งที่ตรวจ memory เห็นว่า flag ถูกตั้งค่าถูกต้องแล้ว (แปลว่า handler ทำงานจริง)
— ไล่ disassemble ต้นฉบับซ้ำให้ละเอียดขึ้นกว่ารอบแรก พบว่า path "สำเร็จ" จริง ๆ ของ handler อย่าง PRINTON
จบด้วย `JP 0x44FB` (ไม่ใช่กระโดดไป epilogue เดียวกับ path "ไม่พบคำสั่ง") และ 0x44FB ทำ `POP HL; XOR A;
EI; RET` (**carry=0**) ส่วน path "ไม่พบคำสั่ง" จริง (0x422A) ทำ `XOR A; SCF; POP HL; EI; RET` (**carry=1**)
— ตอนแรกเข้าใจ polarity กลับด้านสนิท (คิดว่า carry=1=สำเร็จ) แก้เป็น carry=0=สำเร็จ, carry=1=ไม่พบ ตามนี้
แล้วปัญหาหายหมด

**วิธียืนยัน (สำคัญสำหรับงานต่อไป)**: ใช้ openMSX จริงกับ `MSX1_bios.rom`/`MSX2_bios.rom` ของโปรเจกต์นี้
(ไม่ใช่ C-BIOS) ผ่าน custom machine config ที่ `/root/.openMSX/share/machines/UserMSX1_test.xml` (ชี้ตรง
ไปที่ path ของไฟล์ BIOS แทนใช้ ROM-pool sha1 lookup ที่หา `user_msx1_bios.rom` ไม่เจอ) พร้อม TCL script
พิมพ์คำสั่งจริงผ่าน `type "..."` + เช็คด้วย `screenshot -raw` และ `debug read memory <addr>` — วิธีนี้
**เชื่อถือได้กว่า `emutest.py` มาก** เพราะผ่าน boot เต็มระบบจริง (ตัวแปรระบบทุกตัวถูกตั้งค่าจริงตามที่ BASIC
cold-boot ทำ) ควรใช้เป็นขั้นตอนสุดท้ายก่อนส่งมอบทุกครั้งจากนี้ไป ไม่ใช่พึ่ง `emutest.py` อย่างเดียว

**ผลตรวจสอบหลังแก้ทั้ง 2 บั๊ก (ยืนยันบน real MSX1 BIOS ผ่าน openMSX จริง)**:
- `call thaion` → `Ok` (ไม่มี Syntax error, ไม่มีจอเพี้ยน)
- ฟอนต์ที่ VRAM 0x0800 หลัง `call thaion` **ตรงกับ `font_raw.bin` ทุกไบต์ (2040/2040)**
- `call printon` / `call printoff` / `call plockon` → `Ok` ทุกคำสั่ง
- `call plockon` แล้ว `call printon` ซ้ำ → `PRINT_MODE` ยังเป็น 0 ถูกต้อง (บล็อกจริง)
- `call thaioff` → เคลียร์จอ + คืนฟอนต์เดิม (ผลข้างเคียงที่ตั้งใจจาก `INITXT`)

**ผลกระทบต่อ Phase 2**: ข้อสงสัยเรื่อง CGPNT ใน section 9.6 (เดิม) **คลี่คลายแล้ว** ไม่ใช่ข้อจำกัดของ
harness แต่เป็นบั๊กจริงที่แก้แล้ว — ตอนนี้มั่นใจว่า pattern table base ที่ถูกต้องคือ `CGPNT=$F924` และ
name table base น่าจะเป็น `$F922` (ค่า 0x0000 ตอน boot ปกติ ตรงกับที่คาด แต่ยังไม่ได้ทดสอบ WRTVRM เขียนจริง
ที่ตำแหน่งนี้) **ขั้นตอนต่อไปก่อนเขียน Phase 2**: ยืนยัน NAMPNT($F922) ด้วยวิธีเดียวกัน (เขียนอักษรทดสอบ
ผ่าน CHPUT แล้วดูว่า VRAM ที่ NAMPNT+offset เปลี่ยนตามจริงไหม) โดยใช้ openMSX จริงแบบเดียวกับที่ทำสำเร็จ
ในหัวข้อนี้ ไม่ใช้ `emutest.py` เปล่า ๆ อีกต่อไปสำหรับจุดที่พึ่งค่า system variable จริง

### 9.8 Phase 3 ส่วนที่ 1 — ปุ่มสลับภาษาไทย/อังกฤษ (H.KEYC hook) — เสร็จแล้ว ทดสอบผ่านจริง

ผู้ใช้รายงานว่า "font ไทยโหลดได้แล้ว (บั๊กที่แก้ใน 9.7 แก้ปัญหานี้จริง) แต่ยังขาดการสลับภาษา และการจัดระดับ
PRINTON" — สองอย่างนี้ตรงกับ Phase 3 (คีย์บอร์ด) และ Phase 2 (glyph compositing) ที่ยังไม่ได้ทำตามแผนเดิม
ผู้ใช้เลือกให้เริ่ม Phase 3 ก่อน หัวข้อนี้บันทึกเฉพาะส่วนที่ทำเสร็จจริงรอบนี้ (ปุ่มสลับโหมด) และขอบเขตที่ยัง
เหลือ (การประกอบตัวอักษรไทยเต็มรูปแบบ)

**ขอบเขตที่ทำจริงตอนนั้น (รอบแรก)**: เฉพาะปุ่มสลับโหมดไทย/อังกฤษ (`INPUT_MODE` flag เดียวกับที่
`INPUTON`/`INPUTOFF` ควบคุมอยู่แล้ว) ผ่านการติดตั้ง `H.KEYC` hook ตอน `THAION` และถอดตอน `THAIOFF` —
ตอนนั้นยังไม่ได้ทำการประกอบตัวอักษรไทยตอนพิมพ์ เพราะเข้าใจผิดว่าการพิมพ์อักษรไทย "ทุกกรณี" ผูกติดกับกลไก
VRAM compositing ของ Phase 2 โดยตรง (สมมติฐานนี้ผิด — ดูการแก้ไขใน section 9.9 ด้านล่าง ผู้ใช้ชี้แจงว่า
"ถึงไม่เป็น printon ต้องสามารถพิมพ์อักษรภาษาไทยแบบไม่จัดระดับได้" และสืบเพิ่มพบว่าถูกต้องจริง — การพิมพ์
ตัวอักษรเดี่ยวแบบไม่ซ้อนระดับไม่ต้องพึ่ง Phase 2 เลย ทำเสร็จแล้วใน section 9.9)

**สิ่งที่ยืนยันจาก disassembly จริง (ไม่ใช่การเดา)**:

1. รูปแบบเรียก `H.KEYC` จริง — disassemble `MSX1_bios.rom` ที่ 0x1021-0x1032 พบ BIOS เรียก
   `LD A,C; LD HL,<table>; CALL 0xFDCC` แล้วเดินตาราง dispatch ของตัวเองต่อโดยใช้ค่า `A` ที่ hook
   คืนมา — **และเรียกจาก interrupt-driven keyboard scan ทุกเฟรม (~60 ครั้ง/วินาที) ต่อเนื่องตลอดเวลา
   ที่ hook ติดตั้งอยู่** (`C=0xFF` เวลาไม่มีคีย์กด) ไม่ใช่แค่ตอนมีคนกดคีย์จริงเท่านั้น
2. ปุ่มสลับโหมดจริงคือ scan code `0x34` (จากตาราง dispatch เดิมที่ 0x4C7A) ชี้ไปที่ handler 0x4DEE ซึ่ง
   toggle flag ที่ `0xFCAC` (ตรงกับ `INPUT_MODE` ของเราพอดี) แล้วคืน `A=0` ก่อน `RET` — ค่า `A=0` คือ
   sentinel "ไม่มีอะไรให้ queue" ที่ต้นฉบับใช้เอง
3. เสียง/ไฟ click ตอนสลับโหมด — ต้นฉบับเขียนไปที่ PSG port `0xA0`/`0xA1`/`0xA2` ตรง ๆ — พอร์ตมาตรฐานแบบนี้
   portable ข้าม generation จริง (ต่างจาก internal ROM address) จึงเก็บพฤติกรรมนี้ไว้ได้อย่างปลอดภัย

**ทำไมไม่ใช้กลไก stack-hijack ที่ 0x4C57 ของต้นฉบับ**: ต้นฉบับมี stub พิเศษก่อนเข้า logic จริงที่ค้นหา
return address บนสแตก (`LD DE,0x1028` เทียบกับตำแหน่งไบต์ถัดจาก `CALL 0xFDCC` ใน BIOS) — เทคนิคภายในที่
เปราะบางแบบนี้ขัดกับหลักการออกแบบของโปรเจกต์ (section 0) เวอร์ชันใหม่ใช้ contract ทางการของ `H.KEYC`
(อ่าน `C`, คืนค่า `A`) ตรง ๆ แทน ไม่ต้องพึ่งกลไกนี้เลย

**บั๊กจริงที่เจอระหว่างทดสอบจริง (ทั้งสองข้อแก้แล้ว)**:

1. **HL ต้องคืนค่าเดิมทุกสาขาของ `KEYC_HOOK`** — เวอร์ชันแรกของสาขา toggle ใช้ `LD HL,INPUT_MODE` แล้ว
   ไม่คืนค่า พอ `RET` กลับไปที่ BIOS caller (0x1028) ซึ่งทำ `CP (HL)` ต่อทันทีโดยใช้ `HL` เป็น pointer เดิน
   ตาราง dispatch ของ BIOS เอง — `HL` ที่ถูกเปลี่ยนทำให้ BIOS เดินหลุดไปอ่านหน่วยความจำมั่ว ๆ เป็น "ตาราง
   dispatch" ปลอม ๆ คีย์บอร์ดใช้งานไม่ได้อีกเลยหลังกดปุ่มสลับโหมดครั้งแรก แก้โดยเลี่ยง `HL` ทั้งหมดในสาขา
   toggle ใช้ `LD A,(nn)/LD (nn),A` แบบ absolute address แทน (ยืนยันด้วย unit test ที่เช็ค `HL` ไม่เปลี่ยน
   ใน `test_phase3_keyboard.py`)
2. **การ patch hook table ที่กำลังถูกเรียกใช้งานอยู่จริงต้องทำอย่างปลอดภัย** — `KEYC_INSTALL`/
   `KEYC_UNINSTALL` ครอบด้วย `DI`/`EI` และเรียงลำดับการเขียน 3 ไบต์ให้ "ทุกจังหวะระหว่างเขียนยังเป็นโค้ด
   ที่ปลอดภัยเสมอ" (ตอนติดตั้ง เขียนไบต์ address ก่อน แล้วเขียน opcode `$C3` ทีหลังสุด — ตอนถอด เขียน
   opcode เดิมคืนก่อน) เพราะ `H.KEYC` ถูกเรียกถี่ระดับ 60Hz ระหว่างที่กำลัง patch อยู่จริง

**ข้อค้นพบสำคัญเรื่องวิธีทดสอบ (ไม่ใช่บั๊กในโค้ด แต่กระทบทุกการทดสอบคีย์บอร์ดต่อจากนี้)**: openMSX's
`type` command (= `type_via_keyboard`) จำลองการกด/ปล่อยคีย์จริงผ่าน keyboard matrix ซึ่งพึ่ง interrupt-
driven scan ที่เรียก `H.KEYC` ทุกเฟรมตามข้อ 1 ข้างต้น — ทดสอบพบว่าแค่ติดตั้ง hook ที่ "JP ไปที่ไหนสักที่
แล้ว RET เฉย ๆ" (ไม่มี logic เพิ่มเลย ไม่ใช่บั๊ก race-condition เพราะยืนยันด้วย DI/EI + เรียงลำดับเขียน
ปลอดภัยแล้วอาการเดิมยังอยู่) ก็ทำให้ `type` พิมพ์คำสั่งถัดไปไม่ขึ้นบนจอเลย — bisect ทีละขั้นแล้วสรุปว่าเป็น
ความไวต่อ **latency** ของกลไกจำลอง keyboard matrix ของ openMSX เอง (เพิ่ม `JP` หนึ่งจังหวะเข้าไปใน chain
ที่ถูกเรียกถี่ระดับ 60Hz ก็รบกวนการจำลองของ openMSX แล้ว) ไม่ใช่บั๊กจริงในโค้ดของเรา — สลับไปใช้
`type_via_keybuf` (poke ตัวอักษรเข้า keyboard buffer ตรง ๆ ไม่ผ่าน matrix scan) สำหรับพิมพ์คำสั่งทุกตัว
"หลัง" ติดตั้ง hook แล้วพบว่า **ใช้งานได้ปกติสมบูรณ์** — `test_full_regression.tcl` อัปเดตให้ใช้
`type_via_keybuf` สำหรับคำสั่งหลัง `thaion` แล้ว **กติกาจากนี้ไป**: การทดสอบอะไรก็ตามที่เกี่ยวกับ
keyboard hook บน openMSX ให้ใช้ `type_via_keybuf` เสมอสำหรับข้อความที่พิมพ์ "หลัง" ติดตั้ง hook (ส่วน
ก่อนติดตั้ง hook ใช้ `type` ปกติได้ตามเดิม)

**ผลตรวจสอบสุดท้าย (ยืนยันบน real MSX1 BIOS ผ่าน openMSX จริง, `test_full_regression.tcl`)**:
- `H_KEYC` opcode หลัง `thaion` = `0xC3` (JP, hook ติดตั้งแล้ว) — หลัง `thaioff` = `0xC9` (RET, ถอดคืนแล้ว)
- คำสั่ง `print 111+222` (พิมพ์ผ่าน `type_via_keybuf` หลังติดตั้ง hook) คำนวณถูกต้อง (`333`) ไม่มีอะไรเพี้ยน
- ลำดับคำสั่งเต็ม `thaion → printon → printoff → plockon → thaioff` ทุกตัวขึ้น `Ok` ปกติ, ฟอนต์ยัง match
  100% เหมือนเดิม (ไม่ได้ regression จาก Phase 1)
- หลัง `thaioff` พิมพ์ด้วย `type` ปกติ (matrix) กลับมาใช้งานได้ตามเดิม (hook ถูกถอดจริง ไม่ใช่ถอดแค่บนกระดาษ)
- ยืนยันตรรกะปุ่มสลับโหมด (`KEYC_HOOK` เมื่อ `C=0x34`) ผ่าน `test_phase3_keyboard.py` (unit test บน
  `emutest.py`, ไม่ใช่ openMSX เพราะ `type`/`type_via_keybuf` ทั้งคู่ไม่สามารถจำลอง scan code ดิบเฉพาะจุด
  แบบ 0x34 ได้ตรง ๆ ในสภาพแวดล้อมทดสอบนี้): toggle flag ถูกต้อง, คืน `A=0`, และที่สำคัญที่สุดคือ **`HL`
  ไม่เปลี่ยนแปลง** ทั้งสาขา toggle และสาขา passthrough (ล็อกบั๊กข้อ 1 ไว้ไม่ให้กลับมาอีก)

**อัปเดต (แก้ไขสมมติฐานข้างต้น)**: ดู section 9.9 — การพิมพ์อักษรไทยแบบเรียบ/ไม่จัดระดับ (consonant +
สระ/วรรณยุกต์เดี่ยว ๆ ไม่ซ้อนหลายตัวในช่องเดียว) **ทำเสร็จและทดสอบผ่านแล้ว** ไม่ต้องรอ Phase 2 ส่วนที่ยัง
เหลือจริง ๆ คือการ "ซ้อน" หลายเครื่องหมายในช่องเดียว (Phase 2 / `PRINTON` leveling) เท่านั้น

### 9.9 Phase 3 ส่วนที่ 2 — พิมพ์อักษรไทยแบบเรียบ (ไม่จัดระดับ) — เสร็จแล้ว ทดสอบผ่านจริง

ผู้ใช้แก้สมมติฐานที่ผิดใน 9.8: "ถึงไม่เป็น printon ต้องสามารถพิมพ์อักษรภาษาไทยแบบไม่จัดระดับได้" — สืบเพิ่ม
เติมแล้วพบว่าถูกต้อง: การพิมพ์ตัวอักษรเดี่ยว ๆ (ไม่ซ้อนหลายเครื่องหมายในช่องเดียว) เป็นคนละกลไกกับ VRAM
OR-compositing ของ `PRINTON` — เป็นแค่การ "ดัน font code ที่ถูกต้องเข้าคิวคีย์บอร์ด" ตรง ๆ เหมือนอักษร
อังกฤษปกติทุกประการ ไม่ต้องพึ่ง Phase 2 เลย

**วิธียืนยัน scan-code-to-font-code mapping (ความมั่นใจสูงมาก ไม่ใช่การเดา)**:

1. Disassemble handler แถวอักษรหลัก (`MSXTHA102_original_16k.rom` 0x4D4C, scan `0x16`-`0x2F`) พบว่า
   "compact index" ที่คำนวณจาก `C - 0x15` (ได้ 1-26) ไม่ได้ถูกแปลงเป็น font code ทันทีตอนกดคีย์ (ตรงนี้
   คือจุดที่เข้าใจผิดไว้ตอนแรกใน 9.8) แต่มีตาราง data แปลง "compact index → font code" อีกชั้นแยกต่างหาก
2. เทียบ compact index (1-26) กับตาราง keyboard matrix มาตรฐานของ MSX International
   (map.grauw.nl/articles/keymatrix.php) พบว่าเรียงตามตัวอักษร A-Z พอดี (เรียง row2..row5 ตาม
   bit0..bit7 แล้วตัดคีย์ที่ไม่ใช่ตัวอักษรออก จะได้ A..Z เรียงลำดับตรงกับ compact index 1-26 เป๊ะ) —
   ยืนยันอิสระจากการอ่านโค้ด ไม่ใช่แค่เดาจาก pattern ตัวเลข
3. ดึง Kedmanee layout มาตรฐาน (สาธารณะ, เอกสารจริง) จาก `xkeyboard-config` (`symbols/th`, X.Org/XKB) —
   ได้ผลตรงกับ mnemonic ที่รู้จักกันดี (แถว home row A S D F G H J K L ; ' = ฟ ห ก ด เ ้ ่ า ส ว ง)
4. ค้นหา byte pattern ในไฟล์ ROM ต้นฉบับที่ "ควรจะเป็น" ตามข้อ 2+3 ผสมกับ font code พยัญชนะที่ยืนยันแล้ว
   จากการ render `assets/font_raw.bin` (161=ก...206=ฮ เรียงตามตัวอักษร) — พบตารางจริง 2 ตาราง:
   - `0x4EC1` (26 ไบต์): compact index 1-26 (A-Z) → font code สำหรับแถวอักษรหลัก
   - `0x4EB5` (12 ไบต์): scan `0x0A`-`0x15` (12 คีย์: `-=\[];'` \` `,./` DEAD) → font code สำหรับ row1
   ตรงกับค่าที่คำนวณไว้ล่วงหน้า **ครบทั้ง 21 ตำแหน่งที่เป็นพยัญชนะ** (ฟ,ก,ด,ร,ส,ท,น,ย,พ,ห,อ,ป,ผ,ข,ช,บ,
   ล,ว,ง,ม,ฝ) พร้อมกันทั้งสามอย่าง (ลำดับ matrix, layout Kedmanee, ลำดับฟอนต์) — ยืนยันว่าถูกต้องแน่นอน
   ตำแหน่งที่เหลือ (สระ/วรรณยุกต์ 14 ตัว: ิ แ ำ เ ้ ่ า ื ๆ ะ ี ไ ั ใ) ไม่มีทางคำนวณล่วงหน้าได้ (font
   code ของสระ/วรรณยุกต์ไม่เคยยืนยันมาก่อน) จึงดึงค่าจริงจากตาราง ROM ตรง ๆ แทน — เป็นการ reuse "ข้อมูล"
   ตามหลักการโปรเจกต์ (section 0) ไม่ใช่การ reuse กลไก implementation ที่เปราะบาง (การอ่านค่า data คงที่
   ไม่เกี่ยวกับ stack-hijack/internal call ใด ๆ เลย)

**ตำแหน่งที่ต้นฉบับเองก็ไม่ได้ map เป็นไทย** (ปล่อยผ่านเป็นค่าเดิม/ASCII ปกติ, ไม่ implement เพิ่ม): row1
ตำแหน่ง `\` (ให้ค่า graphic code 130 ไม่ใช่ ฃ), `` ` `` (ให้ ASCII `;`), และ DEAD key (0xFF = ไม่มี mapping)

**Shift**: ต้นฉบับ (`0xFBEB AND 3`) ให้ค่า 0/1=โหมดไทย, 2/3=fallback อังกฤษตัวพิมพ์ใหญ่เสมอ (ยืนยันจาก
0x4D61: `ADD A,0x40` แปลง compact index เป็น `'A'-'Z'` พอดี) — เวอร์ชันใหม่เลียนแบบกฎเดียวกัน: กด Shift
ค้างอยู่ = ปล่อยผ่านให้ BIOS จัดการแบบเดิม (ได้อังกฤษตัวใหญ่ตามปกติของแป้นพิมพ์อยู่แล้วโดยไม่ต้องเขียนโค้ด
แปลงเอง)

**Implementation**: `src/keyboard.asm` — เพิ่มตาราง `MAIN_ROW_TABLE`/`ROW1_TABLE` (คัดลอกค่าจริงจาก ROM
ตรง ๆ ตามข้อ 4) และ routine `QUEUE_PUSH_CHAR` (เลียนแบบ 0x4E25-0x4E33 ของต้นฉบับ: เขียน font code ไปที่
`(KEYQ_TAIL)`=`(0xF3F8)`, เลื่อน pointer แบบ circular ผ่าน `KEYQ_ADVANCE`=`CALL 0x10C2` — ยืนยันด้วยการ
disassemble `MSX1_bios.rom` เองที่ 0x10C2 ว่าเป็น `INC HL / LD A,L / CP 0x18 / RET NZ / LD HL,0xFBF0 /
RET` คือ routine เลื่อน pointer บัฟเฟอร์คีย์บอร์ดแบบ circular ของ BIOS จริง ไม่ใช่ address ที่เดาไว้ลอย ๆ
— แล้วเช็คขอบเขตที่ `(KEYQ_END)`=`(0xF3FA)`) `KEYC_HOOK` ขยายให้เช็ค `THAI_MODE`+`INPUT_MODE`+
`SHIFT_STATE` ก่อน แล้ว dispatch ไปตาราง `MAIN_ROW_TABLE`/`ROW1_TABLE` ตาม scan code — `HL` ยังคง
preserve ทุกสาขาเหมือนเดิม (ไม่กลับไปเป็นบั๊กข้อ 1 ของ 9.8)

**ผลตรวจสอบ (`test_phase3_typing.py`, unit test บน `emutest.py` — วิธีเดียวกับที่ 9.8 ใช้ยืนยันปุ่มสลับ
โหมด เพราะ `type`/`type_via_keybuf` บน openMSX ไม่สามารถจำลอง scan code ดิบเฉพาะจุดได้ตรง ๆ)**:
- แถวอักษรหลักทั้ง 26 คีย์ ดัน font code ตรงกับตาราง `0x4EC1` ที่ดึงมาทุกตัว (byte-exact)
- row1 คีย์ที่มี mapping (เช่น `-` → ข) ดันเข้าคิวถูกต้อง — คีย์ที่ไม่มี mapping (DEAD) ปล่อยผ่านจริง ไม่มี
  อะไรหลุดเข้าคิว
- กด Shift ค้าง → fallback อังกฤษ (ไม่ดันอักษรไทย) ตรงตามพฤติกรรมต้นฉบับ
- `THAI_MODE`/`INPUT_MODE` ปิดอยู่ → ปล่อยผ่านปกติ ไม่กระทบการพิมพ์อังกฤษทั่วไป
- ปุ่มสลับโหมด (`KEYC_TOGGLE_CODE`) ยังทำงานถูกต้องเหมือนเดิมหลังเพิ่มโค้ดส่วนนี้ (regression test ผ่าน)
- `HL` ถูก preserve ทุก branch ที่ทดสอบ (ล็อกบั๊กข้อ 1 ของ 9.8 ไว้ไม่ให้กลับมา)
- รัน `test_full_regression.tcl` (openMSX จริง, real MSX1 BIOS) ซ้ำหลังแก้ — ฟอนต์ยัง match 100%,
  `H_KEYC` install/uninstall ยังถูกต้อง, ลำดับคำสั่งเต็มยัง `Ok` สะอาดทุกตัว (ไม่มี regression)

**บั๊กจริงที่ผู้ใช้เจอทันทีหลังส่งมอบรอบแรก (แก้แล้ว) — "call thaion แล้วพิมพ์อะไรไม่ได้เลย"**: หลังส่งมอบ
โค้ดในหัวข้อนี้รอบแรก ผู้ใช้รายงานทันทีว่าพิมพ์อะไรไม่ได้เลยหลัง `call thaion` — ไล่กลับไปดู `KEYC_HOOK`
พบว่า `.passthrough` (จุดที่ปล่อยผ่านให้ BIOS จัดการแบบเดิมเมื่อไม่ใช่ปุ่มสลับโหมด/ไม่ใช่การพิมพ์อักษรไทย)
ไม่ได้คืนค่า `A=C` (scan code เดิม) ก่อน `JP PREV_KEYC` — จุดที่เพิ่มเข้ามาใหม่ในหัวข้อนี้ (เช็ค `THAI_MODE`/
`INPUT_MODE`/`SHIFT_STATE` ก่อนถึง `.passthrough`) แต่ละจุดทำ `LD A,(flag)` ทับค่า `A` เดิมไปเรื่อย ๆ โดยไม่
มีการคืนค่า — เนื่องจาก `THAION` ตั้ง `THAI_MODE=TRUE` ทันที แต่ `INPUT_MODE` เริ่มต้นเป็น `FALSE` (ผู้ใช้
ยังไม่กดปุ่มสลับโหมด) ทุกคีย์ **และทุก idle poll ที่เกิดขึ้น ~60 ครั้ง/วินาทีต่อเนื่อง** จะตกเงื่อนไข
"`INPUT_MODE=0` → ปล่อยผ่าน" ทันที โดย `A` ถูกเขียนทับด้วยค่า `INPUT_MODE` (=0) แทนที่จะเป็น scan code จริง
(หรือ `0xFF` สำหรับ idle) — BIOS เอาค่า `A` ที่ผิดนี้ไปเดินตาราง dispatch ของตัวเองต่อทุกเฟรม ทำให้คีย์บอร์ด
ใช้งานไม่ได้เลยทันทีหลัง `THAION` (ไม่ต้องรอผู้ใช้กดปุ่มสลับโหมดด้วยซ้ำ) — ตรงกับอาการที่รายงานเป๊ะ

จุดที่พลาดไปตอน implement ครั้งแรก: `test_full_regression.tcl` (openMSX) ใช้ `type_via_keybuf` สำหรับพิมพ์
คำสั่งหลัง `thaion` ทั้งหมดตามธรรมเนียมที่วางไว้ใน 9.8 — แต่ `type_via_keybuf` poke ตัวอักษรเข้าไปใน
keyboard buffer **ตรง ๆ โดยไม่ผ่าน `H.KEYC` เลย** จึงไม่มีทางเจอบั๊กนี้ได้จาก openMSX regression test ชุดนี้
เลย (เป็น blind spot จริง) ส่วน unit test เดิม (`test_row1_unmapped_dead_key_passes_through` ฯลฯ) ก็พลาด
เพราะ stub `PREV_KEYC` ที่ใช้ทดสอบเขียน `A=0x77` ทับเองอยู่แล้วไม่ว่า `A` ขาเข้าจะเป็นอะไร (พิสูจน์แค่ว่ามี
การ chain เกิดขึ้นจริง แต่ไม่ได้พิสูจน์ว่า `A` ที่ chain ไปถูกต้อง) — **บทเรียน**: unit test ที่ยืนยัน
"ปล่อยผ่านแบบไม่ทำอะไร" ต้องใช้ stub ที่เป็น `RET` ล้วน ๆ (ไม่แตะ `A`) เพื่อจำลอง default hook slot ของ BIOS
จริง ไม่ใช่ stub ที่เขียนค่าทับเพื่อ "พิสูจน์ว่ามาถึง" เท่านั้น

**แก้แล้ว**: เพิ่ม `LD A,C` เป็นคำสั่งแรกของ `.passthrough` เสมอ (คืน `A=C` ก่อนส่งต่อทุกครั้ง) และครอบ
`LD C,A` ใน `.lookup` (ที่ยืม `C` เป็นตัวแปรชั่วคราวสำหรับคำนวณ index ตาราง) ด้วย `PUSH`/`POP BC` เพื่อไม่ให้
`C` (scan code เดิม) หายไปแม้ในเส้นทางที่คีย์ไม่มี mapping ไทย (ตาราง `=0xFF`) — เพิ่ม unit test ใหม่
`test_passthrough_preserves_scan_code_in_a_regression_bug3` ใน `test_phase3_typing.py` ที่ใช้ stub เป็น
`RET` ล้วน ๆ ตรวจว่า `A==C` ทุกครั้งที่ปล่อยผ่าน ครอบคลุมทุกเงื่อนไข (`THAI_MODE` ปิด, `INPUT_MODE` ปิด,
Shift ค้าง, row1 คีย์ไม่มี mapping) — **ยืนยันว่า test นี้จับบั๊กได้จริง** โดยย้อนโค้ดกลับไปเป็นเวอร์ชันก่อน
แก้แล้วรันซ้ำ (ล้มเหลวตามคาด: `AssertionError: [THAI_MODE off] scan=16: A should equal C, got 00`) ก่อน
แก้กลับและยืนยันผ่านทั้งหมด — ดูโค้ดจริงและคอมเมนต์เต็มใน `src/keyboard.asm` หัวข้อ 4 "บั๊กจริงข้อ 3"

**บั๊กจริงรอบสอง (แก้แล้ว) — ผู้ใช้ทดสอบบน MSX2: "กดปุ่มสลับโหมด cursor หาย/มา สลับได้ แต่พิมพ์อักษรไทย
แล้วไม่ขึ้นเลย"**: หลังแก้บั๊กข้อ 3 ผู้ใช้ยังเจอปัญหาอยู่ แต่คราวนี้อาการเปลี่ยนไป (ไม่ใช่พิมพ์อะไรไม่ได้เลย
แบบเดิม) — ปุ่มสลับโหมดทำงาน (สังเกตได้จาก cursor เปลี่ยนสถานะตอนกด) แต่พิมพ์อักษรไทยแล้วไม่ขึ้นอะไรเลย

ไล่กลับไปพบว่าเป็นบั๊กจริงข้อ 4: ค่า `A=0` ("XOR A") ที่ใช้เป็น sentinel "ไม่มีอะไรให้ BIOS ทำต่อ" ทั้งใน
branch toggle และ branch push อักษรไทย **ถูกยืมมาจากโค้ดต้นฉบับตรง ๆ โดยไม่ทันสังเกตว่าบริบทการเรียกต่าง
กัน** — ต้นฉบับเรียก handler ผ่านกลไก stack-hijack (`0x4C57`) ซึ่ง "ข้าม" การเดินตาราง dispatch ของ BIOS
เองไปเลย ค่า `A` ตอน `RET` จึงไม่มีผลอะไรกับ BIOS ในบริบทนั้น แต่เวอร์ชันนี้ใช้ `H.KEYC` contract มาตรฐาน
(ไม่ hijack stack) ซึ่ง BIOS **เดินตาราง dispatch ของตัวเองต่อจริง** โดยใช้ `A` ที่ hook คืนมา — ยืนยันด้วย
การ disassemble ทั้ง `MSX1_bios.rom` และ `MSX2_bios.rom` ที่ตำแหน่ง `0x1028`-`0x1031` (โค้ดเหมือนกันเป๊ะทั้ง
สองรุ่น): BIOS วนเทียบ `A` กับตาราง threshold ที่ `0x1B96` (ยืนยันว่า**เนื้อตารางเหมือนกันไบต์ต่อไบต์ทั้ง
MSX1 และ MSX2** ในส่วนที่เกี่ยวข้อง) แล้วกระโดดไป handler ที่ตรงกัน — พบว่า `A` ในช่วง 0-47 (ครอบคลุม scan
code ของแถวอักษรหลัก+row1 ที่แปลงเป็นอักษรไทยพอดี) ตกไปที่ handler `0x0F83` ซึ่งอ่าน `SHIFT_STATE`
(`0xFBEB`) เอง ใช้ `A` เป็น index เข้าตารางแปลอักษรอังกฤษ/กราฟิกของ BIOS แล้ว **push ผลลัพธ์เข้าคิว
คีย์บอร์ดของมันเองอีกรอบ** — สรุปคือค่า `A=0` ไม่ใช่ no-op เลย แต่ทำให้ BIOS push อักขระ "ที่ตำแหน่งคีย์ 0"
ของ BIOS เอง **เพิ่มเข้าไปอีกตัว** ซ้อนกับที่เรา push อักษรไทยไปแล้ว ทำให้ผลลัพธ์บนจอผิดเพี้ยน/ไม่ขึ้นเลย
และผลข้างเคียงของ handler นี้ (ที่ไปยุ่งกับ `SHIFT_STATE`/คิวของ BIOS เอง) เป็นคำอธิบายที่สมเหตุสมผลที่สุด
สำหรับอาการ cursor หาย/มา ที่สังเกตเห็นตอนกดปุ่มสลับโหมดเช่นกัน (branch toggle ก็คืน `A=0` แบบเดียวกัน)

**แก้ตอนนั้น (ภายหลังพบว่ายังผิดอยู่ — ดูบั๊กจริงข้อ 5 ด้านล่าง)**: เปลี่ยนทั้งสองจุด (branch toggle และ
หลัง `QUEUE_PUSH_CHAR`) จาก `XOR A` (`A=0`) เป็น `LD A,$FF` โดยอ้างว่าค่านี้คือ idle sentinel ตัวเดียวกับที่
BIOS ใช้เองทุกเฟรมตอนไม่มีคีย์กด และตารางที่ดึงมาแสดงว่า `0xFF` ไม่ตรงกับ threshold ใดที่จับคู่กับ handler
ที่ทำอะไรจริงจัง — สมมติฐานนี้ผิด (ดูบั๊กจริงข้อ 5) — ผู้ใช้ทดสอบ build นี้บน blueMSX แล้วยังรายงานอาการ
"พิมพ์อะไรไม่ได้เลย (ค้างสนิท)" เหมือนเดิม

**บั๊กจริงข้อ 5 (แก้แล้ว จริง ๆ รอบนี้ — แทนที่การแก้บั๊กข้อ 4 ด้านบนทั้งหมด)**: ผู้ใช้ยืนยันชัดเจนว่าทดสอบ
ไฟล์ล่าสุด (ที่มี `A=$FF`) บน **blueMSX** แล้วอาการกลับไปเป็น **"พิมพ์อะไรไม่ได้เลย (ค้างสนิท)"** — รุนแรง
กว่าอาการ "cursor หาย/มา แต่พิมพ์ไม่ขึ้น" ของบั๊กข้อ 4 เดิมด้วยซ้ำ นี่คือสัญญาณว่าการแก้บั๊กข้อ 4 (`A=$FF`)
เองมีปัญหาใหม่

ตรวจสอบซ้ำด้วยการรัน CPU emulator (Python + z80dis, harness ของโปรเจกต์เอง) เรียก routine dispatch ของ
BIOS **จริง** (`0x1021` ใน `MSX1_bios.rom` สด ๆ ไม่ผ่าน cartridge เลย แม้แต่นิดเดียว) ด้วย `A=C=0xFF`
โดยตรง พบว่า **ค้างวนลูปไม่มีวันจบ** (ทดสอบด้วย `max_ticks=500,000` ไม่ halt, PC ค้างอยู่ที่ `0x1031`) —
สาเหตุ: loop เดินตาราง threshold ที่ `0x1B96` (ดูบั๊กข้อ 4 ด้านบน) ออกจาก loop ได้ทางเดียวคือ `RET c` ซึ่ง
ต้องการ `A < threshold` แต่ `A=0xFF=255` คือค่าสูงสุดที่ byte เก็บได้ ไม่มีทาง "น้อยกว่า" threshold ใด ๆ
ได้เลยแม้แต่ entry สุดท้าย (`threshold=255` เอง `A` ก็ยังไม่น้อยกว่า `255`) — **นี่ไม่ใช่แค่กรณี idle
เท่านั้น แต่เกิดขึ้นทุกครั้งที่ hook คืนค่า `A=0xFF` กลับไป** ไม่ว่าจะเป็น branch toggle หรือ branch push
อักษรไทยสำเร็จก็ตาม เพราะทั้งสอง branch คืนค่านี้เหมือนกัน

ไล่ลึกลงไปอีกเพื่อดูว่า "idle, C=0xFF" ตามที่เข้าใจไว้ตอนแก้บั๊กข้อ 4 (จาก counter ที่ฝังทดสอบในเซสชันก่อน
หน้า) เกิดขึ้นจริงหรือไม่ โดยค้นหา **ทุกจุด** ที่เรียก `CALL 0xFDCC` (=H.KEYC) ทั่วทั้งไฟล์ ROM แบบ byte
pattern (ไม่ใช่การเดา) ทั้ง `MSX1_bios.rom` และ `MSX2_bios.rom` แยกกัน พบว่า **มีผู้เรียกเพียงจุดเดียวใน
ทั้งสองรุ่น** (ที่ `0x1025` ภายใน routine `0x1021`) และ routine `0x1021` เองก็ถูกเรียกจากจุดเดียวเท่านั้น
(`CALL c,0x1021` ที่ `0x0D9A`) ภายใน loop สแกน matrix คีย์บอร์ดจริงที่ `0x0D89`-`0x0DA2` (byte เหมือนกัน
ทุกไบต์ทั้ง MSX1/MSX2) — การเรียกนี้มีเงื่อนไข ผูกกับผลลัพธ์ของ "ค่า matrix แถวปัจจุบัน XOR ค่าที่บันทึกไว้
รอบก่อน AND ค่าเดิม" (คือ "บิตที่เพิ่งเปลี่ยนจากปล่อยเป็นกด" เท่านั้น) และค่า `C` ที่ส่งเข้ามาคำนวณจาก
`(11-row)*8+column` ซึ่งมีค่าได้แค่ `0`-`87` (`0x00`-`0x57`) เท่านั้นจากโครงสร้าง matrix 11 แถว x 8 คอลัมน์
— สรุปคือ **`C=0xFF` ไม่เคยเกิดขึ้นจริงจากเส้นทางนี้เลย** สมมติฐานเดิมที่ว่า "0xFF ปลอดภัยเพราะเป็นค่าที่
BIOS ใช้เองทุกเฟรมตอน idle อยู่แล้ว" จึงผิดตั้งแต่ต้น (ที่มาของ counter ในเซสชันก่อนหน้าน่าจะวัดอย่างอื่นที่
ไม่ใช่การเรียก H.KEYC ด้วย C=0xFF จริง ๆ) — แต่ข้อเท็จจริงนี้ไม่ใช่ประเด็นหลักอีกต่อไป เพราะบั๊กที่แท้จริง
ร้ายแรงกว่านั้นมาก: **ทุกครั้ง** ที่มีการกดคีย์จริง (ซึ่งเป็น**เหตุการณ์เดียว**ที่ทำให้ hook ถูกเรียกเลย) แล้ว
hook คืน `A=0xFF` กลับไป BIOS จะค้างทันที — ไม่ใช่กรณีทฤษฎีตอน idle แต่เป็นกรณีจริงทุกครั้งที่ใช้งาน

ไล่หาค่า `A` อื่นที่ "ปลอดภัย" ในตาราง dispatch ของ BIOS เองต่อ พบว่า**ไม่มีค่าไหนปลอดภัยจริง**: ทุก handler
ปลายทาง (`0x0F83`, `0x0F36`, `0x0F46`, `0x0F06`, `0x0FC3`) ทำงานจริงจัง (อ่าน/เขียน `SHIFT_STATE`, PSG
ports, หรือ push อักขระเข้าคิวเอง) แม้แต่ bucket ที่ดูกว้างสุด/เหมือน default (`0x0F10`, ครอบคลุม
`A=48-50,52,58-59,61-64,66-254`) ก็ไม่ใช่ no-op เสมอไป — มันเรียก hook อื่นอีกชั้น (`H.KEYI` ที่ `0xFDD1`)
แล้วใช้ผลลัพธ์นั้นเป็น index เข้าตาราง data ที่ `0x1003` เพื่อตัดสินใจว่าจะ push อักขระเพิ่มหรือไม่ — ค่า
`A>87` (นอกช่วง scan code จริงที่ BIOS ออกแบบไว้) จะทำให้ index เข้าตารางนี้หลุดไปอ่าน "ไบต์ของโค้ดจริง"
แทนข้อมูล (เพราะตารางนี้สั้นกว่าที่ `A` จะ index ถึง) กลายเป็นค่ามั่ว ๆ ที่อาจ push อักขระขยะเข้าคิว — สรุป
คือไม่มี "ค่า sentinel วิเศษ" ตัวเดียวที่ปลอดภัยกับทุกกรณีจริง ๆ

**แก้จริง**: เปลี่ยนวิธีคิดใหม่ทั้งหมด แทนที่จะพยายามหาค่า `A` ที่ "ปลอดภัย" สำหรับให้ BIOS เดินตาราง
dispatch ของมันเองต่อ ให้ **"ข้ามการเดินตาราง dispatch ทั้งก้อนไปเลย"** เมื่อ hook จัดการคีย์นั้นเองเสร็จ
แล้ว (push อักษรไทยหรือ toggle โหมดสำเร็จ) — ทำได้อย่างปลอดภัยเพราะยืนยันแล้วว่า H.KEYC ถูกเรียกจากจุดเดียว
ในทั้งไฟล์ ROM เท่านั้น (ทั้ง MSX1 และ MSX2) คือ `CALL 0xFDCC` ที่ `0x1025` — ตอน hook เริ่มทำงานจึงมี
return address พิเศษของ BIOS เองวางซ้อนอยู่บนสุดของ stack เสมอ (เดิมคือ `0x1028` ซึ่งเป็นจุดเริ่มเดินตาราง
dispatch อันตราย) เหนือ return address ของผู้เรียกตัวจริง (เดิมคือ `0x0D9D`, จุดต่อจาก `CALL c,0x1021`)
วิธีแก้คือแทนที่จะ `RET` ครั้งเดียว (ไปเจอ `0x1028`) เรา **`POP` ทิ้งค่า return address พิเศษนั้นไปเฉย ๆ
(ไม่สนใจค่า) แล้ว `RET` อีกครั้ง** — คราวนี้กลับไปเจอผู้เรียกตัวจริงโดยตรง เท่ากับ "`CALL c,0x1021` ของ BIOS
คืนค่าทันทีโดยไม่ทำอะไรเพิ่ม" ซึ่งปลอดภัย 100% เพราะโค้ดที่จุดนั้น (`POP AF; POP BC; INC C; DJNZ`) แค่ pop
ค่าที่ตัวมันเอง push ไว้ก่อนหน้ากลับคืน ไม่แตะ `A`/`HL` ที่เรา return มาเลย — วิธีนี้ไม่ต้องพึ่งค่า sentinel
ใด ๆ อีกต่อไป และไม่ต้องพึ่งพฤติกรรมของ handler ปลายทางที่อาจต่างกันระหว่าง generation ด้วย (เพราะไม่เคย
เข้าไปรันเลย) — ต่างจากกลไก stack-hijack ของต้นฉบับ (`0x4C57`) ตรงที่ไม่ได้ "ค้นหา/เดา" ตำแหน่งค่าใด ๆ บน
stack เลย รู้แน่นอน 100% ว่ามี return address พิเศษอยู่ตรงไหนเพราะยืนยันแล้วว่า H.KEYC มีผู้เรียกจุดเดียว
เท่านั้นทั้งไฟล์ ROM (ค้นหาแบบ byte-pattern ทั่วทั้งไฟล์) และ calling convention ตรงนี้เหมือนกันทุกไบต์ทั้ง
MSX1/MSX2 (ยืนยันแล้ว)

**การยืนยันการแก้บั๊กข้อ 5**: นอกจาก unit test ระดับ logic เดิม (`test_phase3_keyboard.py`,
`test_phase3_typing.py`, ปรับให้จำลอง stack สองชั้นตาม calling convention ใหม่) ยังเพิ่ม test ระดับ
"end-to-end จริง" ใหม่ (`test_phase3_e2e_biosreal.py`) ที่ติดตั้ง hook ผ่าน `KEYC_INSTALL` จริงแล้วเรียกจุด
เข้า `0x1021` ของ BIOS **ตรง ๆ** (ไม่ผ่าน `KEYC_HOOK` โดยตรงแบบ test อื่น) เหมือนที่ `CALL c,0x1021` ใน
matrix-scan loop ของ BIOS ทำทุกประการ ทดสอบทั้งปุ่มสลับโหมด (`0x34`) คีย์แถวหลัก (`0x16`) และคีย์ row1
(`0x0A`) กับทั้ง `MSX1_bios.rom` และ `MSX2_bios.rom` — ยืนยันว่า (1) ไม่ค้าง กลับไปหา caller ตัวจริงได้ครบ
ทุกเคส (2) มีอักขระถูก push เข้าคิวคีย์บอร์ด**เพียงตัวเดียว**ต่อการกดหนึ่งครั้ง (พิสูจน์ว่า BIOS's dispatch
handler ไม่ได้ถูกรันเลยจริง ๆ ไม่ใช่แค่ไม่ค้างเฉย ๆ) — รันผ่านครบทุกเคสทั้งสอง generation

**บั๊กจริงข้อ 6 (แก้แล้ว — พบหลังผู้ใช้ยืนยันว่าบั๊กข้อ 5 ยังไม่หาย "ค้างสนิท" เหมือนเดิมบน blueMSX จริง)**:
หลังส่งไฟล์ build ที่แก้บั๊กข้อ 5 (RET สองชั้นข้าม dispatch table) ไปให้ผู้ใช้ทดสอบ ยังได้รับรายงานว่า
"ยังเหมือนเดิม" / "พิมพ์อะไรไม่ได้เลย (ค้างสนิท)" อยู่ดี — ขอไฟล์ BIOS จริงที่ใช้ (`MSX2.rom` จาก machine
profile ชื่อ "MSX2-Thai" ใน blueMSX) มาตรวจสอบตรง ๆ พบว่า **เหมือนกับ `MSX2_bios.rom` ที่ใช้ทดสอบทุกไบต์
เป๊ะ** (md5 ตรงกัน) — ตัดความเป็นไปได้เรื่อง "BIOS คนละตัว/คนละ generation" ออกไปได้เลย

ขอให้ผู้ใช้เปิด Debugger ในตัวของ blueMSX ระหว่างที่ "ค้าง" แล้วกด Pause ซ้ำหลายรอบ (เพื่อแยกแยะ "ค้างจริง
ที่ PC เดิม" ออกจาก "แค่ pause ระหว่างทำงานปกติ") พบว่า **PC ค้างวนอยู่ในช่วง 0x10D9-0x10DC ตลอด** ("loop
10d9 10dc" ตามที่ผู้ใช้รายงาน) ซึ่งตรงกับโค้ดของ BIOS เอง (ไม่ใช่โค้ดของคาร์ทริดจ์เราเลย):
```
10D1: CALL 0x0d6a
10D4: JR nz,0x10e1        ; คิวมีอักขระค้างรอ consume -- ไปประมวลผลต่อตามปกติ
10D6: CALL 0x0a37
10D9: CALL 0x0d6a         ; <-- ติดอยู่ตรงนี้ วนไม่จบ
10DC: JR z,0x10d9         ; คิว "ว่าง" (PUTPNT==GETPNT ที่ 0xF3F8/0xF3FA) -- วนสแกน matrix รอคีย์ใหม่
```
นี่คือ loop มาตรฐานของ BIOS ที่ใช้ "รอจนกว่าจะมีคีย์ใหม่เข้าคิว" (วนสแกน keyboard matrix ซ้ำผ่าน `0x0d6a`
ซึ่งมี `EI` เป็นคำสั่งแรกทุกรอบ ก่อนเดินสแกน/dispatch คีย์บอร์ดจริงต่อ) — ปัญหาคือ **`0x0d6a` ทำ `EI` ใหม่
ทุกครั้งที่วนซ้ำ** และถ้าการสแกน/ประมวลผลคีย์ที่เกิดขึ้นข้างในแต่ละรอบ (ผ่าน `0x0D12` → compare loop →
`0x0D89` → `0x1021` → `H.KEYC` → **hook ของเรา**) ใช้เวลานานพอ จะเปิดช่องให้ VSYNC interrupt ตัวใหม่แทรกเข้า
มา **ระหว่าง** ที่ยังประมวลผล key event เดิมอยู่ (reentrant) — interrupt ใหม่ที่แทรกเข้ามาจะไปเจอ loop รอ
คีย์ตัวใหม่ของตัวเอง ซึ่งไม่มีทางเจอคีย์ใหม่จริง (เพราะคีย์ที่กดจริงกำลังถูกประมวลผลอยู่โดย generation ที่
เก่ากว่า ซึ่งถูกแช่แข็งอยู่ตรงนี้พอดี) — เกิดเป็น **deadlock แบบ reentrant ซ้อนกันไม่มีที่สิ้นสุด**
(generation ใหม่รอ generation เก่าให้เสร็จ แต่ generation เก่าไม่มีทาง ได้ CPU คืนเพราะ generation ใหม่ยึด
ไปแล้ว) — ตรงกับที่เห็นใน callstack ของ blueMSX debugger จริง: pattern
`23E7→10CB→...→FDCC→0D9A→0D5B→0CE5` ซ้ำกันซ้อนกันหลายรอบ ยืนยันตามทฤษฎีนี้เป๊ะ

เหตุผลที่ไม่เคยเกิดบน stock BIOS (ไม่มี cartridge): `H.KEYC` เดิมเป็นแค่ `RET` เฉย ๆ (ไม่กี่ T-state) ทำให้
แต่ละรอบของ loop รอคีย์เร็วมากจน "ช่วงเวลาเสี่ยง" ที่ interrupt ใหม่จะแทรกทันเวลาพอดีแคบมากจนแทบไม่เกิดขึ้น
จริงในทางปฏิบัติ — แต่ hook ของเรา (เช็ค flag หลายตัว + ค้นตาราง + push เข้าคิว) ใช้เวลานานกว่าพอสมควร ทำให้
"ช่วงเวลาเสี่ยง" นี้กว้างขึ้นจนโดนได้จริงบน timing ของ blueMSX (ต่างจาก openMSX ที่ไม่เคยเจอ เพราะ timing/
การจำลอง interrupt คนละแบบกัน — สอดคล้องกับที่ `test_full_regression.tcl` บน openMSX ผ่านตลอดแม้กระทั่ง
ตอนที่บั๊กนี้ยังอยู่ในโค้ด)

**แก้แล้ว**: เพิ่ม `DI` เป็นคำสั่งแรกสุดของ `KEYC_HOOK` (ก่อน `PUSH HL` ด้วยซ้ำ) แล้วเพิ่ม `EI` ก่อน return
ทุกเส้นทาง (`.lookup` สำเร็จ, `.toggle`, และ `.passthrough` ก่อน `JP PREV_KEYC`) — ปิด interrupt ตลอดช่วงที่
hook ของเราทำงานเอง (สั้นมาก ไม่กี่สิบ T-state) ปิดช่องโหว่ที่เราเป็นคนเปิดขึ้นมาเองโดยเฉพาะ โดยไม่ไปยุ่งกับ
จังหวะ `EI` อื่น ๆ ที่ BIOS ตั้งใจทำเองในส่วนอื่น — ยืนยันด้วยการ disassemble ROM ที่ build จริงว่า `DI`
(`0xF3`) อยู่เป็นไบต์แรกสุดของ `KEYC_HOOK` และ `EI` (`0xFB`) อยู่ก่อน `RET`/`JP PREV_KEYC` ทุกเส้นทางจริง
รันชุด test ทั้งหมดผ่านครบ (`test_phase1.py`, `test_phase3_keyboard.py`, `test_phase3_typing.py`,
`test_phase3_e2e_biosreal.py`, `test_full_regression.tcl` บน openMSX) — ดูโค้ดจริงและคำอธิบายเต็มใน
`src/keyboard.asm` หัวข้อ 4 "บั๊กจริงข้อ 6"

**ข้อจำกัดของ sandbox นี้ (สำคัญ ต้องบอกผู้ใช้ตรง ๆ)**: พยายามรัน end-to-end บน openMSX ด้วย MSX2 BIOS
จริง (`UserRealMSX2` machine) แต่เครื่องนี้ **บูตไม่ถึงหน้าจอ BASIC เลยในสภาพแวดล้อมทดสอบนี้** (จอเป็นสีดำ
ล้วนแม้รอนานถึง 9+ วินาที ทั้งมีคาร์ทริดจ์และไม่มีคาร์ทริดจ์ก็เหมือนกัน — พิสูจน์ว่าเป็นข้อจำกัดของ machine
config/ROM ใน sandbox นี้เอง ไม่เกี่ยวกับคาร์ทริดจ์ของเราเลย และเป็นข้อจำกัดเดิมที่มีมาตั้งแต่ก่อนหน้านี้ใน
โปรเจกต์ ไม่ใช่ regression ใหม่) จึง **ไม่สามารถยืนยันการพิมพ์จริงบน MSX2 แบบ interactive ผ่าน openMSX ใน
sandbox นี้ได้** การยืนยันสำหรับ MSX2 รอบนี้จึงอาศัย (ก) การ disassemble `MSX2_bios.rom` จริงโดยตรงเทียบ
กับ `MSX1_bios.rom` พบว่าโค้ด/ตารางที่เกี่ยวข้องทั้งหมด (`H.KEYC` calling convention, ตาราง threshold ที่
`0x1B96`, `KEYQ_ADVANCE` ที่ `0x10C2`) เหมือนกันไบต์ต่อไบต์ และ (ข) รัน unit test ชุดเต็มด้วย
`MSX2_bios.rom` เป็น system ROM ผ่าน `emutest.py` ผ่านหมดทุกเคส — ยังไม่ใช่การพิสูจน์แบบเห็นจอจริงบน MSX2
ผู้ใช้ควรทดสอบ ROM เวอร์ชันนี้บนเครื่อง/emulator ของตัวเองแล้วรายงานผลกลับมา

**ยังไม่ได้ทำ (ขอบเขตที่เหลือจริง ๆ)**: (1) การซ้อนหลายเครื่องหมาย (พยัญชนะ+สระ+วรรณยุกต์) ลงช่องเดียวกัน
แบบ 3 ระดับ ยังต้องรอ Phase 2 (`COMPOSITE_GLYPH`) ตามเดิม — นี่คือสิ่งที่ `PRINTON` ควบคุมจริง ๆ (2) ตัว
แปร Kedmanee shift-state (ตัวที่ 3, `0xFCAB`) ที่ต้นฉบับใช้เพื่อพิมพ์ชุดอักษรไทยตัวที่สองบนบางคีย์ยังไม่ได้
ถอด/implement — ตอนนี้ Shift ค้างอยู่จะ fallback เป็นอังกฤษเสมอ (ปลอดภัย ตรงตามพฤติกรรมต้นฉบับสำหรับกรณี
shift-state 2 แต่ยังไม่ครอบคลุมกรณี state 3) (3) ตัวเลข/สระบนแถวตัวเลข (`0x00-0x09`, เช่น ภ ถ ุ ู) ยังไม่
ได้ทำ เพราะ dispatch เดิมกำหนดให้แถวนี้เป็น ASCII digit เสมอโดยยังไม่ได้ตรวจสอบเงื่อนไข THAI_MODE เพิ่มเติม
ในโค้ดต้นฉบับให้ครบ — เป็น fast-follow ที่ทำได้ในแนวทางเดียวกัน (ดึงตาราง data จริงจาก ROM เหมือนที่ทำแล้ว)

### 9.10 บั๊กจริงข้อ 7 (แก้แล้ว) — "ค้างสนิท" บน blueMSX ต่อ แม้หลังแก้บั๊กข้อ 5 และ 6 แล้ว — สาเหตุจริง: slot/page mapping

**อาการ**: หลังส่งไฟล์ build ที่แก้บั๊กข้อ 6 (DI/EI รอบ `KEYC_HOOK` กันปัญหา reentrant interrupt) ไปให้
ผู้ใช้ทดสอบ ผู้ใช้ยืนยันด้วย **blueMSX debugger** (เปิด Pause/Resume ซ้ำหลายรอบเพื่อแยกแยะ "ค้างจริงที่ PC
เดิม" ออกจาก "แค่ pause ระหว่างทำงานปกติ") ว่า PC ยังค้างอยู่ที่ `0x10D9`-`0x10DC` เหมือนก่อนแก้บั๊กข้อ 6
ทุกประการ — **การแก้บั๊กข้อ 6 ไม่มีผลอะไรเลย** พิสูจน์ว่าทฤษฎี reentrant-interrupt ของบั๊กข้อ 6 ไม่ใช่สาเหตุ
ที่แท้จริง (หรืออย่างน้อยก็ไม่ใช่สาเหตุเดียว)

**วิธีหาสาเหตุจริง**: ขอให้ผู้ใช้เปิด memory viewer ของ blueMSX debugger ดูที่อยู่ `0x4169` (ตำแหน่งของ
`KEYC_HOOK` ใน build ตอนนั้น) ระหว่างที่ "ค้าง" อยู่จริง — **ผลลัพธ์ที่ได้คือ `af  xor a`** ไม่ใช่ `f3` (`DI`,
ไบต์แรกจริงของ hook เรา) เลย — หลักฐานตรง ๆ ชัดเจนว่า CPU ไม่ได้กำลังรันโค้ดของเราอยู่ที่ตำแหน่งนั้น
ทั้งที่ `H_KEYC` ($FDCC) ยังชี้มาที่ address นี้ถูกต้อง

**สาเหตุจริง (root cause)**: cartridge ของโปรเจกต์นี้ `org $4000` คือวางอยู่ที่ **หน้า 1** (page 1,
`0x4000`-`0x7FFF`) ของหน่วยความจำ — แต่หน้า 1 **ไม่ได้ถูก map ไปที่ cartridge ตลอดเวลา** ตามสถาปัตยกรรม
slot ของ MSX (ยืนยันจาก machine config จริงที่ใช้ทดสอบโปรเจกต์นี้: primary slot 0 = System ROM ครอบคลุม
`0x0000`-`0x7FFF` ทั้งก้อน [BIOS+BASIC interpreter อยู่ในนี้ **รวมถึงหน้า 1 ด้วย**], primary slot 1 =
cartridge ของเรา [external slot]) — BASIC interpreter เองก็มีโค้ดจริงอยู่ที่หน้า 1 เช่นกัน (คนละ slot กับ
cartridge) และ **ต้องการหน้า 1 ของ slot ตัวเองเพื่อรันโค้ดของตัวเอง** ทำให้ระหว่าง idle ที่ BASIC prompt
(หลังจาก `CALL THAION` return กลับไปแล้ว) หน้า 1 จะถูก BASIC "แย่งกลับไป" เป็น slot ของระบบแทนที่จะเป็น
slot ของ cartridge — `H.KEYC` เป็น hook table แบบ flat (ไม่รู้จัก slot เลย) ที่ BIOS เรียกด้วย `CALL`
ธรรมดา ดังนั้นทุกครั้งที่ VSYNC interrupt เรียก keyboard scan ระหว่าง idle (หน้า 1 = slot ของ BASIC ไม่ใช่
ของเรา) `CALL H_KEYC` จะไปรันโค้ดของ **BASIC เอง** ที่บังเอิญอยู่ตำแหน่งเดียวกับ hook ของเรา ไม่ใช่โค้ดของ
เราเลย — อธิบายได้ทั้งอาการ "ค้างสนิท" (โค้ดของ BASIC ที่ไม่ได้ถูกออกแบบมาให้ถูกกระโดดเข้ามาแบบนี้ ทำให้
BIOS เข้าสู่สถานะที่ไม่สอดคล้องกัน) และเหตุผลที่การแก้บั๊กข้อ 6 (DI/EI) ไม่มีผลอะไรเลย (ปัญหาไม่เกี่ยวกับ
จังหวะ interrupt แต่เกี่ยวกับว่า "โค้ดของเราไม่เคยถูกรันเลยตั้งแต่แรก")

**ยืนยันอิสระอีกชั้นด้วย openMSX เอง** (คนละ emulator, คนละ BIOS ROM แต่สถาปัตยกรรม slot เดียวกัน): เขียน
สคริปต์ peek หน่วยความจำที่ `0x4169` ซ้ำ ๆ ระหว่าง idle หลัง `call thaion` บน `UserMSX1` machine (system
ROM slot 0 + cartridge slot 1 แยกกันจริงตามสถาปัตยกรรม MSX มาตรฐาน) พบไบต์ `AF 32 AA F6` (`xor a` /
`ld ($F6AA),a`) **ตรงกับที่ผู้ใช้เห็นบน blueMSX เป๊ะทุกไบต์ รวมถึง address ปลายทาง `$F6AA` ด้วย** — เป็น
การยืนยันอิสระ (คนละเครื่อง คนละ BIOS ROM) ว่าทฤษฎี slot-aliasing ถูกต้องจริง ไม่ใช่การเดา

**ทางแก้ที่พิจารณาแต่ไม่ใช้**: กลไกมาตรฐานของ MSX BIOS สำหรับเรียกข้ามช่วง slot คือ `CALSLT` (`$001C`)
หรือ `RST 30H`/`CALLF` (`$0030`) — แต่ disassemble การทำงานจริงของ `CALLF` (ทั้งใน `MSX1_bios.rom` และ
`MSX2_bios.rom`) พบว่า core logic ของมันกระโดดเข้าไปทำงานใน **RAM ที่ `0xF38C`** ซึ่งมีแต่ cold boot จริง
เท่านั้นที่จะเติมค่าให้ (sandbox ทดสอบของโปรเจกต์นี้ไม่ได้จำลอง cold boot เต็มรูปแบบ RAM ย่านนี้จึงเป็น
ศูนย์/NOP เสมอ) ทำให้ไม่สามารถยืนยันความถูกต้องของ `CALSLT`/`CALLF` ได้อย่างมั่นใจในสภาพแวดล้อมทดสอบนี้ —
ตัดสินใจไม่ใช้กลไกที่ verify ไม่ได้เต็มที่ แม้จะเป็นวิธี "ทางการ" ก็ตาม

**ทางแก้จริง**: เขียน trampoline เล็ก ๆ (~74 ไบต์, `KEYC_TRAMPOLINE_SRC` ใน ROM) ที่ `KEYC_INSTALL` จะ
copy ไปไว้ใน **RAM หน้า 3** (`KEYC_TRAMPOLINE` = `$FD10`, หน้า 3 map ไปที่ RAM เสมอไม่ว่า slot ไหนจะถูก
เลือกสำหรับหน้า 1 — จึงเข้าถึงได้แน่นอนเสมอ) แล้วตั้ง `H_KEYC` ให้ `JP` ไปที่ trampoline นี้แทนที่จะชี้ไปที่
`KEYC_HOOK_REAL` (ที่อยู่ใน ROM หน้า 1) ตรง ๆ — trampoline ใช้ primitive ของ BIOS ที่ยืนยันแล้วว่าเป็น
ROM-resident ล้วน ๆ ไม่มีการพึ่งพา RAM ที่ต้อง cold-boot เติม (`RSLREG`=`$0138`, `ENASLT`=`$0024`) ทำ 5
ขั้นตอน: (1) หา slot ปัจจุบันของหน้า 1 (`RSLREG`+`EXPTBL`+`SLTTBL`, อัลกอริทึม GETSLT มาตรฐานตามเอกสาร MSX
Assembly Page) (2) สลับหน้า 1 ไปที่ slot ของ cartridge เอง (คำนวณครั้งเดียวตอน `KEYC_INSTALL`, เก็บไว้ที่
`MY_SLOT_ID`) ผ่าน `ENASLT` (3) เรียก `KEYC_HOOK_REAL` (โค้ดเดิมข้างในไม่เปลี่ยน นอกจากเปลี่ยนสัญญาณผลลัพธ์
เป็น carry flag — ดูด้านล่าง) (4) สลับหน้า 1 กลับไปที่ slot เดิมก่อนเสมอ (ไม่ว่าผลจะเป็นอะไร) ผ่าน `ENASLT`
อีกครั้ง (5) แยกสาขาตาม carry: `SCF`=จัดการเองแล้ว (`RET` ตรง ๆ ซึ่งเท่ากับข้ามตาราง dispatch อันตรายของ
BIOS ไปในตัวตามบั๊กข้อ 5 เดิม เพราะ trampoline ถูกเรียกด้วย `CALL` ชั้นเดียวจาก `H_KEYC`) หรือ `OR A`=ปล่อย
ผ่าน (`EI`+`JP PREV_KEYC` ต่อ) — `DI`/`EI` ที่ครอบ trampoline ตอนนี้มีไว้ป้องกัน `OUT ($A8)` ของ `ENASLT`
เองไม่ให้ non-atomic (ไม่ใช่ทฤษฎี reentrant-interrupt ของบั๊กข้อ 6 เดิมอีกต่อไป ซึ่งพิสูจน์แล้วว่าไม่ใช่
สาเหตุจริง) — `KEYC_HOOK_REAL` เองยังคงทำงานเหมือนเดิมทุกประการ (ตาราง `MAIN_ROW_TABLE`/`ROW1_TABLE`,
`QUEUE_PUSH_CHAR` ฯลฯ ไม่แตะเลย) เพราะตอนมันถูกเรียก หน้า 1 ถูกสลับมาเป็น slot ของ cartridge เองแล้วโดย
trampoline เสมอ ปลอดภัยเข้าถึง ROM/ตาราง/routine ของตัวเองได้ตามปกติ

**ความเสี่ยงเชิงสถาปัตยกรรมที่พบระหว่างทาง (บันทึกไว้ ยังไม่แก้ — ดู "ยังไม่ได้ทำ" ด้านล่าง)**: RAM ที่ใช้
เก็บ trampoline/ตัวแปรใหม่ (`MY_SLOT_ID`=`$FD0E`, `SAVED_P1_SLOT`=`$FD0F`, `KEYC_TRAMPOLINE`=`$FD10`-
`$FD5A`) รวมถึง RAM เดิมของโปรเจกต์ (`PRINT_MODE`=`$FD09`, `PLOCK_MODE`=`$FD0A`, `PREV_KEYC`=`$FD0B`-
`$FD0D`) ทั้งหมดอยู่ในย่านที่เอกสาร MSX2 Technical Handbook เรียกว่า **SLTWRK** (`$FD09`-`$FD88`, work
area ที่ระบบสำรองไว้ให้ cartridge ต่าง ๆ ใช้ 2 ไบต์ต่อ slot/page combination ตามสูตร
`SLTWRK_addr = $FD09 + 32×primary_slot + 8×secondary_slot + 2×page`) — ตามสูตรนี้ cell ของ primary slot
0 (ระบบเอง) จะตกอยู่ที่ `$FD09`-`$FD10` พอดี ซึ่งทับซ้อนกับ RAM ที่โปรเจกต์นี้ใช้อยู่บางส่วน — ยังไม่เคยเจอ
ปัญหาจริงจากเรื่องนี้ตลอดการทดสอบทั้งโปรเจกต์ (เป็นไปได้ว่า slot 0 เองไม่ได้ใช้ cell ของตัวเองในทางปฏิบัติ)
แต่เป็นความเสี่ยงที่ควรจับตาไว้ ไม่ได้แก้ในรอบนี้เพื่อจำกัดขอบเขตการเปลี่ยนแปลง

**การยืนยัน**:
1. หน่วยความจำ/logic ระดับ unit test (`test_phase3_keyboard.py`, `test_phase3_typing.py` — ปรับให้เรียก
   `KEYC_HOOK_REAL` ตรง ๆ ด้วยสัญญาใหม่ [single RET + carry flag] แทนการจำลอง double-return-address stack
   แบบเดิม, เพิ่ม `test_trampoline_passthrough_chains_to_prev_keyc` ทดสอบ end-to-end ของ trampoline เองผ่าน
   `H_KEYC` จริงหลัง `KEYC_INSTALL` — ครอบคลุมเคสที่ `test_phase3_e2e_biosreal.py` เดิมไม่เคยทดสอบมาก่อน
   [เคส "ปล่อยผ่าน"]) — รันผ่านครบทั้ง 23 เคสในสี่ไฟล์
2. ระดับ real BIOS entry point (`test_phase3_e2e_biosreal.py`, ไม่ต้องแก้ไขเลยเพราะ interface ของ
   `KEYC_INSTALL`/`H_KEYC` ไม่เปลี่ยน) — ผ่านครบทั้งสาม generation ที่ทดสอบ (main row, row1, toggle)
3. ยืนยันไบต์ที่ `0x4169` ระหว่าง idle บน openMSX ตรงกับที่ผู้ใช้เจอบน blueMSX เป๊ะ (ดูด้านบน) — พิสูจน์ว่า
   กลไก slot-aliasing เกิดขึ้นจริงในทั้งสอง emulator ไม่ใช่ปรากฏการณ์เฉพาะของ blueMSX

**ข้อจำกัดที่พบระหว่างพยายามยืนยันแบบ end-to-end เต็มรูปแบบบน openMSX (บอกตรง ๆ ไม่ปิดบัง)**: พยายามเขียน
สคริปต์ที่จำลองสถานการณ์ครบวงจร (idle ยาว ๆ ให้ BASIC แย่ง slot คืน แล้วกดปุ่มจริงผ่าน matrix scan เพื่อดูว่า
ค้างจริงไหม) แต่พบว่า openMSX's `type` command (จำลองการกดคีย์ผ่าน matrix จริง) มีปัญหาความไวต่อ latency ใน
สถานการณ์นี้อยู่แล้ว (เป็นข้อจำกัดเดิมของโปรเจกต์ ที่เคยพบและบันทึกไว้ตั้งแต่ section 9.8) ทำให้ทดสอบทั้ง
build ที่มีบั๊กและ build ที่แก้แล้วด้วยสคริปต์นี้แล้ว **ไม่มี build ไหนแสดงอาการค้างชัดเจนบน openMSX เอง**
(ต่างจาก timing ของ blueMSX ที่ผู้ใช้ยืนยันว่าค้างจริง — สอดคล้องกับรูปแบบเดิมที่เคยพบตอนบั๊กข้อ 6 เช่นกัน ว่า
openMSX กับ blueMSX จำลอง timing ต่างกันมากพอที่จะทำให้บั๊กบางประเภทแสดงอาการต่างกัน) — สคริปต์
`test_phase3_idle_freeze_bug7.tcl` ยังเก็บไว้ในโปรเจกต์เพื่อยืนยันข้อ 3 ข้างต้น (byte match) และเป็น
regression check เบื้องต้น แต่ **ไม่ใช่การพิสูจน์ freeze/fix แบบเห็นจอจริงเต็มรูปแบบ** — หลักฐานหลักของการ
แก้บั๊กนี้คือ debugger screenshot ของผู้ใช้เอง + การยืนยันไบต์อิสระบน openMSX + unit test ของกลไก trampoline
เอง ผู้ใช้ควรทดสอบ build นี้บน blueMSX จริงแล้วรายงานผลกลับมาเพื่อปิดวงจรการยืนยันให้สมบูรณ์

**ยังไม่ได้ทำ (เพิ่มเติมจากรอบก่อน)**: (4) ความเสี่ยงเรื่อง SLTWRK collision ที่กล่าวถึงข้างต้น — ควรจับตา
ไว้ถ้าในอนาคตมีการเพิ่ม RAM ใหม่ในย่าน `$FD09`-`$FD88` หรือถ้าพบอาการแปลก ๆ ที่เกี่ยวกับตัวแปรใหม่หลัง
`KEYC_INSTALL` บนเครื่องที่มี cartridge อื่นในระบบ slot เดียวกันพร้อมกัน

### 9.11 บั๊กจริงข้อ 8 (แก้แล้ว) — พิมพ์แล้วตัวอักษรเพี้ยน/ไม่ขึ้นจอ, cursor กะพริบทุกครั้งที่กดปุ่ม — สาเหตุจริง: ENASLT เองทับ C (scan code)

**อาการ**: หลังส่ง build ที่แก้บั๊กข้อ 7 (trampoline สลับ slot หน้า 1 ก่อนเรียก `KEYC_HOOK_REAL`) ไปให้
ผู้ใช้ทดสอบบน blueMSX จริง ผู้ใช้ยืนยันว่า **ไม่ค้างแล้ว** (บั๊กข้อ 7 แก้ได้จริง) แต่รายงานอาการใหม่ที่ต่างไป
โดยสิ้นเชิง: "no character show on the screen. press one key the cursor disappear press another key the
cursor appear press another key the cursor disappear and the loop continue" — คือพิมพ์แล้วตัวอักษรไม่ขึ้นจอ
(หรือขึ้นเป็นตัวที่ผิด) และ cursor กะพริบหาย/โผล่สลับกันทุกครั้งที่กดปุ่ม

**วิธีหาสาเหตุจริง**: harness ทดสอบแบบ flat-memory เดิม (Python, ใช้ทดสอบบั๊กข้อ 1-7 มาตลอด) **ไม่มี model
ของฮาร์ดแวร์ slot switching จริงเลย** (`OUT ($A8)` เป็น no-op, cartridge ROM มองเห็นได้ตลอดเวลาไม่ว่า slot
state จะเป็นอะไร) — แปลว่า unit test ที่ผ่านทั้งหมดไม่เคยพิสูจน์อะไรเกี่ยวกับความถูกต้องของกลไก
ENASLT-based slot switch/restore ในบั๊กข้อ 7 เลยแม้แต่น้อย จึงต้องกลับไปใช้ openMSX (จำลอง slot switching
จริง) อีกครั้ง — คราวนี้พบว่า machine config เดิมที่เคยใช้ (`UserMSX1`, primary slot 0 = System ROM
**ไม่ expand**) ไม่เคยจำลองสถานการณ์ที่ตรงกับเครื่องจริงของผู้ใช้เลย เพราะ MSX1/MSX2 จริงจำนวนมาก (รวมถึง
เครื่องที่ผู้ใช้ใช้ทดสอบบน blueMSX) มี primary slot 0 แบบ **expanded (subslotted)** จึงสร้าง machine config
ใหม่ (`UserMSX1_expanded`, primary slot 0 expand จริงผ่าน `<secondary slot="0">`) — ผลคือ **จำลองอาการที่
ผู้ใช้รายงานได้ตรงเป๊ะ**: พิมพ์ `Hello` หลัง `CALL THAION` แล้วขึ้นจอเป็น `!\WWW` แทน (ยืนยันด้วย keyboard
buffer ตรง ๆ: ได้ไบต์ `33 190 190 190 190` แทนที่จะเป็น `72 101 108 108 111` ของ "Hello") ขณะที่ config เดิม
(ไม่ expand) ไม่เคยจำลองอาการนี้ได้เลย — ชี้ชัดว่าสาเหตุอยู่ในเส้นทางการจัดการ expanded slot ของ trampoline
บั๊กข้อ 7

ตั้ง breakpoint ที่จุดต่าง ๆ ใน `KEYC_HOOK_REAL` เอง (`0x4175`/`0x4178`/`0x41b3`/`0x41a2`) เพื่อดูว่า logic
การจำแนกปุ่ม (Thai mode / input mode / shift state / เลือกตาราง) ยังถูกต้องอยู่ไหม — **พบว่าถูกต้องทุก
ประการ**: `THAI_MODE`/`INPUT_MODE` อ่านค่าถูก, scan code ใน `C` ถูกต้องตรงกับปุ่มที่กด (29, 26, 33 ตาม H/e/l),
เข้า `.passthrough` ถูกทุกครั้ง (breakpoint ของ `.lookup` ไม่เคยทำงานเลย แปลว่าไม่ได้ไปโดนตาราง mapping ภาษา
ไทยผิดตัว) — ปัญหาจึงไม่ได้อยู่ใน `KEYC_HOOK_REAL` เอง แต่ต้องอยู่ใน**โค้ดของ trampoline เองหลังจากเรียก
`KEYC_HOOK_REAL` กลับมาแล้ว** (ส่วนที่สลับ slot หน้า 1 กลับที่เดิม แล้วแยกสาขาไป `PREV_KEYC`)

**สาเหตุจริง (root cause)**: disassemble `ENASLT` จริง (`$0024`) พบว่ามีสองเส้นทางแยกกันตามชนิดของ slot
ปลายทาง — เส้นทาง **slot ธรรมดา** (`IN A,($A8); AND C; OR B; OUT ($A8),A; RET`) ใช้ `B`/`C` เป็นแค่ mask
อ่านอย่างเดียว **ไม่ได้เขียนทับ `C`** แต่เส้นทาง **slot expanded** (`PUSH HL; CALL ...; LD C,A; LD B,0; ...`)
**เขียนทับ `C` ตรง ๆ** (ใช้เป็นตัวคำนวณ index ของ `SLTTBL` ภายใน) — โค้ด `KEYC_TRAMPOLINE_SRC` ของบั๊กข้อ 7
เรียก `ENASLT` สองครั้ง: ครั้งแรก (ก่อนเรียก `KEYC_HOOK_REAL`) มี `push hl/push bc/push de` ครอบไว้ก่อน แล้ว
`pop de/pop bc/pop hl` คืนถูกต้องก่อนเรียก `KEYC_HOOK_REAL` จริง (`C`=scan code จึงเข้าไปถึง
`KEYC_HOOK_REAL` ถูกต้อง ตรงกับที่ breakpoint ยืนยัน) — **แต่ครั้งที่สอง (หลัง `KEYC_HOOK_REAL` คืนค่ามา, ตอน
สลับ slot หน้า 1 กลับที่เดิม) มีแค่ `push af`/`push hl` ครอบไว้ ไม่มี `push bc`/`push de` เลย** — เมื่อ slot
เดิมของหน้า 1 (ระบบ) เป็น slot ที่ expand (ตรงกับเครื่องจริงของผู้ใช้) `ENASLT` ครั้งที่สองนี้จะเขียนทับ `C`
ด้วย secondary-slot index ที่คำนวณภายในตัวมันเอง ก่อนจะกระโดดต่อไปที่ `PREV_KEYC` (เส้นทาง passthrough) ซึ่ง
เป็นโค้ด BIOS เดิมที่ต้องพึ่ง `C` = scan code จริงเพื่อแปลงเป็นตัวอักษรแล้วดันเข้าคิวคีย์บอร์ด — ผลคือ
`PREV_KEYC` ได้ `C` ที่เป็นขยะ (ค่า secondary-slot index ของ `ENASLT`) แทนที่จะเป็น scan code จริง ทำให้
ตัวอักษรที่ขึ้นจอเพี้ยนไปหมด (บางครั้งเป็นค่าเดิมซ้ำ ๆ เพราะ `ENASLT` คำนวณ index เดิมทุกครั้งที่ slot ปลายทาง
เดิม) — อาการ cursor กะพริบหาย/โผล่ตามที่ผู้ใช้รายงานคืออาการข้างเคียงของ BASIC ตีความ scan code ขยะพวกนี้
เป็น control code บางตัวที่ไปกระทบการวาด cursor ไม่ใช่บั๊กแยกต่างหาก — และบั๊กนี้ **ไม่เกิดกับ slot 0 แบบไม่
expand** เพราะเส้นทางง่ายของ `ENASLT` ไม่แตะ `C` เลยอย่างที่กล่าวไว้ข้างต้น (อธิบายได้พอดีว่าทำไมเครื่องจริง
ของผู้ใช้ถึงเจอ แต่ config ทดสอบเดิมของโปรเจกต์ที่ไม่ expand ไม่เคยเจอ)

**ทางแก้**: เพิ่ม `push bc`/`push de` ก่อนเรียก `ENASLT` ครั้งที่สอง และ `pop de`/`pop bc` คืนหลังเรียกเสร็จ
(สมมาตรกับที่ครอบ `ENASLT` ครั้งแรกไว้อยู่แล้ว) ใน `KEYC_TRAMPOLINE_SRC` (`src/keyboard.asm`) — เปลี่ยนแค่
4 ไบต์ (opcode `push bc`/`push de`/`pop de`/`pop bc` อย่างละ 1 ไบต์) ไม่กระทบ logic อื่นเลย ตำแหน่ง
push/pop สมดุลกันทั้งสองสาขา (`.handled` และ passthrough) เพราะอยู่ก่อนจุดแยกสาขา (`jr c,.handled`) — RAM
ที่ `KEYC_TRAMPOLINE` ใช้ (`$FD10`-) ยังเหลือพื้นที่ว่างก่อนถึง `PROCNM` (`$FD89`) มากพอ (~47 ไบต์ก่อนแก้,
เหลือ ~43 ไบต์หลังแก้) ไม่ชนกับ RAM cell อื่นของโปรเจกต์

**การยืนยัน**:
1. ชุด unit test เดิมทั้ง 23 เคส (`test_phase1.py`, `test_phase3_keyboard.py`, `test_phase3_typing.py`,
   `test_phase3_e2e_biosreal.py`) ผ่านครบเหมือนเดิม (คาดไว้แล้วว่าจะไม่จับบั๊กนี้ได้ เพราะ harness แบบ
   flat-memory ไม่จำลอง slot switching จริงตามที่อธิบายไว้ข้างต้น — ใช้ยืนยันแค่ว่าการแก้ไม่ได้ทำให้ logic
   เดิมพัง)
2. ทดสอบแบบ A/B บน openMSX จริงด้วย `UserMSX1_expanded` machine (slot 0 expand): build **ก่อนแก้**
   (ยังไม่มี `push bc`/`push de` ครั้งที่สอง) พิมพ์ `Hello` หลัง `CALL THAION` ได้ keyboard buffer
   `33 190 190 190 190` (ตรงกับอาการเดิมที่ผู้ใช้รายงานเป๊ะ) — build **หลังแก้** พิมพ์ `Hello` เดียวกัน ได้
   keyboard buffer `72 101 108 108 111` ตรงกับ "Hello" **แบบไบต์ต่อไบต์ทุกตัวอักษร** และหน้าจอ (screenshot)
   แสดง `Hello` ถูกต้องจริง (ไม่ใช่แค่ใน memory) — ทดสอบซ้ำด้วยประโยคยาวกว่าและซับซ้อนกว่า
   (`"The Quick Fox 123!"`, มีทั้งตัวพิมพ์ใหญ่/เล็ก/เลข/สัญลักษณ์ผสมกัน) ก็ได้ผลตรงทุกไบต์เช่นกัน ยืนยันว่า
   การแก้ครอบคลุมทั้ง shift state และช่วง scan code ต่าง ๆ ไม่ใช่แค่กรณีเดียว
3. ทดสอบซ้ำบน `UserMSX1` (slot 0 ไม่ expand) ด้วย build ก่อนแก้และหลังแก้ — **ไม่พบความแตกต่างกัน** (ตามคาด
   เพราะเส้นทางที่แก้เป็นเส้นทาง expanded-slot โดยเฉพาะ ไม่ได้แตะเส้นทางง่าย) แต่ระหว่างการทดสอบนี้พบว่า
   config ไม่ expand มีปัญหาที่ `type` command ของ openMSX เองไม่ยอมพิมพ์อะไรเลยหลัง `CALL THAION` (ไม่ใช่
   แค่ latency แบบที่เคยพบในบั๊กข้อ 7 แต่ไม่มีอะไรขึ้นแม้รอนานถึง 6 วินาที) — ยืนยันแล้วว่า **เป็นพฤติกรรมเดิม
   ของ build ก่อนแก้ด้วยเช่นกัน** (ทดสอบ build เดียวกันทั้งสองแบบได้ผลเหมือนกัน) จึงเป็นข้อจำกัด/ความแปลกของ
   `type` command กับ machine config นี้โดยเฉพาะ ไม่เกี่ยวกับการแก้บั๊กครั้งนี้ และไม่ใช่ผลข้างเคียงของโค้ด
   cartridge (บันทึกไว้ตรง ๆ เผื่อใครมาแก้ต่อในอนาคตจะได้ไม่งง)

**ข้อสังเกตเพิ่มเติม**: บั๊กข้อ 8 นี้เป็นตัวอย่างที่ชัดเจนว่าทำไมการมี real-hardware test (หรืออย่างน้อย
emulator ที่จำลอง slot switching ตรงตาม config จริงของเครื่องผู้ใช้) สำคัญกว่าการผ่าน unit test ล้วน ๆ —
โค้ดของบั๊กข้อ 7 "ดูถูกต้อง" และผ่านทุก unit test แต่พังจริงเฉพาะเมื่อ slot ปลายทางเป็นแบบ expanded ซึ่งเป็น
รายละเอียดของ **ฮาร์ดแวร์ปลายทาง** ไม่ใช่ของโค้ดเราเอง — ควรพิจารณาเพิ่ม `UserMSX1_expanded` (หรือเทียบเท่า)
เป็น machine config มาตรฐานสำหรับทดสอบ ใน pipeline ถัดไป แทนที่จะใช้แค่ config ไม่ expand เป็นค่าเริ่มต้น

### 9.12 ปิดใช้งาน H.KEYC hook ชั่วคราว (ตามคำขอผู้ใช้ หลังบั๊กข้อ 8 แก้แล้วแต่ยังพิมพ์ไม่ได้บนเครื่องจริง)

**สถานการณ์**: หลังส่ง build ที่แก้บั๊กข้อ 8 (ENASLT ทับ C) ไปให้ผู้ใช้ทดสอบ ผู้ใช้แจ้งว่า **ยังพิมพ์ไม่ได้
อยู่ดี** บนเครื่องจริง และขอให้กลับไปเป็น "version ที่ยังไม่มีการสลับภาษา" — คือยังไม่ทราบสาเหตุที่แท้จริงของ
รอบนี้ (อาจเป็นบั๊กใหม่อีกตัวในกลไก trampoline/ENASLT เอง, หรือ BIOS ของเครื่องจริงต่างจาก `MSX1_bios.rom`
ที่ใช้ทดสอบมาตลอด ในจุดที่ยังไม่เคยตรวจสอบ เช่น ตำแหน่งจริงของ `H.KEYC`/keyboard buffer บนเครื่องนั้น) —
ยังไม่ได้ไล่หาต่อในรอบนี้ ตามคำขอของผู้ใช้ให้ priority กับการมีเวอร์ชันที่ใช้พิมพ์ได้จริงก่อน

**สิ่งที่ทำ**: comment บรรทัด `call KEYC_INSTALL` ใน `CMD_THAION` และ `call KEYC_UNINSTALL` ใน
`CMD_THAIOFF` (`src/thaicmd.asm`) ออก — **ไม่ได้ลบโค้ดของกลไก trampoline ทิ้งเลย** (`KEYC_INSTALL`,
`KEYC_UNINSTALL`, `KEYC_HOOK_REAL`, `KEYC_TRAMPOLINE_SRC` ทั้งหมดยังอยู่ครบใน `src/keyboard.asm` พร้อม
กลับมาเปิดใช้ต่อได้ทันทีด้วยการเอา `;` หน้าสองบรรทัดนั้นออก) แค่ทำให้ `CALL THAION` ไม่แตะ `H.KEYC` เลย
อีกต่อไป — ผลคือ:
- `THAION` ยังคงโหลดฟอนต์ไทยเข้า VRAM ตามปกติ (ส่วนนี้ไม่เคยมีปัญหา ไม่เกี่ยวกับบั๊กคีย์บอร์ดเลย) และยังตั้ง
  `THAI_MODE` flag ตามเดิม
- ปุ่มคีย์บอร์ดพิมพ์เป็นภาษาอังกฤษปกติ 100% เหมือน BASIC เปล่า ๆ ไม่มีการสลับภาษา/แปลง scan code ใด ๆ
  เลย (เพราะ `H.KEYC` ไม่เคยถูก patch) — แก้ปัญหาพิมพ์ไม่ได้/ตัวอักษรเพี้ยน/ค้างทั้งหมดโดยตรง เพราะสาเหตุ
  ทั้งหมด (บั๊กข้อ 5, 6, 7, 8) อยู่ในกลไกนี้ล้วน ๆ
- `THAIOFF` ยังคง reset flag ทั้งหมดและเรียก `INITXT` ตามเดิม (ส่วนนี้ไม่เกี่ยวกับ hook เลย)

**การยืนยัน**: rebuild แล้วรัน unit test ทั้ง 4 ไฟล์ (21 เคส) ผ่านครบเหมือนเดิม (เพราะเทสของ trampoline/hook
เรียก `KEYC_INSTALL` ตรง ๆ ไม่ผ่าน `CMD_THAION` จึงไม่ได้รับผลกระทบ) — ทดสอบเพิ่มบน openMSX ด้วย
`UserMSX1_expanded` (config เดียวกับที่เคยจำลองบั๊กข้อ 8 ได้): `debug read_block` ที่ `H_KEYC` ($FDCC) หลัง
`CALL THAION` ได้ `201 201 201` (`RET RET RET`, ค่า default ของ BIOS จริง ไม่ถูกแก้เลย) แล้วพิมพ์ประโยคยาว
`"Hello World 123"` ได้ผลลัพธ์ถูกต้อง 100% ทั้งใน keyboard buffer และบนจอ (screenshot) ไม่มีอาการค้าง/
เพี้ยน/cursor กะพริบผิดปกติใด ๆ เลย

**ยังไม่ได้ทำ**: หาสาเหตุที่แท้จริงว่าทำไมพิมพ์ยังไม่ได้บนเครื่องจริงของผู้ใช้แม้หลังแก้บั๊กข้อ 8 แล้ว (ฟีเจอร์
สลับภาษาไทย/อังกฤษถูกปิดไว้ชั่วคราวเท่านั้น ไม่ใช่การยกเลิกถาวร) — ถ้าจะกลับมาทำต่อในอนาคต ควรเริ่มจากถาม
ผู้ใช้ว่า blueMSX debugger เห็นอะไรตรง `H_KEYC` ($FDCC)/`KEYC_TRAMPOLINE` ($FD10) ตอนกดปุ่มจริงบนเครื่องที่
ยังไม่ได้ปิด hook (หรือ build ก่อนหน้านี้ที่ยังเปิด hook อยู่) เพื่อดูว่าเข้าไปถึง trampoline จริงไหม ก่อนจะ
สงสัยจุดอื่น

### 9.13 การออกแบบใหม่ (RST 30H / CALLF แทน trampoline เดิม) + บั๊กจริงข้อ 9 (SHIFT_STATE กลับขั้ว)
และบั๊กจริงข้อ 10 (BIOS's post-hook dispatch ดันตัวอักษรซ้ำ) — เปิดใช้งานฟีเจอร์สลับภาษาอีกครั้ง

**สถานการณ์ตั้งต้น**: หลังปิดใช้งาน H.KEYC hook ชั่วคราวไว้ (section 9.12) ผู้ใช้ส่งไฟล์ ROM ที่ port ต้นฉบับ
(`MSXTHA102`) ไปยัง MSX2 สำเร็จแล้วจริง (`MSXTHA102_MSX2_FINAL_16k.rom`, ผ่านการ direct-binary-patch ถึง
v62) พร้อมเอกสารบันทึกกระบวนการ (`MSXTHA102_MSX2_porting_notes.md`) มาให้ พร้อมคำถามว่าเทคนิคจากไฟล์นั้น
เอามาใช้กับโปรเจกต์นี้ได้ไหม — เอกสารนั้นยืนยันว่า RST 30H (CALLF, official BIOS inter-slot call primitive
ตาม MSX2 Technical Handbook Chapter 5B) เป็นกลไกที่ใช้ได้จริงบนฮาร์ดแวร์จริง (ผ่านการทดสอบมาถึง v62) แทนที่
จะเขียน trampoline เอง (ที่เป็นต้นเหตุของบั๊กข้อ 7/8) จึงตัดสินใจนำมาปรับใช้แทนกลไกเดิมทั้งหมด

**ทำไมตอนแรก (บั๊กข้อ 7) ถึงตัดสินใจไม่ใช้ RST 30H**: ตอนนั้น disassemble พบว่า CALLF's เส้นทาง "slot
ธรรมดา" กระโดดไปรันโค้ดที่ค้างอยู่ใน RAM (`$F38C`) ซึ่งต้องถูก BIOS เติมไว้ตอน **cold boot จริงเท่านั้น**
harness ทดสอบตอนนั้น (`emutest.py`) ไม่จำลอง cold boot เต็มรูปแบบ ทำให้ไม่สามารถยืนยันพฤติกรรมของ CALLF ได้
มั่นใจในสภาพแวดล้อมทดสอบ จึงเลือกเขียน trampoline เองแทน (ใช้แค่ RSLREG+ENASLT ที่ยืนยันแล้วว่าอยู่ใน ROM
ล้วน ๆ) — เอกสารอ้างอิงจากผู้ใช้เป็นหลักฐานที่ยืนยันว่า `$F38C` ทำงานถูกต้องจริงบนฮาร์ดแวร์จริงที่ผ่าน cold
boot ตามปกติ (ซึ่งเป็นกรณีเดียวที่เกิดขึ้นจริงเสมอนอก unit test) จึงตัดสินใจกลับมาใช้ RST 30H ในรอบนี้

**ทำไมเปลี่ยนใจกลับมาใช้ RST 30H รอบนี้**: trampoline เอง (บั๊กข้อ 7/8) พิสูจน์แล้วว่าเปราะบางกว่าที่คิด —
ต้องเขียน/ดูแล logic การสลับ slot ที่ถูกต้อง 100% เอง (บั๊กข้อ 8 คือตัวอย่างจริงที่ลืม push/pop BC/DE รอบสอง)
และแม้แก้บั๊กข้อ 8 แล้วผู้ใช้ก็ยังรายงานว่าพิมพ์ไม่ได้อยู่ดี (ไม่ทราบสาเหตุแน่ชัด อาจเป็นบั๊กใหม่อีกตัวในกลไก
เดียวกัน) — ขณะที่ RST 30H/CALLF เป็นกลไก **official ของ BIOS เอง** ที่ผ่านการทดสอบจริงบนฮาร์ดแวร์จริงมาแล้ว
(ยืนยันจากเอกสารอ้างอิงของผู้ใช้) และจาก disassembly จริงของ `CALLF` (`$0205`-`$025D` ใน `MSX1_bios.rom`)
พบว่าใช้ **shadow register set** (`EXX`/`EX AF,AF'`) ระหว่างคำนวณ slot ภายใน ทำให้ primary BC/DE/HL ของผู้
เรียกไม่ถูกแตะเลย — ป้องกันบั๊กแบบเดียวกับข้อ 8 (ลืม push/pop BC รอบ ENASLT) ได้โดยธรรมชาติ เพราะไม่ต้องเขียน
โค้ดสลับ slot เองอีกต่อไปเลย ยกภาระทั้งหมดไปให้ BIOS's เอง

**สิ่งที่เปลี่ยน**: `H_KEYC` ($FDCC) ตอนนี้เป็น hook slot มาตรฐานของ MSX 5 ไบต์: `[$F7=RST 30H][slot ID ของ
cartridge นี้][addr_lo][addr_hi ของ KEYC_HOOK_REAL][$C9=RET]` — ชี้ตรงไปที่ `KEYC_HOOK_REAL` (ROM) เลย ไม่มี
`KEYC_TRAMPOLINE` (RAM) อีกต่อไป (`KEYC_TRAMPOLINE_SRC`/`KEYC_TRAMPOLINE` เก็บไว้เป็น dead code ใน
`src/keyboard.asm` เผื่อย้อนกลับ/อ้างอิง ไม่ถูกเรียกจริงอีกต่อไป) `KEYC_INSTALL`/`KEYC_UNINSTALL` เขียนใหม่
ให้สำรอง/คืนค่า 5 ไบต์เต็ม (จากเดิม 3 ไบต์ — hook slot จริงยาว 5 ไบต์เสมอ ยืนยันจาก openMSX ว่า default
slot ที่ยังไม่ติดตั้งคือ `C9 C9 C9 C9 C9` ไม่ใช่แค่ 3 ไบต์ตามที่เข้าใจไว้ตั้งแต่บั๊กข้อ 7)

**การสลับกลับมาใช้ RST 30H เปิดเผยบั๊กใหม่ 2 ตัวที่ซ่อนอยู่มาตลอด (ไม่เกี่ยวกับ RST 30H โดยตรง):**

**บั๊กจริงข้อ 9 (แก้แล้ว) — SHIFT_STATE กลับขั้ว ทำให้โหมดพิมพ์ไทยไม่เคย engage เลย**: ระหว่างตรวจสอบซ้ำ
ทั้งระบบก่อนเปิดใช้งานอีกครั้ง พบว่า `SHIFT_STATE` ($FBEB, = NEWKEY row 6) เป็นค่า **active-low** ตามมาตรฐาน
NEWKEY จริง (bit=0 คือกดอยู่, bit=1 คือปล่อย) ยืนยันจาก `debug read_block memory 0xFBE5 11` บน openMSX ตอน
idle จริง = `255` (all bits set) ทุก row — แต่โค้ดเดิม (`AND 3; CP 2; JR NC,.passthrough`) เข้าใจขั้วผิด คิด
ว่า idle=0/1 (active-high) จึงตีความ idle จริง (=3) เป็น "Shift ถูกกดอยู่เสมอ" ทำให้ fallback ไปอังกฤษทุก
ครั้ง โหมดพิมพ์ไทยไม่เคย engage เลยแม้ตั้งค่าทุกอย่างถูกต้อง — แก้เป็น `AND 3; CP 3; JR NZ,.passthrough`
(idle จริง=3 เท่านั้นที่ไปต่อ Thai lookup) บั๊กนี้ไม่เกี่ยวกับ RST 30H เลย เป็นบั๊กเดิมที่ซ่อนอยู่ตั้งแต่ก่อน
บั๊กข้อ 7 แต่ไม่เคยถูกจับได้ เพราะไม่มี test ไหนเคยตั้งค่า `SHIFT_STATE` ให้ตรงกับ idle จริงของฮาร์ดแวร์มา
ก่อนเลย (unit test เดิมใช้ `shift=0` เป็นค่า default ซึ่งบังเอิญ "ผ่าน" เพราะไม่เคยเทียบกับขั้วจริง)

**บั๊กจริงข้อ 10 (แก้แล้ว) — BIOS's post-hook dispatch table ดันตัวอักษรซ้ำ**: หลังเปลี่ยนมาใช้ RST 30H พบ
ว่า BIOS's caller จริง (`$1025 CALL $FDCC`, ดู `$1028` ใน `MSX1_bios.rom`) จะเดินตาราง dispatch ของมันเอง
ต่อ**เสมอ**ไม่ว่า carry จะเป็นอะไร (trampoline เดิมข้ามมันไปได้ด้วยการเขี่ย return address ออกจาก stack ตรง
ๆ — `POP AF` ทิ้งค่า `$1028` แล้ว `RET` อีกที, ดูบั๊กข้อ 5 — แต่ CALLF ไม่มีกลไกแบบนั้นให้ใช้) ตารางนั้นเช็ค
ค่า `A` ที่ hook กลับมาว่าอยู่ช่วงไหน (`CP` กับ bound หลายค่าที่ `$1B96`) เพื่อตัดสินใจว่าจะดันตัวอักษร
default ของมันเองซ้ำหรือไม่ — โค้ดเดิมปล่อยให้ `A` เป็นค่าเหลือจาก `QUEUE_PUSH_CHAR` (ใช้ `A` เป็น scratch
ภายใน) หรือจาก PSG `OUT` ของ `.toggle` ซึ่งเป็นค่าไม่คาดคิด ทำให้ไปโดน entry ที่ผิด ดันตัวอักษรซ้ำจริง
(ยืนยันด้วย debugger บน openMSX: พิมพ์ "a" ได้ `0xBF` ของเราถูกต้อง แต่ตามด้วย `0xC4` อีกตัวจาก BIOS เอง) —
แก้โดยเดินตามตาราง dispatch ของ `MSX1_bios.rom` จริงด้วยมือ พบว่า entry ที่ bound=`$33` (`E=$10` → `$0f10`
→ `CALL $FDD1` ซึ่งเป็น `RET` เปล่า ๆ ใน BIOS นี้ ไม่แตะ `A` เลย → อ่าน `($1003+A)`) — ยืนยันแล้วว่าไบต์ที่
`$1033`/`$1034`/`$1035` (`=$1003+$30/$31/$32`) เป็น `0` ทั้งหมด (BIOS ทิ้งคีย์เอง ไม่ดันอะไรซ้ำ) จึงเพิ่ม
`SAFE_DISPATCH_A equ $30` แล้ว `ld a,SAFE_DISPATCH_A` ก่อน `scf`/`ret` ทุกครั้งที่ hook "จัดการคีย์เองแล้ว"
(ทั้งใน `.lookup` สำเร็จ และ `.toggle`) — ตรงกับเทคนิคเดียวกับที่เอกสารอ้างอิงของผู้ใช้ใช้กับ BIOS คนละตัว
(คืน `A=$30` = รหัสปุ่ม Shift เอง ซึ่งตารางแปล default ของ BIOS นั้นไม่มี action เช่นกัน) แม้ที่มาของค่าจะ
ต่างกันเล็กน้อย (ของเราคือ "entry ที่ `CALL $FDD1` เป็น no-op พอดี") แต่ผลลัพธ์เดียวกัน ยืนยันด้วย byte ตรง
ๆ ว่าปลอดภัยจริงกับ `MSX1_bios.rom` (**`MSX2_bios.rom` ยังไม่ได้ตรวจตารางเทียบเป๊ะ ๆ — ควรตรวจซ้ำถ้าพบ
ปัญหาบน MSX2 จริง**)

**เปิดใช้งานฟีเจอร์สลับภาษาอีกครั้ง**: เอา `;` หน้า `call KEYC_INSTALL` (ใน `CMD_THAION`) และ
`call KEYC_UNINSTALL` (ใน `CMD_THAIOFF`, `src/thaicmd.asm`) ที่ comment ไว้ตั้งแต่ section 9.12 ออก

**การยืนยัน**:
1. ชุด unit test ทั้ง 4 ไฟล์ (`test_phase1.py`, `test_phase3_keyboard.py`, `test_phase3_typing.py`,
   `test_phase3_e2e_biosreal.py`) ปรับปรุงให้ตรงกับ contract ใหม่ (hook slot 5 ไบต์, ไม่มี `KEYC_TRAMPOLINE`,
   assert `A==SAFE_DISPATCH_A` ทุกเส้นทาง "จัดการเอง", `SHIFT_STATE` default เปลี่ยนเป็น 3) ผ่านครบทุกเคส —
   ลบเทสที่เคยยืนยัน "chain ไป PREV_KEYC ผ่าน H_KEYC ตรง ๆ" ทิ้งไปด้วย เพราะการออกแบบใหม่ไม่ chain ไป
   PREV_KEYC จากโค้ดเราเองอีกต่อไป (BIOS's own dispatch ทำหน้าที่แทน "ปล่อยผ่าน" โดยธรรมชาติ) และ harness
   แบบ flat-memory เดิม (`emutest.py`) ไม่จำลอง slot switching จริง ทำให้การเรียกผ่าน `H_KEYC` ตรง ๆ (กระตุ้น
   RST 30H เต็มรูปแบบ) ให้ผลที่ไม่แน่นอน/ไม่ตรงกับพฤติกรรมจริง (ยืนยันด้วยการ trace จริงระหว่างแก้)
2. `test_phase3_e2e_biosreal.py` (เรียกผ่านจุดเข้าจริงของ BIOS ที่ `$1021` ตรง ๆ หลังติดตั้ง hook ด้วย
   `KEYC_INSTALL` เหมือนการใช้งานจริงทุกประการ) ต้อง seed ค่าไบต์จริงของ cold-boot trampoline ที่ `$F38C`
   (dump จาก openMSX จริง) เข้า RAM ก่อน เพราะ harness นี้กระโดดตรงเข้า BIOS entry โดยไม่ผ่าน cold boot จริง
   เลย — หลัง seed แล้วผ่านครบทุกเคส ทั้งสอง BIOS ROM (`MSX1_bios.rom`/`MSX2_bios.rom`) ยืนยันว่า
   `SAFE_DISPATCH_A` แก้ปัญหา "queue tail advanced by more than 1 char" (อาการของบั๊กข้อ 10) ได้จริง
3. openMSX regression เดิม (`test_full_regression.tcl`, `UserMSX1_expanded`): อัปเดตค่า opcode คาดหวังของ
   `H_KEYC` หลัง `CALL THAION` เป็น `247` (`$F7`=RST 30H แทน `195`=`$C3` JP เดิม) — รันผ่าน: font โหลดถูก,
   hook ติดตั้ง/ถอดถูกไบต์ต่อไบต์ (`247`→`201` หลัง `THAIOFF`), `PRINTON`/`PRINTOFF`/`PLOCKON` ทำงานปกติ
4. `test_phase3_expanded_slot_bug8.tcl` (`UserMSX1_expanded`, primary slot 0 expand จริง — สภาพแวดล้อม
   เดียวกับที่เคยจับบั๊กข้อ 8 ได้): พิมพ์ `Hello` หลัง `CALL THAION` ผ่าน matrix scan จริง ได้ keyboard
   buffer `72 101 108 108 111` ตรงกับ "Hello" เป๊ะ — ไม่มีการ regress กลับไปเป็นบั๊กข้อ 8
5. เพิ่ม openMSX regression ใหม่ `test_phase3_thai_typing_bug9_bug10.tcl` (คุมทั้งบั๊กข้อ 9 และ 10 พร้อมกัน
   — ยืนยันว่าไม่ต้องพึ่งการตั้งค่าพิเศษใด ๆ ในการทดสอบ): `CALL THAION` → `CALL INPUTON` (กดปุ่มสลับโหมด
   จริง) → พิมพ์ `asd` ผ่าน matrix scan จริง (`type`) ได้ font code ไทยตรงตาม `MAIN_ROW_TABLE` เป๊ะ
   (`191 203 161` = ฟ,ส,ด) **ไม่มีตัวอักษรซ้ำ/ขยะแทรกเลย** — ยืนยันทั้งสองบั๊กพร้อมกันด้วยการพิมพ์จริงผ่าน
   คีย์บอร์ดจำลองแบบ end-to-end เต็มรูปแบบ (matrix scan → interrupt → H.KEYC → RST 30H → KEYC_HOOK_REAL →
   BIOS's dispatch table → keyboard queue) ไม่ใช่แค่เรียก routine ตรง ๆ

**ข้อสังเกตเพิ่มเติม**: การกลับมาใช้กลไก official ของ BIOS เอง (RST 30H) แทนที่จะพยายามเขียน/ดูแล slot-
switching mechanism เองอย่างสมบูรณ์แบบ (ซึ่งพิสูจน์แล้วสองรอบติดว่าพลาดได้ง่าย — บั๊กข้อ 7 ลืมจัดการ
`$F38C` dependency, บั๊กข้อ 8 ลืม push/pop BC/DE รอบสอง) เป็นตัวอย่างที่ดีของหลักการ "reuse สิ่งที่ยืนยันได้
ว่าถูกต้องแล้วจากแหล่งที่เชื่อถือได้ (official BIOS mechanism + เอกสาร port จริงบนฮาร์ดแวร์จริง) แทนที่จะ
พยายาม re-implement เอง" — แลกมาด้วยการต้องเจาะลึก **ผลข้างเคียง** ของกลไกนั้น (บั๊กข้อ 10: BIOS's
post-hook dispatch ที่ RST 30H ไม่สามารถข้ามได้เหมือน trampoline เดิม) ให้ครบถ้วนแทน ซึ่งยืนยันได้ง่ายกว่า
มาก (แค่ตรวจ byte คงที่ในตาราง dispatch ของ BIOS) กว่าการต้องเขียน/ดูแล slot-switching logic ที่ถูกต้อง
สมบูรณ์เองไปตลอด

### 9.14 บั๊กจริงข้อ 11 (แก้แล้ว) — แถวตัวเลขไม่เป็นไทย + กด Shift ไม่ได้อักษรไทยตัวบน,
และบั๊กจริงข้อ 12 (แก้แล้ว) — RAM ของ `INPUT_MODE`/`THAI_MODE` ชนกับตัวแปรระบบจริงของ BIOS/BASIC
("กด BS/Shift+ตัวอักษร/Enter กลับเป็นอังกฤษ")

**สถานการณ์ตั้งต้น**: หลัง section 9.13 ผู้ใช้ทดสอบบนฮาร์ดแวร์จริง (blueMSX) แล้วรายงานว่าพิมพ์ไทยได้แล้ว
แต่มี 3 อาการเพิ่มเติม: (1) แถวตัวเลข (`1`-`0`) ไม่ขึ้นเป็นไทย (2) กด Shift ค้างไม่ได้อักษรไทยชุดที่สอง
(อักษรตัวบน) และ (3) กด BS แล้วกลับไปพิมพ์เป็นอังกฤษ — ต่อมาผู้ใช้รายงานเพิ่มเติมว่าอาการ (3) ไม่ได้เกิด
เฉพาะ BS เท่านั้น แต่เกิดหลังกด Shift+ตัวอักษร หรือกด Enter ด้วยเช่นกัน ("ไม่เฉพาะ BS ที่กลับเป็นภาษา
อังกฤษ เวลา shift กับตัวอักษร หลังจากนั้นก็เป็นภาษาอังกฤษ หรือ enter")

**บั๊กจริงข้อ 11 — สาเหตุ**: disassemble ต้นฉบับ (`MSXTHA102_original_16k.rom`) ใหม่ทั้งเส้นทางตั้งแต่จุด
เข้าจริง `$4D10` พบว่าโค้ดที่โปรเจกต์นี้เคยดึงมาใช้ก่อนหน้านี้ (`MAIN_ROW_TABLE`/`ROW1_TABLE`) เป็นแค่ชิ้นส่วน
ย่อยของตารางรวมชุดใหญ่กว่าที่ `$4EAB` (48 ไบต์ ครอบคลุม scan code `$00`-`$2F` ทั้งหมดโดยตรง รวมแถวตัวเลขที่
ไม่เคยถูกดึงออกมาก่อน) และมีตารางคู่กันสำหรับตอนกด Shift ค้างอีกชุดที่ `$4EDB` (48 ไบต์เช่นกัน) ที่ไม่เคยถูก
พบ/ใช้มาก่อนเลย — ตัวจริงของ `$4E53` (unified Thai dispatcher) เช็ค `SHIFT_STATE` bit0 เพื่อเลือกว่าจะใช้
ตารางไหน แล้ว index ด้วย scan code ตรง ๆ (ไม่ใช่ offset/compact index แบบที่โค้ดเดิมของโปรเจกต์นี้เข้าใจ) —
นอกจากนี้ยังพบว่าโค้ดเดิมของโปรเจกต์นี้เช็ค `SHIFT_STATE` แบบหยาบ (`AND 3 == 3` จากการแก้บั๊กข้อ 9) ซึ่งไม่
แยกแยะ CTRL ออกจาก SHIFT ให้ถูกต้อง และไม่เช็ค GRAPH เลย

**บั๊กจริงข้อ 11 — การแก้**: แทนที่ `MAIN_ROW_TABLE`/`ROW1_TABLE` เดิมด้วย `THAI_UNSHIFTED_TABLE` (คัดลอกจาก
`$4EAB` ตรง ๆ) และ `THAI_SHIFTED_TABLE` (คัดลอกจาก `$4EDB` ตรง ๆ) ทั้งสองยาว 48 ไบต์ index ด้วย scan code
`$00`-`$2F` โดยตรง (`equ KEYC_THAI_TABLE_N $30`) — แทนที่การเช็ค `SHIFT_STATE` แบบหยาบด้วยการเช็คทีละบิตแม่นยำ:
`BIT 1,A` (CTRL) และ `BIT 2,A` (GRAPH) ปล่อยผ่านเป็นอังกฤษเสมอถ้ากดอยู่ (ตรงกับพฤติกรรมต้นฉบับที่กระโดดไป
ตาราง dispatch อังกฤษ/handler กราฟิกแยกต่างหาก) ส่วน `BIT 0,A` (SHIFT) เลือกตารางแทนที่จะปล่อยผ่าน — เคส
ที่ยังไม่ implement (บันทึกไว้ตรง ๆ ใน comment ไม่เดาสุ่ม): combo พิเศษที่ `$FBE7` bit5 + scan `$20`/`$25`,
"sticky shift" flag ที่ `$FCAB` ของต้นฉบับ, และ composite push พิเศษของค่า `$85` ที่ scan `$07` ในตาราง
shifted (ต้นฉบับ push 2 ไบต์สำหรับคีย์นี้ ไม่ใช่ 1)

**บั๊กจริงข้อ 12 — สาเหตุ**: ใช้ openMSX write-watchpoint (`debug set_watchpoint write_mem 0xFCAC`) จับได้ว่า
มีการเขียนทับ `$FCAC` (ตำแหน่งที่โปรเจกต์นี้เลือกให้ `INPUT_MODE` ใช้ตั้งแต่ Phase 1 — คัดลอกตำแหน่งมาจาก
ต้นฉบับตรง ๆ) โดย PC ที่รายงานคือ `$4188` — ตรวจสอบใน ROM ของเราเองที่ `$4188` พบว่าเป็นแค่คำสั่ง `BIT 2,A`
เฉย ๆ ไม่มีการเขียนหน่วยความจำเลย ซึ่งตรงกับสาเหตุเดียวกันที่เคยพบมาแล้วในบั๊กข้อ 7 (section 9.10): ที่อยู่
เชิงตรรกะช่วง page 1 (`$4000`-`$7FFF`) เดียวกันนี้ ถูกใช้ร่วมกันโดย ROM คนละตัว (cartridge slot ของเรา
กับ BASIC/system ROM slot อื่น) ค่า PC ที่ debugger รายงานจึงหมายถึงโค้ดของ BASIC/system ROM จริง ๆ ไม่ใช่
โค้ดของเรา — ยืนยันด้วย control test บน stock BIOS ล้วน ๆ (ไม่มี cartridge นี้เสียบเลย, CleanMSX1) ว่าการ
พิมพ์+กด BS ปกติไม่แตะ `$FCAC` เลย **เว้นแต่**ผ่านกลไก passthrough ของ RST 30H hook ของเรา (ซึ่งปล่อยให้
BIOS's post-hook dispatch table เดินจนจบเสมอ ตามที่อธิบายไว้ในบั๊กข้อ 10 — ต่างจาก trampoline เดิมที่อาจ
ข้ามบางเส้นทางไปได้) — สรุปคือ `$FCAC`/`$FCAD` เป็นตัวแปรระบบจริงของ BIOS/BASIC เอง ถูกเขียนทับเป็นผลข้าง
เคียงของเส้นทาง default handling หลายเส้นทาง ไม่ใช่แค่ตอนกด BS เท่านั้น — ตรงกับที่ผู้ใช้รายงานเพิ่มเติมว่า
Shift+ตัวอักษร และ Enter ก็ทำให้กลับเป็นอังกฤษได้เหมือนกัน (คนละเส้นทาง default handling ของ BIOS/BASIC แต่
ชนที่อยู่ RAM เดียวกันทั้งหมด) — ข้อสังเกตนี้เคยถูกเตือนไว้แล้วใน section 3 (เลือก RAM ใหม่ไม่ชนกับ BASIC
work area) แต่ตอนนั้นยังไม่มีการทดสอบยืนยันแบบจับ watchpoint จริงบน `$FCAC`/`$FCAD` โดยเฉพาะ

**บั๊กจริงข้อ 12 — การแก้**: ย้าย `INPUT_MODE` จาก `$FCAC` → `$FD11` และ `THAI_MODE` จาก `$FCAD` → `$FD12`
(อยู่ในย่านเดียวกับ `PREV_KEYC`/`MY_SLOT_ID` ที่ผ่านการทดสอบ e2e จริงมามากตลอดทั้งโปรเจกต์โดยไม่เคยพบการชน
เลย) — `PRINT_MODE`/`PLOCK_MODE` ที่ `$FD09`/`$FD0A` (เดิมอยู่แล้ว) ไม่พบปัญหาจึงคงตำแหน่งเดิมไว้ แก้ที่
`src/equates.asm` จุดเดียว (raw address ทั้งหมดรวมศูนย์อยู่ที่นี่ตามหลักการ section 0) โค้ด logic
(`src/init.asm`, `src/thaicmd.asm`, `src/keyboard.asm`) ไม่ต้องแก้เลยเพราะอ้างอิงผ่านชื่อ equate เสมอ

**การยืนยัน**:
1. `test_phase1.py` เคย hardcode ตำแหน่ง `$FCAC`/`$FCAD` ไว้ในไฟล์ทดสอบเองแยกจาก `equates.asm` (ไม่ผ่าน
   symbol table) ทำให้หลังย้าย RAM แล้ว test พังทันที (`THAI_MODE not cleared`) — เป็นการยืนยันทางอ้อมว่า
   การย้ายมีผลจริง — แก้ไฟล์ทดสอบให้โหลดตำแหน่งจาก `build/thairom.sym` เหมือนไฟล์ทดสอบอื่น ๆ ในโปรเจกต์
2. ชุด unit test ทั้ง 4 ไฟล์ผ่านครบทุกเคส (25 เคสรวม) หลังย้าย RAM — ยืนยันว่าไม่กระทบ logic เดิมเลย
3. openMSX regression ใหม่ `test_phase3_bug12_ram_collision.tcl` (แทนที่ `test_phase3_bug11_live.tcl` ซึ่ง
   เป็นแค่สคริปต์สืบสวนสดที่ไม่เช็ค PASS/FAIL เอง) ยืนยันครบทั้งบั๊กข้อ 11 และ 12 แบบ end-to-end จริงผ่าน
   matrix scan จำลอง (`type`) บน `UserMSX1_expanded`: พิมพ์ `123` ได้ font code ไทยตรงตาราง (`163 47 95`),
   Shift+A ได้ `196` (`THAI_SHIFTED_TABLE`) ไม่ใช่ `65`='A', พิมพ์ `a`→กด BS→พิมพ์ `a` อีกที ยังได้ `191`
   ทั้งสองครั้ง (ไม่ใช่ `97`='a' อังกฤษ), และเพิ่มเคสตามที่ผู้ใช้รายงานรอบสอง: Shift+A→พิมพ์ `a` ต่อ ยังได้
   `191`, และกด Enter→พิมพ์ `a` ต่อ ยังได้ `191` — ครบทุกอาการที่ผู้ใช้รายงานทั้งสองรอบ **ALL PASS**

**ข้อจำกัดที่ยังไม่ทดสอบ**: การยืนยันทั้งหมดนี้ทำบน openMSX (`UserMSX1_expanded`) เท่านั้น ยังไม่ได้ทดสอบซ้ำบน
ฮาร์ดแวร์จริง/blueMSX ของผู้ใช้ — ควรขอให้ผู้ใช้ทดสอบซ้ำทั้ง 3 อาการเดิมบนเครื่องจริงอีกครั้งก่อนถือว่าปิดบั๊ก
ทั้งสองข้อนี้สมบูรณ์

### 9.15 คำขอผู้ใช้: banner ตอน boot + THAION เป็น default + THAIOFF ไม่เคลียร์หน้าจอ

**คำขอ**: (1) เพิ่ม banner ใต้บรรทัด Copyright ตอน boot ข้อความ "Thai BASIC version 1.0" (2) THAION เป็น
default ตอน boot ไม่ต้องพิมพ์เอง (3) THAIOFF ไม่ต้องเคลียร์หน้าจอ

**สืบสวน (สำคัญ — เปลี่ยนวิธีคิดเรื่อง `INIT` ทั้งหมด)**: ทดสอบสดบน openMSX (cold boot เต็มรูปแบบ ไม่ใช่
เรียก `INIT` ตรง ๆ แบบ `emutest.py`) พบข้อเท็จจริง 3 ข้อที่ทำให้วิธี "ง่ายที่สุด" ใช้ไม่ได้จริง:

1. `INIT` ของเราถูกเรียกจริง (ยืนยันด้วยการเปลี่ยนสี border ผ่าน VDP port ตรง ๆ ใน `INIT` แล้วเห็นสีเปลี่ยน
   จริงบน screenshot) แต่ถูกเรียก **หลัง** BIOS แสดง splash แรกของมันเอง ("MSX system / version 1.0 /
   Copyright...") ไปแล้ว ไม่ใช่ก่อนอย่างที่เข้าใจไว้แต่แรก
2. ไม่ว่าจะพิมพ์ข้อความผ่าน `CHPUT` ตรง ๆ ใน `INIT` (มี `call INITXT` ก่อนหรือไม่ก็ตาม) ข้อความนั้นก็ถูก
   บัง/ลบไปเสมอ ไม่รอดถึงจอสุดท้าย — ยืนยันด้วย screenshot ที่ boot เสร็จแล้วเห็นแต่ banner ปกติของระบบ
   ไม่มีข้อความของเราเลย — สาเหตุ: BIOS's เองยังมีขั้นตอนพิมพ์ banner จริงของระบบต่ออีกหลังจาก `INIT`
   ของเรา `RET` กลับไป (เรียก `INITXT` ซ้ำ + พิมพ์ "MSX BASIC/Copyright/Bytes free" ของมันเอง) ซึ่งเคลียร์
   จอทับข้อความของเราไปหมด
3. รอมต้นฉบับ (`MSXTHA102`) แก้ปัญหานี้ด้วยการ hijack stack return address ให้กระโดดข้ามขั้นตอน banner
   ของ BIOS เองไปเลย (ผู้ใช้ยืนยันเองว่า ROM เดิมค้างอยู่ที่จอ splash ของตัวเองตลอด ไม่เคยโชว์ banner ปกติ
   ของระบบเลย — ทดสอบจริงยืนยันตรงกัน) — นี่คือเทคนิค "internal address / stack hijack" แบบเดียวกับที่
   หลักการออกแบบของโปรเจกต์นี้ (section 0) ห้ามใช้ตั้งแต่ต้น และเป็นสาเหตุของบั๊กข้อ 7/8 ที่เพิ่งแก้ไปเอง
   ในเซสชันนี้ — ตัดสินใจไม่เลียนแบบเทคนิคนี้

**วิธีแก้ที่เลือก (safe, portable, ไม่พึ่ง internal BIOS address ใด ๆ เลย)**: "stuff" ตัวอักษรเข้าคิว
คีย์บอร์ด (`$FBF0`, documented system variable, ผ่าน `QUEUE_PUSH_CHAR` ตัวเดียวกับที่ผ่านการทดสอบมาเต็มที่
ตลอดทั้งโปรเจกต์) ให้เหมือนผู้ใช้พิมพ์คำสั่ง `CALL THAION:?"Thai BASIC version 1.0"` เองทันทีที่ Ok prompt
พร้อมรับ input — วิธีนี้ปล่อยให้ BIOS boot ปกติสมบูรณ์ทุกขั้นตอนโดยไม่ถูกรบกวนเลย (ไม่เสี่ยง hang/ผิดเพี้ยน
ข้าม BIOS version เหมือนข้อ 3) แล้วอาศัยกลไกมาตรฐานของ BASIC เอง (อ่านคิวคีย์บอร์ดที่ Ok prompt) รันคำสั่ง
ของเราหลังบูตเสร็จสมบูรณ์แล้วเท่านั้น — ข้อความ/การเปิดใช้ Thai mode จึง "รอด" อยู่บนจอจริง เพราะไม่มีขั้นตอน
ใดของ BIOS ที่เคลียร์จอ/คิวหลังจุดนี้อีกแล้ว (Ok prompt คือจุดสุดท้ายของ cold boot)

ใช้ `CALL THAION` (ไม่ใช่การตั้ง flag/โหลดฟอนต์/ติดตั้ง hook ตรง ๆ ใน `INIT`) เพราะเหตุผลเดียวกับข้อ 2:
การโหลดฟอนต์เข้า VRAM หรือติดตั้ง `H.KEYC` hook ตอน `INIT` (ก่อน BIOS's เองเรียก `INITXT` รอบสุดท้าย) มี
ความเสี่ยงเดียวกันที่จะถูกขั้นตอน boot ที่เหลือของ BIOS เขียนทับ/รีเซ็ตอีก — ให้ BASIC "พิมพ์" `CALL THAION`
เองหลังบูตเสร็จ รับประกันว่า `CMD_THAION` (โค้ดเดิมที่ทดสอบมาเต็มที่แล้ว) ทำงานในบริบทเดียวกับตอนผู้ใช้พิมพ์
เองทุกประการ ไม่มีความเสี่ยงเรื่องจังหวะ boot เลย — ยืนยันแล้วว่าต้องมี `CALL ` นำหน้าเสมอ (ทดสอบพิมพ์
`thaion` เปล่า ๆ ไม่มี `CALL` แล้วได้ "Syntax error" จริงบน openMSX)

ไม่ auto-enable `INPUTON` ด้วย เพราะคำขอผู้ใช้ระบุแค่ "thaion เป็น default" — โหมดพิมพ์ไทยจริง (`INPUTON`)
ยังคงต้องกดปุ่มสลับโหมดเองตามปกติเหมือนการใช้งานทั่วไป

ขนาดคิวคีย์บอร์ดจริง = 40 ไบต์ (ยืนยันจาก disassembly ของ `KEYQ_ADVANCE` ที่ `0x10C2` — ดู 9.9: wrap เมื่อ
`L` ถึง `0x18` นับจาก `$FBF0` = `(0x100-0xF0)+0x18` = 40 ไบต์) — ข้อความรวม `CALL THAION:?"Thai BASIC
version 1.0"` + CR ยาว 38 ไบต์ อยู่ในขอบเขตปลอดภัย

**ผลลัพธ์ที่เห็นจริงตอน boot** (ยืนยันด้วย screenshot บน openMSX): บูตปกติทุกอย่าง (splash MSX system →
"MSX BASIC/Copyright 1983 by Microsoft/Bytes free/Ok") แล้วต่อด้วยบรรทัด `CALL THAION:?"Thai BASIC
version 1.0"` (echo ของคำสั่งที่ auto-stuff, พฤติกรรมปกติของ BASIC direct mode) แล้วบรรทัด "Thai BASIC
version 1.0" (ผลของ `?`/`PRINT`) แล้ว `Ok` ใหม่พร้อมใช้งาน — ข้อความ banner **อยู่ใต้ Copyright จริง**
(อยู่ใต้ทั้ง banner มาตรฐานของระบบทั้งชุด ซึ่งเป็นทางเดียวที่ทำให้มันรอดจนถึงจอสุดท้ายได้อย่างปลอดภัย โดยไม่
ต้องแก้ internal BIOS behavior เลย) และ Thai mode เปิดอยู่แล้วพร้อมใช้งานทันที (ตรงตามคำขอข้อ 2)

**THAIOFF ไม่เคลียร์หน้าจอ**: `CMD_THAIOFF` เดิมเรียก `INITXT` เพื่อ reload ฟอนต์มาตรฐานกลับเข้า VRAM แบบ
portable (ดูเหตุผลเดิมในหัวข้อ 3) แต่ `INITXT` เคลียร์ทั้งจอ+รีเซ็ต cursor เป็นผลข้างเคียงเสมอ — ตรวจสอบแล้ว
ว่าฟอนต์ไทย (`FONT_THAI`) กับฟอนต์มาตรฐานของระบบ เหมือนกันแทบทุกไบต์ในช่วง ASCII พิมพ์ได้ (`0x20`-`0x7E`):
เทียบ byte-by-byte จริงบน openMSX (VRAM dump หลัง cold boot ปกติไม่มี cartridge เทียบกับ `assets/
font_raw.bin`) พบต่างแค่ 1 ตัวอักษร (`'b'` รหัส 98) และต่างแค่เศษพิกเซลเดียว (ไม่กระทบการอ่านเลย) — จึงไม่
จำเป็นต้อง reload ฟอนต์ตอน `THAIOFF` เลย เอา `call INITXT` ออกตรง ๆ ปลอดภัย

**การยืนยัน**: แก้ `test_phase1.py` ให้โหลด `INIT`/`STATEMENT`/`PROCNM` จาก `build/thairom.sym` แทนการ
hardcode (เดิม hardcode ตรง ๆ ทำให้พังทันทีที่ `INIT` ยาวขึ้นและ `STATEMENT` เลื่อนที่ — รูปแบบบั๊กเดียวกับ
บั๊กข้อ 12) — แก้ openMSX regression 2 ไฟล์ (`test_phase3_expanded_slot_bug8.tcl`,
`test_phase3_thai_typing_bug9_bug10.tcl`) ให้อ่าน offset จาก `KEYQ_TAIL` จริงแทน offset คงที่มือ (เดิมสมมติ
ว่าคิวเริ่มว่างเปล่าที่ `$FBF0` ตอน test เริ่มพิมพ์ ซึ่งไม่จริงอีกต่อไปเพราะ auto-exec ตอน boot ใช้พื้นที่คิว
ไปก่อนแล้ว 38 ไบต์) — ชุด Python test ทั้ง 25 เคสผ่านครบ, openMSX regression ทั้ง 4 ไฟล์ผ่านครบ
(`test_full_regression.tcl`, `test_phase3_expanded_slot_bug8.tcl`, `test_phase3_thai_typing_bug9_bug10.tcl`,
`test_phase3_bug12_ram_collision.tcl`) ไม่มี regression จากการเปลี่ยนแปลงนี้เลย

**ยังไม่ทำ (คำขอข้อ 4: printon จัดระดับ)**: การซ้อนพยัญชนะ+สระ+วรรณยุกต์หลายตัวลงช่องเดียวกันแบบ VRAM
OR-compositing (`COMPOSITE_GLYPH`, section 5) ยังไม่ implement — เป็นงานแยกขนาดใหญ่ที่ต้องการ disassemble
กลไก compositing จริงของต้นฉบับเพิ่มเติม (ยังไม่เคยทำมาก่อนในโปรเจกต์นี้) ดู section 5 สำหรับ design ที่วางไว้

### 9.16 บั๊กจริงข้อ 13 (แก้แล้ว): "มีตัว C เกินมาทั้งที่ยังไม่ได้กดอะไร"

**รายงานจากผู้ใช้**: หลังส่งมอบ 9.15 ผู้ใช้ทดสอบจริงแล้วพบตัวอักษร "C" โผล่มาเองบนจอตอน boot เสร็จ
ทั้งที่ยังไม่ได้กดคีย์บอร์ดเลย

**การสืบสวน**: ตั้ง breakpoint จริงบน openMSX debugger ที่ทั้ง entry ของ `INIT` และที่ `ret` ท้าย
stuffloop แล้ว dump `KEYQ_TAIL`/`GETPNT`/เนื้อคิวดิบ (`$FBF0`-`$FC17`) ทุกครั้งที่ hit ระหว่าง cold
boot เต็มรูปแบบ พบว่า **BIOS เรียก cartridge `INIT` ของเราถึง 2 ครั้ง** ระหว่าง boot จริง (ครั้งแรก
stuff ครบ 38 ไบต์ปกติ, ครั้งที่ 2 เริ่ม stuff ซ้ำจาก `BOOT_AUTOEXEC` ใหม่ทั้งหมดโดยไม่เช็คสถานะคิวก่อน
เลย) — นี่เป็นพฤติกรรมมาตรฐานที่รู้จักกันดีของ MSX BIOS จริง (มีรอบสแกนสล็อตเบื้องต้นก่อนกำหนด
RAM/ระบบเต็มรูปแบบ แล้วมีรอบ INIT จริงอีกที คาร์ทริดจ์ที่ดีต้องทนต่อการถูกเรียก INIT ซ้ำได้เสมอ) ไม่ใช่
ครั้งเดียวอย่างที่สมมติไว้ตอนออกแบบ 9.15

กลไกที่ทำให้เกิด "C" เกินมา: รอบที่ 2 เขียน 'C' (ตัวแรกของ `BOOT_AUTOEXEC`) ลงช่องว่างที่เหลือ (index 38)
สำเร็จ, ตัวที่ 2 'A' ทำให้คิว "เต็ม" (wrap กลับมาชนตำแหน่ง GETPNT เดิมที่ `$FBF0`) ทำให้ `QUEUE_PUSH_CHAR`
หยุดเลื่อน tail แต่ `.stuffloop` **ไม่เช็คค่าที่ `QUEUE_PUSH_CHAR` คืนมาเลย** จึงเดินลูปต่อจนครบทุกตัวอักษร
ที่เหลือ เขียนทับช่องเดิม (index 39) ซ้ำไปเรื่อย ๆ จนตัวสุดท้ายที่ค้างไว้คือ CR — คิวจริงกลายเป็น
`[...บรรทัดคำสั่งที่ถูกต้อง 38 ไบต์...] + "C" + CR` ซึ่ง BASIC อ่านเป็น "บรรทัดถัดไป" หลัง Ok แรกพอดี
= พิมพ์ "C" แล้ว Enter เองโดยผู้ใช้ไม่ได้กดอะไรเลย (ตามด้วย Syntax error + Ok อีกรอบ)

**วิธีแก้** (portable, ไม่เพิ่มตัวแปร RAM ใหม่, ไม่พึ่ง internal address เพิ่มเติมเลย): เช็ค idempotency
ด้วยตัวคิวคีย์บอร์ดเองตรง ๆ ก่อน stuff ทุกครั้ง — ถ้า `KEYQ_TAIL` (byte ต่ำ, ตำแหน่งเขียน) ไม่เท่ากับ
`KEYQ_END`/GETPNT (byte ต่ำ, ตำแหน่งอ่าน) แปลว่าคิว "ไม่ว่าง" อยู่แล้ว (มีของค้างจาก stuff รอบก่อนหน้าที่
ยังไม่ถูก BASIC อ่านไปหมด) จึงข้าม stuff รอบนี้ไปเลย — ทนต่อ `INIT` ถูกเรียกกี่ครั้งก็ได้ ดู
`src/init.asm` สำหรับโค้ดและ comment เต็ม

**การยืนยัน**: สร้าง diagnostic script ตั้ง breakpoint ที่ `INIT` entry/exit ทั้งก่อนและหลังแก้ — ก่อนแก้
เห็นรอบที่ 2 เขียนทับคิวชัดเจน (index 38/39 กลายเป็น 67/13), หลังแก้รอบที่ 2 ข้าม stuff ทันที (คิวไม่
เปลี่ยนแปลงจากรอบแรกเลย) และตอน boot settle เต็มที่ `GETPNT` ไล่ทัน `KEYQ_TAIL` พอดีที่ `$FC16` (คิวว่าง
สนิท ไม่มีไบต์ค้าง) — screenshot ยืนยันจอสุดท้ายมีแค่ "Ok" เดียว ไม่มี "C" โผล่มาอีก — รัน regression
suite ทั้งหมดซ้ำ (Python 25 เคส + openMSX 3 ไฟล์ที่มี pass/fail assertion จริง) ผ่านครบไม่มี regression

**เรื่องที่ยังไม่แก้ (คำขอ: ซ่อนบรรทัด `CALL THAION...` และบรรทัดว่าง)**: สืบสวนเพิ่มเติมพบว่า:
1. บรรทัด echo ของคำสั่งที่ auto-type เป็นผลข้างเคียง **ที่หลีกเลี่ยงไม่ได้** ของกลไก keyboard-queue-stuff
   เอง (BASIC line-input echo ทุกตัวอักษรที่อ่านจากคิวเสมอ ไม่มีทางสั่ง "อ่านแบบเงียบ" ได้โดยไม่แตะ
   internal address)
2. บรรทัดว่างที่เห็นระหว่าง echo กับ banner **ไม่ใช่พฤติกรรมคงที่** — ทดสอบพิมพ์มือหลายแบบ (`CALL
   THAION:?3`, `CALL THAION:PRINT3`, `CALL THAION:PRINT"hi"`, `CALL THAION:?"hi"`) ล้วน**ไม่มี**บรรทัด
   ว่าง มีแค่กรณี `CALL THAION:?"Thai BASIC version 1.0"` เป๊ะ ๆ (ยาว 38 ไบต์ พอดีคอลัมน์ที่ 40 แบบไม่
   wrap) ที่เจอบรรทัดว่าง — ดูเหมือนเป็น edge case ของ BASIC ROM เองเกี่ยวกับตำแหน่งคอลัมน์/ความยาว
   บรรทัด ไม่ใช่บั๊กที่เราควบคุมได้จากฝั่ง `STATEMENT`/`CMD_THAION`
3. คิวคีย์บอร์ดมีที่ว่างจริงแค่ 39 ไบต์ (ดูหัวข้อ 9.15) และคำสั่ง auto-exec ปัจจุบันใช้ไปแล้ว 38 ไบต์ —
   **ไม่เหลือที่ให้แทรกคำสั่ง "ลบ echo" เพิ่มเข้าไปในรอบ stuff เดียวกันได้เลย**
4. ทางแก้ที่เป็นไปได้จริงคือสร้างกลไกใหม่ (เช่น hook บน interrupt เพื่อลบ/ย้ายข้อความบนจอเองหลัง PRINT
   เสร็จ โดยอ่าน `CSRY`/`$F3DC` หาแถว "Ok" เดิมเป็นจุดอ้างอิง แล้วลบ/เขียนทับ VRAM name table ตรง ๆ ผ่าน
   `RDVRM`/`WRTVRM` ที่พิสูจน์แล้วว่าปลอดภัย, และย้าย cursor กลับด้วย `ESC 'Y'` sequence ผ่าน `CHPUT`
   เหมือนที่ต้นฉบับใช้วาด splash) — เป็น subsystem ใหม่ขนาดไม่เล็ก ต้องการการยืนยันเชิงประจักษ์ของตัวเอง
   เต็มรูปแบบ (ระดับใกล้เคียงกับงาน `COMPOSITE_GLYPH` ที่เลื่อนไว้ใน 9.15) — ยังไม่ implement รอ
   การตัดสินใจของผู้ใช้ว่าจะทำต่อตอนนี้เลยหรือรอทีหลัง

### 9.17 PRINTON implement จริงแล้ว (ทดสอบผ่าน openMSX) — สถาปัตยกรรม, บั๊กที่เจอ+แก้, และขอบเขตที่ยังไม่ครบ

ตามคำขอผู้ใช้ "printon ต่อ" — implement PRINTON เต็มรูปแบบใน `src/printon.asm` (ไฟล์ใหม่) แล้ว
ยืนยันผ่าน openMSX จริงว่าคอมโพสิต 3 ระดับทำงานถูกต้อง

**สถาปัตยกรรม (ปัญหาหลักที่ต้องแก้ก่อน implement ได้เลย):** CHPUT ตัวจริงของ BIOS (ยืนยันจาก
disassemble ตรง ๆ ที่ `0x08BC-0x08DE` ของ `MSX1_bios.rom`) เรียก `CALL $FDA4` เป็นก้าวแรกเสมอ
ก่อนวาดตัวอักษร default เอง **โดยไม่มีสัญญาณใด ๆ ให้ hook บอกว่า "ฉันจัดการเองแล้ว ข้ามการวาด
default ที"** — นี่คือเหตุผลที่ต้นฉบับ MSXTHA102 ใช้เทคนิค stack-hijack (แก้ return address บน
stack ตรง ๆ) ซึ่งโปรเจกต์นี้ห้ามใช้ (section 0) เพราะ fragile/ไม่ portable ข้าม MSX generation

วิธีแก้แบบ portable ที่ใช้จริง: disassemble ต่อพบว่า (1) ตัวอักษร (A) ที่ hook ได้รับ BIOS จะ
**ไม่ยอมใช้ค่าที่ hook แก้** (ใช้ค่าเดิมที่ push ไว้ก่อนเรียก hook เสมอ) แต่ (2) **ตำแหน่งที่จะวาด
(CSRY/CSRX ที่ `$F3DC`/`$F3DD`) ถูกอ่านสดจาก RAM ทุกครั้งก่อนวาดจริง** ไม่ผ่าน register ที่ hook
คุมได้โดยตรง — ดังนั้น hook สามารถ "ยักย้าย CSRY/CSRX ชั่วคราว" ก่อน return เพื่อหลอกให้ BIOS
วาดตัวอักษร (สระ/วรรณยุกต์) ที่ตำแหน่งซ้อน (แถวบน คอลัมน์เดิมของพยัญชนะ) แทนตำแหน่ง cursor จริง
ได้ — ปล่อยให้ BIOS วาด+เลื่อน cursor ของมันเองตามปกติ (ไม่ต้อง WRTVRM เอง) แล้วค่อย "คืนค่า CSRY
ที่ถูกต้อง" ตอน hook ถูกเรียกครั้งถัดไป (เพราะไม่มีจุดให้รันโค้ดหลัง BIOS วาด+เลื่อนเสร็จในรอบ
เดียวกันเลย) — ใช้ RAM shadow 2 ไบต์ (`PRINT_ROW`, `PRINT_REDIRECT`) เก็บ "แถวจริง" กับ "ยักย้าย
ค้างอยู่หรือไม่" ดูรายละเอียดเต็มที่ comment ใหญ่ต้นไฟล์ `src/printon.asm` และที่ `PRINTHOOK` เอง

สูตรคำนวณ VRAM address ของ name table ((CSRY-1)\*40+CSRX+1 — ดู comment ที่ `NAMETAB_ADDR` และที่
equate `LINLEN` ใน `equates.asm`) ยืนยันด้วย breakpoint จริงที่ `CALC_ADDR` ($0BF2 ของ MSX1 BIOS)
เทียบกับสูตรที่คำนวณเอง ไม่ใช่การเดา — พบว่า `LINLEN` (37 ที่เคยสงสัยว่าผิดปกติใน section 9.6)
ไม่เกี่ยวกับสูตรนี้เลยตอน `SCRMOD=0` เลย เป็นแค่ค่าที่ BASIC ใช้ภายในเพื่อ word-wrap เท่านั้น

**บั๊กที่เจอระหว่าง implement + วิธีแก้ (ยืนยันด้วย breakpoint จริงทั้งคู่ ไม่ใช่แก้เดา):**
1. `NAMETAB_ADDR` (แปลง แถว/คอลัมน์ → VRAM address สำหรับอ่านตรวจว่ามีสระบนอยู่แถวเหนือหรือไม่)
   คำนวณ address ขาดไป 1 เสมอ (เทียบกับที่ `CALC_ADDR` ของ BIOS จริงคำนวณ) — สาเหตุคือ `CALC_ADDR`
   จริงมีขั้นตอนเพิ่มที่ไม่เคยเห็นตอน disassemble รอบแรก (LINLEN-derived offset ที่ `$0C0E-$0C14`
   แล้วค่อยบวก `NAMPNT` ที่ `$0C16-$0C19` แล้ว `DEC HL` ที่ `$0C1A`) ซึ่งรวมกันแล้วเท่ากับค่าคงที่
   "+1" พอดีตราบใดที่ `SCRMOD=0`/`LINLEN=37`/`NAMPNT=0` คงที่ตลอด session (จริงเสมอสำหรับโปรเจกต์
   นี้) — แก้ด้วยการเติม `inc hl` ท้าย `NAMETAB_ADDR` แทนการ port สูตร LINLEN ที่ซับซ้อนและผูกกับ
   `SCRMOD=0` อยู่แล้วโดยธรรมชาติ
2. `NAMETAB_ADDR` เอง (`ld de,(NAMPNT)`) โหลดทั้ง D "และ" E ทับค่าคอลัมน์เป้าหมายที่ผู้เรียกตั้งไว้
   ใน E ตั้งแต่ต้น (ไม่ใช่แค่ D ตามที่ comment เดิมเข้าใจผิด) ทำให้วรรณยุกต์ที่ควรซ้อนคอลัมน์เดียวกับ
   พยัญชนะ กลับไปอยู่คอลัมน์ 0 เสมอ (เจอจริงผ่าน breakpoint ที่ `WRITE_CHAR` เทียบ column จริงที่ใช้
   วาด) — แก้ด้วยการ `push de`/`pop de` ครอบขั้นตอนบวก `NAMPNT`

ทั้งสองบั๊กเจอและแก้ด้วยระเบียบวิธีเดียวกับที่โปรเจกต์นี้ใช้มาตลอด: ตั้ง breakpoint ที่ตำแหน่งจริง
ใน BIOS/ของเราเอง อ่านค่า register จริง เทียบกับที่คำนวณเองบนกระดาษ ไม่เชื่อ comment/สมมติฐานเดิม
เฉย ๆ

**ขอบเขตที่ implement จริง (การตัดสินใจเชิงวิศวกรรม):** ต้นฉบับมีตาราง classification ที่ใหญ่กว่านี้
มาก (42 code รวมถึงช่วง `0x82-0x9D` ที่ระบุความหมายแน่ชัดไม่ได้แม้ disassemble ลึกแล้ว — ดู
`printon_algorithm_report.md` section 7) แทนที่จะ port ตารางที่ไม่เข้าใจความหมาย 100% implement
เฉพาะ 2 กลุ่มที่ยืนยันความหมายชัดเจนจาก `THAI_UNSHIFTED_TABLE` เอง: สระบน 5 ตัว (ั ิ ี ึ ื) ซ้อน
แถว-1 เสมอ, วรรณยุกต์ 4 รูป (่ ้ ๊ ๋) ซ้อนแถว-1 ถ้ายังไม่มีสระบน หรือแถว-2 ถ้ามี — **สระล่าง (อุ/อู)
และโค้ดกลุ่ม `0x82-0x9D` ที่ยังไม่ทราบความหมายแน่ชัด ไม่ได้ทำการซ้อนพิเศษ** (ปล่อยให้ BIOS วาดแบบ
ปกติ) เพราะไม่พบหลักฐานว่าต้นฉบับเคยเขียนอักษรจริงไปแถว+1 เลยในทุก path ที่ตามรอยได้ — เป็นการ
ตัดสินใจที่ตั้งใจ สอดคล้องกับหลักการ "ถูกต้องตามพฤติกรรมที่ยืนยันได้ ไม่ใช่ byte-identical กับกลไก
ลึกลับที่ไม่มีเอกสารรองรับ"

**การขึ้นบรรทัดใหม่ (แนวตั้ง):** ผู้ใช้ยืนยัน "24 บรรทัดจะเหลือ 8 บรรทัดโดยใช้ 3 line ต่อบรรทัด" —
`PRINTHOOK` ดักตัวอักษร LF (`$0A`, ตัวที่ทำให้แถวเลื่อนจริงตามที่ยืนยันจาก breakpoint trace — CR
แค่รีเซ็ตคอลัมน์) ตอน `PRINT_MODE=TRUE` แล้วโก่ง `CSRY` ล่วงหน้า +2 ก่อนคืนให้ BIOS ทำ +1 ของมันเอง
ต่อ รวมเป็น +3 สุทธิ *** ข้อจำกัดที่ทราบและยังไม่ได้แก้ (ไม่ครบ 100%): ยังไม่ได้จัดการกรณีใกล้ขอบล่าง
จออย่างสมบูรณ์ (กันคร่าว ๆ ด้วยการข้ามการโก่งถ้า `CSRY>=22` เพื่อไม่ให้ scroll ผิดจังหวะ แต่ไม่ได้
ทดสอบละเอียดว่า scroll ทำงานถูกต้องสมบูรณ์แบบทุกกรณีใกล้ขอบจอ) — ควรทดสอบเพิ่มก่อนใช้งานจริงกับ
ข้อความยาวเกิน 1 หน้าจอ

**การยืนยัน (openMSX จริง, ไม่ใช่ทฤษฎี):** เทส `test_printon_compositing.tcl` (ใหม่) — พิมพ์ ก
(consonant) + ิ (สระบน) + ่ (วรรณยุกต์) ผ่าน THAION+INPUTON จริง (matrix scan จริง ไม่ใช่ poke คิว
ตรง ๆ) ยืนยันว่า 3 ตัวอักษรลง VRAM ที่ตำแหน่ง (แถวเดียวกับพยัญชนะ), (แถว-1), (แถว-2) ตามลำดับ ใน
คอลัมน์เดียวกันเป๊ะ ("PASS: PRINTON compositing (3-row stacking) ถูกต้องครบทุกจุด") — และแยกยืนยัน
ว่าเมื่อ `PRINT_MODE` ไม่เคยเปิดเลย ตัวอักษรชุดเดียวกันวาดเรียงปกติคนละคอลัมน์ในแถวเดียว (ไม่ซ้อน)
ยืนยันว่า flag คุมพฤติกรรมได้จริง — รัน `test_full_regression.tcl`/`test_phase3_thai_typing_bug9_bug10.tcl`/
`test_phase3_bug12_ram_collision.tcl`/`test_plock_verify.tcl` ซ้ำแล้วยืนยันไม่มี regression จากงาน
PRINTON นี้ (หมายเหตุ: ค่า `H_KEYC opcode after thaioff` ใน `test_full_regression.tcl` อ่านผิดจาก
ที่คาดตลอด — ยืนยันแล้วว่าเป็น artifact เดิมของ **ROM เวอร์ชันก่อนงาน PRINTON นี้ด้วย** ไม่ใช่สิ่งที่
งานนี้ทำให้เกิดใหม่ — ยังไม่ไล่หาสาเหตุจริง เก็บไว้เป็นงานถัดไป)

**ยังไม่ implement / คำถามเปิดที่เหลือ:** (1) ไม่มีสัญญาณ visual ใด ๆ เตือนผู้ใช้เมื่อพิมพ์ถึงขอบ
ล่างจอขณะ PRINTON เปิด (นอกจากการข้าม LF-bump แบบเงียบ ๆ ตามข้อจำกัดด้านบน) (2) ยังไม่ทดสอบ
PRINTON ร่วมกับ INPUTOFF (พิมพ์โค้ด font ไทยตรง ๆ ผ่าน `CHR$()`/`PRINT` แทนคีย์บอร์ด) แม้ควรทำงาน
เหมือนกันโดยหลักการ (เพราะ hook ทำงานที่ระดับ CHPUT ไม่ใช่ระดับคีย์บอร์ด) (3) `PLOCKON` ยังไม่ได้
ทดสอบร่วมกับ PRINTON โดยตรง (ทดสอบแยกกับ PRINT_MODE/INPUT_MODE ใน `test_plock_verify.tcl` เดิมแล้ว
เท่านั้น)

### 9.18 แก้ไข PRINTON รอบสอง: "ผสม glyph" สระบน+วรรณยุกต์, สระล่างซ้อนแถว+1, undo ด้วย Backspace
(ผู้ใช้แก้ไขความเข้าใจผิดของ 9.17 โดยตรง + ยืนยันด้วยฟอนต์บิตแมปจริง)

หลัง 9.17 implement เสร็จและทดสอบผ่าน ผู้ใช้ชี้ correction 3 จุดโดยตรง ทำให้พบว่าความเข้าใจใน 9.17
ผิด 2 จุด (ไม่ใช่แค่ปรับแต่ง แต่เป็นการแก้พฤติกรรมที่ผิด) และเพิ่ม 1 ฟีเจอร์ที่ยังไม่มี:

**1. วรรณยุกต์ตามหลังสระบนต้อง "ผสม" เป็น glyph เดียว ไม่ใช่ซ้อนแถว-2 แยก** -- ผู้ใช้ระบุตรง ๆ ว่า
"ไม้หันอากาศ ไม้เอกต้องผสมกัน แล้ววาดแทนไม้หันอากาศเดิม ที่ N-1" จากนั้นผู้ใช้อัปโหลด `thaifont.asm`
(ฟอนต์บิตแมปจริง 256 ตัวอักษรพร้อม comment ระบุตัวอักษรแต่ละโค้ด) ซึ่งยืนยันตรงกันทั้งสองทาง:
- disassembly ของ original ROM เอง (`printon_algorithm_report.md` section 1.5/2.1, ตาราง `$5582`
  ที่ `$521D`/`$5272` ใช้) มีกลุ่ม lookup 5 ชุด (D1,D4,D5,D6,D7 แต่ละชุด x 4 วรรณยุกต์) ที่คืนค่าเป็น
  โค้ดในช่วง `0x82-0x9D` -- ช่วงที่ 9.17 เคยบันทึกไว้ว่า "ความหมายไม่ชัดเจนแม้ disassemble ลึกแล้ว"
- pixel-match กับ `thaifont.asm` ตรงทุกไบต์: โค้ด `0x83` (แถวบน 2 แถว = รูปวรรณยุกต์ ่ เป๊ะ, แถวล่าง
  6 แถว = รูปสระ `$D1` เดี่ยว ๆ เป๊ะ) และเช่นเดียวกันสำหรับ `0x85-0x91` ทั้งหมด (เทียบกับ `$D1,$D4,$D5`
  เดี่ยว ๆ และ `$E8,$E9,$EA,$EB` เดี่ยว ๆ ทีละไบต์) -- ยืนยันว่าโค้ดกลุ่มนี้คือ glyph ที่ผสม
  สระบน+วรรณยุกต์ไว้แล้วในตัวเดียวจริง ไม่ใช่อะไรลึกลับ
- แก้โดยเพิ่ม `COMBINE_TABLE`/`COMBINE_LOOKUP` ใน `src/printon.asm`: เมื่อวรรณยุกต์ตามหลังสระบน
  (D1/D4/D5) ที่แถว-1 คอลัมน์เดียวกัน ให้ lookup โค้ดผสมแล้วเขียนทับตำแหน่งสระบนเดิมที่แถว-1 แทน
  -- **ปัญหาทางสถาปัตยกรรม**: hook ไม่มีทางแทนที่ตัวอักษรที่ BIOS กำลังจะวาดได้เลย (ดู 9.17) จึง
  ต้องปล่อยให้ BIOS วาดวรรณยุกต์ตัวเปล่าทับตำแหน่งนี้ไปก่อน (ผิดชั่วคราว 1 จังหวะ ไม่มีผลเห็นจริง
  เพราะแก้เสร็จก่อนเฟรมถัดไปแสดงผล) แล้วแก้ทับด้วย `WRTVRM` ตรง ๆ ตอนเรียก `PRINTHOOK` **ครั้งถัดไป**
  (ก่อนประมวลผลตัวอักษรใหม่ใด ๆ เลย) -- ดู `PRINT_COMBINE_PENDING/ROW/COL/CODE` ใน `equates.asm`
  ข้อจำกัดที่ทราบ: ถ้าผู้ใช้หยุดพิมพ์ทันทีหลังวรรณยุกต์ที่ต้องผสม (ไม่กดอะไรต่อเลย) ช่องนั้นจะค้าง
  แสดงวรรณยุกต์เปล่า (ยังไม่ผสม) จนกว่าจะมี `CHPUT` ครั้งถัดไป (คีย์ถัดไป, Enter, หรือ scroll ก็ได้)

**2. Backspace ทันทีหลังวรรณยุกต์ที่ผสมไป ต้อง undo กลับเป็นสระบนเปล่า ๆ** -- ผู้ใช้ระบุ: "ทำนอง
เดียวกันถ้าลบวรรณยุกต์ออกต้องกลับมาวาดสระบน" -- แก้โดยเพิ่ม `PRINT_LAST_MARK_VALID/ROW/COL/VOWEL`
ใน `equates.asm`: ทุกครั้งที่ผสม glyph สำเร็จ (ข้อ 1) จะบันทึกตำแหน่ง+โค้ดสระบนเดิมไว้ด้วย valid แค่
"ตัวถัดไปที่พิมพ์ทันที" เท่านั้น (ถูกเคลียร์ทิ้งถ้าตัวถัดไปไม่ใช่ Backspace `$08`) ถ้าเป็น Backspace
จริง จะ `WRTVRM` เขียนทับกลับเป็นสระบนเปล่า ๆ ทันที (ไม่ต้อง defer เพราะ BIOS ไม่เขียน VRAM อะไรเอง
สำหรับ control code `<0x20` อยู่แล้ว) -- ทดสอบยืนยันว่า pending-fix (ข้อ 1) กับ undo (ข้อ 2) ทำงาน
ถูกต้องแม้อยู่ใน `PRINTHOOK` call เดียวกันพอดี (พิมพ์วรรณยุกต์แล้ว Backspace ติดกันใน statement
เดียว `PRINT CHR$(...)CHR$(8)`) เพราะ deferred-fix (ขั้น 1.6) รันก่อน BS-check (ขั้น 1.7) เสมอ ผล
สุดท้ายที่เห็นจริงจึงถูกต้องไม่มี flicker

**3. สระล่าง (อุ/อู) ซ้อนแถว+1 ไม่ใช่ "ไม่ทำอะไร" แบบที่ 9.17 เข้าใจผิด** -- ผู้ใช้ระบุ: "◌ุ กับ ◌ู
วาดที่ N+1" -- ตรวจสอบพบว่า 9.17 เข้าใจผิดว่า D6/D7 เป็นสระบนอีก 2 ตัว (รวมอยู่ใน `UPPER_VOWEL_TABLE`
5 ตัว) แต่ที่จริงเป็นสระล่าง คนละกลุ่ม -- ยืนยันซ้ำจาก `thaifont.asm` ที่ label โค้ดนี้ตรงตัวว่า
"ุ"/"ู" พอดี -- แก้โดยย้าย D6/D7 ออกจาก `UPPER_VOWEL_TABLE` (เหลือ 3 ตัว: D1,D4,D5) ไปเป็น
`LOWER_VOWEL_TABLE` ใหม่ (2 ตัว) ที่ `CLASSIFY_THAI_MARK` คืนค่า class=3 แยกต่างหาก ซ้อนแถว **+1**
(ใต้พยัญชนะ) แทนแถว-1 ไม่มีการผสมกับวรรณยุกต์ใด ๆ (ตำแหน่งคนละที่กับวรรณยุกต์ซึ่งอยู่เหนือพยัญชนะ)

**บั๊กที่เจอระหว่างแก้รอบนี้ (ยืนยันด้วย breakpoint trace จริงทั้งคู่ ไม่ใช่แก้เดา):**
1. `NAMETAB_ADDR` เอกสารตัวเองบอกอยู่แล้วว่า "ทำลาย A/BC/DE(D)" -- ใช้ C เป็น scratch ภายใน
   (`ld c,l`) โดยไม่คืนค่า -- รอบแรก (9.17) ไม่เคยเจอบั๊กนี้เพราะไม่เคยต้องใช้ C ต่อหลังเรียก
   แต่รอบนี้ต้องใช้ C (วรรณยุกต์เดิม) ต่อที่ `COMBINE_LOOKUP` ทำให้ค่าเพี้ยน -- แก้ด้วย
   `push bc`/`pop bc` ครอบการเรียก `NAMETAB_ADDR` ตรงจุดที่ตรวจสอบแถวเหนือ
2. `COMBINE_LOOKUP` เอกสารตัวเองก็บอกว่า "ทำลาย B/C/HL" (ใช้ B เป็นตัวนับดัชนีสระแล้วทับด้วย offset
   ผลลัพธ์) -- แต่โค้ดที่เรียกใช้อ่าน B ซ้ำ "หลัง" เรียกไปแล้วเพื่อเก็บ `PRINT_LAST_MARK_VOWEL` ทำให้
   ได้ค่า 0 (เพี้ยน) แทนที่จะเป็นโค้ดสระบนจริง (209) -- เจอจาก breakpoint trace ที่ `PRINTHOOK` entry
   เทียบ `PRINT_LAST_MARK_VOWEL` จริงกับที่คาด -- แก้ด้วยการสำรอง B ไว้ใน D ก่อนเรียก
   `COMBINE_LOOKUP` แล้วใช้ D แทน B หลังจากนั้น
3. (ไม่ใช่บั๊ก logic แต่เป็นข้อจำกัดของ assembler) ฟังก์ชัน `PRINTHOOK` ขยายใหญ่ขึ้นมากจากโค้ดใหม่
   ทั้งหมดนี้ ทำให้ `jr` (relative jump, range ±127 ไบต์) หลายจุดเกิน range -- แก้โดยเปลี่ยนเป็น `jp`
   (absolute jump) ทุกจุดที่ไกลเกิน (pasmo แจ้ง "Relative jump out of range" ตรง ๆ ตอน build ทำให้
   หาจุดที่ต้องแก้ได้ง่าย)

**การยืนยัน (openMSX จริง):** เทสใหม่ `test_printon_combine.tcl` (3 เคส: ผสม D1+E8->131, undo ด้วย
Backspace กลับเป็น D1 เปล่า ๆ, สระล่าง D6 ซ้อนแถว+1) ผ่านหมด + อัปเดต `test_printon_compositing.tcl`
ให้ตรงกับพฤติกรรมที่ถูกต้อง (D4+E8 ต้องผสมเป็น 136 ไม่ใช่ซ้อน 212/232 แยก) ผ่านแล้ว + regression suite
เดิมทั้งหมด (`test_full_regression.tcl`/`test_phase3_thai_typing_bug9_bug10.tcl`/
`test_phase3_bug12_ram_collision.tcl`/`test_plock_verify.tcl`) รันซ้ำแล้วผลตรงกับ baseline เดิมทุก
ประการ ไม่มี regression ใหม่จากงานรอบนี้

**ยังไม่ทดสอบ/คำถามเปิด (สืบทอดจาก 9.17 + เพิ่มใหม่):** (1)-(3) เดิมจาก 9.17 ยังคงค้างอยู่ (2) การ
"ผสม" glyph ยังจำกัดแค่สระบน D1/D4/D5 เท่านั้น (ยังไม่รองรับ D6/D7/ED แม้ original ROM มีกลุ่ม lookup
สำหรับ ED ด้วย -- ไม่ implement เพราะไม่ได้ track ED เป็นสระบนเลยในเวอร์ชันนี้) (3) ยังไม่ทดสอบ
Backspace undo ข้ามบรรทัด (LF) หรือกรณี PRINT_COMBINE_PENDING ค้างพร้อมกับ Backspace-undo ค้างพร้อม
กันทั้งคู่ (ทฤษฎีควรทำงานถูกต้องเพราะ deferred-fix รันก่อน BS-check เสมอในโค้ดเดียวกัน แต่ยังไม่มีเทส
เจาะจงกรณีนี้โดยตรง)

### 9.19 ข้อความไทยใน LIST/RUN หายสระบน/ล่าง -- ต้นเหตุจริง: ตัวแก้ไขบรรทัดของ BIOS อ่านจอแถวเดียว

**อาการ (ผู้ใช้รายงานบน blueMSX)**: PRINTON แล้วพิมพ์ `10 print "สวัสดีครับ"` -> `list`/`run` ได้ "สวสดครบ"
ผู้ใช้ชี้ชัด: ไม่ได้ต้องการแค่ย้ายตำแหน่งวาด แต่ต้อง "เก็บ" ตัวอักษรระดับบน/ล่างด้วย (ต้องได้ ส ว ั ส ด ี)

**ต้นเหตุ (disassemble MSX1/MSX2/MSX2+ BIOS ยืนยัน -- โครงสร้างเหมือนกันทั้ง 3 รุ่น)**: ตอนกด Enter
INLIN ($245A) อ่านบรรทัดกลับจาก VRAM เข้า BUF ($F55E) ทีละช่องจากแถวที่ cursor อยู่เท่านั้น สระ/วรรณยุกต์
ที่ PRINTON ย้ายไปวาดแถว-1/แถว+1 จึงไม่เคยเข้า BUF เลย (โปรแกรมเก็บแค่แถวกลาง) + บั๊กซ้อน: ถ้าตัวสุดท้าย
ก่อน Enter เป็นเครื่องหมาย CSRY ยังค้างที่แถวบน/ล่าง (PRINTHOOK คืนค่าตอน CHPUT ครั้งถัดไป ซึ่ง Enter
ไม่ผ่าน) BIOS จึงอ่านผิดแถวทั้งบรรทัด (เช่น `20 print "ดี` หายทั้งบรรทัด)

**วิธีแก้ (ไม่มี stack-hijack, ใช้ hook มาตรฐานเท่านั้น)**:
1. `H.PINL`/`H.INLI` -> `INLIN_HOOK` ตั้ง `INLIN_ACTIVE` ตอนเริ่มรับบรรทัด (ทุกแบบ: direct/โปรแกรม,
   INPUT, LINE INPUT)
2. หลังอ่าน BUF เสร็จ BIOS ESC-Y ไปท้ายบรรทัดแล้วพิมพ์ LF ($24B9) -- PRINTHOOK เห็น LF ขณะ
   `INLIN_ACTIVE` -> `INLIN_REBUILD` ประกอบ BUF ใหม่จาก VRAM: ต่อคอลัมน์ = ตัวแถวกลาง + สระล่าง (แถว+1)
   + สระบน/วรรณยุกต์ (แถว-1, glyph ผสมแยกกลับเป็น 2 โค้ดด้วย `DECOMBINE`) จำลองกติกา BIOS ครบ
   (FSTPOS ข้าม prompt ของ INPUT, ข้ามช่อง 0, control code เป็น $01+โค้ด+$40, ตัดช่องว่างท้าย)
   บรรทัดตรรกะหลายแถว (LINTTB) ปล่อย BUF ของ BIOS ตามเดิม
3. `H.CHGE` -> `CHGE_HOOK` คืน CSRY แถวจริง + แก้ glyph ผสมที่ค้าง ทันทีก่อนรอคีย์ทุกครั้ง (cursor ไม่
   ค้างผิดแถวอีก และหมดข้อจำกัด "วรรณยุกต์ค้างไม่ผสมจนกว่าจะกดคีย์ถัดไป" ของ 9.18)

**พบระหว่างทางและแก้ด้วย**:
- ตารางจัดกลุ่มเครื่องหมายผิดมาตั้งแต่ 9.18: ฟอนต์คือ TIS-620 (วาด glyph จาก font_raw.bin ดูจริง) --
  สระบน D1,D4,D5,D6,D7,E7,ED / วรรณยุกต์ E8-EC / สระล่าง D8,D9,DA (เดิม D6/D7 ซึ่งคือ ึ ื ถูกวาดลงแถว
  ล่าง และ ุ ู ของจริงไม่ถูกซ้อน) COMBO_TABLE เพิ่ม ึ ื ํ +วรรณยุกต์ และ ิ+์ (ตรวจ pixel ทุกตัว)
- `NAMETAB_ADDR` เขียนใหม่ตามสูตร CALC_ADDR ของ BIOS เต็มรูป (MSX1 $0BF2 / MSX2,2+ $0B98): รองรับ
  SCREEN 0 ทุก WIDTH, 80 คอลัมน์, และ SCREEN 1 -- MSX2+ ญี่ปุ่นบูตเป็น SCREEN 1 WIDTH 29 ทำให้ PRINTON
  เดิมใช้ไม่ได้เลยบน MSX2+
- THAION ซ้ำ (ROM สั่งเองตอนบูต + ผู้ใช้สั่งอีก) ทำให้ installer สำรอง hook ของเราเองเป็น "ค่าเดิม"
  THAIOFF จึงถอดไม่ออก (= artifact "H_KEYC after thaioff" ใน 9.17) -- เพิ่มการเช็คติดตั้งซ้ำใน CMD_THAION

**การยืนยัน**: openMSX 19.1 + BIOS จริงของผู้ใช้ (blueMSX Shared Roms: MSX.rom, MSX2.rom+EXT,
MSX2P.rom+EXT) 4 config (MSX1, MSX1 slot 0 expanded, MSX2, MSX2+) -- `tests_919/`:
- `t_inlin.tcl`: 10 "สวัสดีครับ" / 20 "ดี (จบด้วยสระ) / 30 "ดี่" / 40 "ปู่" -> โปรแกรมเก็บครบทุกโค้ดทั้ง 4 เครื่อง
  (ROM เดิม: `CA C7 CA B4 A4 C3 BA` = "สวสดครบ" ตรงกับที่ผู้ใช้เห็น, บรรทัด 20 หายทั้งบรรทัด)
- `t_input.tcl`: INPUT A$ พิมพ์ ดี่ -> ASC = 180 213 232 ครบ (prompt "? " ถูกข้ามถูกต้อง)
- `t_edit.tcl`: LIST แล้ว NEW แล้วเลื่อน cursor ขึ้นไปกด Enter บนบรรทัดที่แสดงอยู่ -> ได้บรรทัดคืนครบสระ
- `t_off.tcl`: THAIOFF คืนทั้ง 5 hook เป็น RET จริง
- regression เดิมทั้งหมดผ่าน (`test_printon_combine.tcl` TEST3 แก้จาก D6 เป็น D8 ตามตารางที่ถูก)

**ข้อจำกัดที่ทราบ**: (1) กด Ctrl+J (LF) กลางบรรทัดตอนพิมพ์จะไปกระตุ้น rebuild ก่อนเวลา บรรทัดนั้นจะ
ไม่ได้รวมเครื่องหมาย (2) บรรทัดยาวเกิน 1 แถวขณะ PRINTON ไม่รวมเครื่องหมาย (แถวถัดไปเป็นแถวสระล่าง
อยู่แล้ว) (3) ตัวแปรใหม่ $FD22-$FD33 อยู่ใน SLTWRK เหมือนตัวแปรเดิม -- อาจชนถ้ามี ROM อื่นใน slot 1-0

### 9.20 scroll 3 แถวที่ขอบล่างจอ, BS/DEL/INSERT ลบ-แทรกพร้อมสระบน/ล่าง

**คำขอผู้ใช้**: (1) พอถึงบรรทัดล่างสุดจอเลื่อนทีละแถว (2) BS ต้องลบสระบน/ล่างไปด้วย ทั้งบนจอและใน data

**(1) scroll**: เดิม LF ใกล้ขอบล่าง (CSRY>=22) ข้ามการโก่ง แล้ว BIOS scroll ทีละแถว -- ตอนนี้แถวใหม่ T
ต้องมีแถวสระล่างอยู่ในจอเสมอ (T <= ล่างสุด-1, ล่างสุด = CRTCNT+CNSDFG สูตรเดียวกับ BIOS $0C32
ไม่ทับแถว function key) ถ้าไม่พอ ย้าย cursor ไปแถวล่างสุดแล้วเรียก CHPUT(LF) ซ้อนเอง s ครั้ง
(BIOS scroll + ดูแล LINTTB เอง, `PRINT_IN_LF` กัน hook ทำงานซ้อน) แล้ววาง CSRY ให้ LF ของ BIOS
พาไปแถว ล่างสุด-1 พอดี -- ยืนยัน: RUN พิมพ์ 9 บรรทัด + พิมพ์คำสั่งต่อที่ขอบล่าง ระยะห่าง 3 แถวคงที่ตลอด

**(2) BS/DEL**: disassemble พบว่า BS ของตัวแก้ไขบรรทัด ($2561) และ DEL ($2550) **ไม่ผ่าน CHPUT เลย**
(เลื่อนแถวกลางเองผ่าน routine ภายใน) -- BS-undo ของ 9.18 จึงไม่เคยทำงานจากคีย์บอร์ดจริง ทำงานเฉพาะ
PRINT CHR$(8) -- วิธีแก้: ระหว่างรับบรรทัดขณะ PRINTON (`INLIN_ACTIVE`) KEYC_HOOK ดันโค้ดส่วนตัว
$10 (BS) / $11 (DEL) เข้าคิวแทน INLIN ส่ง control code ที่ไม่อยู่ในตาราง dispatch ไป CHPUT ตรง ๆ
($2428) และ CHPUT ไม่ทำอะไรกับมัน PRINTHOOK จึงได้ทำเอง:
- BS ทันทีหลังพิมพ์สระ/วรรณยุกต์ -> ลบเฉพาะเครื่องหมายตัวนั้น (คืนค่าช่องเดิม: ว่าง หรือสระบนก่อนผสม)
- BS กรณีอื่น -> ลบทั้งคอลัมน์ (ตัวแถวกลาง+สระบน/วรรณยุกต์+สระล่าง) เลื่อนทั้ง 3 แถวไปซ้าย ถอย cursor
- DEL -> ลบทั้งคอลัมน์ที่ cursor
- **INSERT** (พบระหว่างทาง): BIOS ($24F2) เลื่อนแถวกลางไปขวาก่อน CHPUT -- ตัวปกติ: เลื่อนแถวบน/ล่าง
  ตาม; สระ/วรรณยุกต์: ปิดช่องที่ BIOS เปิดไว้คืน (ข้ามไบต์ ESC sequence ด้วย ESCCNT -- ไม่งั้น ESC Y
  ตอนกด Enter ทำให้สระบนเลื่อนเพี้ยน 3 ช่อง เจอจริงระหว่างทดสอบ)
- แถวบน/ล่างจะถูกเลื่อนก็ต่อเมื่อมีแต่เครื่องหมาย/ช่องว่าง (`SR_VALID`) กันไปเลื่อนข้อความบรรทัดอื่น
data ถูกต้องตามจอโดยอัตโนมัติ เพราะ INLIN_REBUILD อ่านจากจอตอน Enter

**การยืนยัน** (`tests_919/t_bs.tcl`, `t_scroll.tcl` + ชุดเดิม) MSX1 / MSX1 expanded / MSX2 / MSX2+ ผ่าน
เหมือนกันทุกไบต์: "ดี"+BS -> "ด" / "กดีค"+BS+BS -> "ก" / DEL บน ด ของ "ดีค" -> "ค" / INS แทรก ก หน้า
"ดีค" -> "กดีค" / INS แทรก ี -> "ดีค" / BS จากคีย์จริง (keymatrix) -> "ด" / BS อักษรอังกฤษปกติยังทำงาน

**ยังไม่รองรับ**: บรรทัดยาวเกิน 1 แถว (MSX2+ ญี่ปุ่นบูตเป็น SCREEN 1 WIDTH 29 จึงเจอบ่อย)

### 9.21 cursor แยกสถานะไทย/อังกฤษ

BIOS วาด cursor (MSX1 $09E6 / MSX2,2+ $0A43 โค้ดเหมือนกัน) โดยคัดลอก glyph ของตัวใต้ cursor มากลับสี
(CSTYLE=0 ทั้ง 8 แถว / โหมด INS 3 แถวล่าง) ลง glyph 255 แล้วเขียน 255 ลงช่อง, ตอนลบเขียน CURSAV คืน --
มี hook มาตรฐาน H.DSPC ($FDA9) ก่อนวาด และ H.ERAC ($FDAE) ก่อนลบ
- DSPC_HOOK (สถานะไทย = INPUT_MODE): เตรียม glyph 255 เป็นรูปที่ต้องการแบบกลับสีล่วงหน้า แล้วเขียน 255
  ลงช่องแทนตัวจริง -> BIOS กลับสีอีกรอบได้รูปที่ต้องการพอดี; ERAC_HOOK ใส่ตัวจริงคืนใน CURSAV
- รูป: อังกฤษ = ของ BIOS (ทึบ / INS ขีดล่าง) ไม่ยุ่งเลย, ไทย = กรอบสี่เหลี่ยมรอบตัวอักษร / INS = แถบตั้งซ้าย
  (ความกว้างกรอบตาม SCREEN 0 6 จุด / SCREEN 1 8 จุด)
- กดปุ่มสลับภาษาระหว่างรอคีย์: KEYC_HOOK เรียก CUR_REDRAW วาด glyph 255 ใหม่ทันที (อยู่ใน interrupt --
  ทำเฉพาะช่วง cursor รอคีย์ของ CHGET แสดงอยู่ และข้ามถ้า BIOS มีงานวาดป้าย function key ค้างจาก SHIFT)
- ยืนยัน (`tests_919/t_cursor.tcl`, MSX1/MSX2/MSX2+): glyph 255 ถูกต้องทั้ง 4 แบบ, สลับทันทีระหว่างรอคีย์,
  ตัวใต้ cursor คืนถูกต้อง (10 REM abc ที่ขยับ cursor ผ่าน b เก็บได้ตรง), THAIOFF ถอด hook ครบ 7 ตัว
- RAM ใหม่ $FD35-$FD42 (ย่าน SLTWRK เหมือนตัวแปรเดิม)

### 9.22 บูตโดยไม่มีบรรทัด `CALL THAION:?"Thai BASIC version 1.0"` บนจอ

เดิม INIT ดันคำสั่งนี้เข้าคิวคีย์บอร์ดให้ BASIC พิมพ์เอง (echo บนจอเลี่ยงไม่ได้ -- ดู 9.16) เพราะตอน INIT
ถ้าโหลดฟอนต์เองจะโดน INITXT ของ BASIC โหลดทับทีหลัง -- แก้: INIT ติดตั้ง hook **H.READ ($FF07)** แทน
(disassembly: BASIC เรียกที่ $4128 ก่อนพิมพ์ "Ok" เหมือนกันทั้ง MSX1/2/2+ ครั้งแรกคือหลัง banner + INITXT
เสร็จแล้ว) BOOT_HOOK ทำครั้งเดียว: คืน hook เดิม -> THAION_CORE (แยกจาก CMD_THAION) -> ขึ้นบรรทัดถ้าจำเป็น
(MSX1 "Bytes free" ไม่ขึ้นบรรทัดให้) -> พิมพ์ "Thai BASIC version 1.0" -> ต่อไปยัง hook เดิม
INIT ถูกเรียกซ้ำได้ -> ไม่ติดตั้งซ้ำถ้า H.READ ชี้มาที่เราแล้ว, equate H_READ เดิม ($FE01) ผิด แก้เป็น $FF07
ยืนยัน (`tests_919/t_boot.tcl`): จอบูต = banner ของ BASIC + "Thai BASIC version 1.0" + "Ok" ทั้ง MSX1/MSX2/MSX2+,
ฟอนต์ไทยอยู่ใน VRAM, hook ติดตั้งครบ, คิวคีย์บอร์ดว่าง, พิมพ์ไทย+PRINTON เก็บได้ครบ -- เทสทั้งหมดผ่านเหมือนเดิม

### 9.23 banner "BASIC ไทย", ปุ่ม " (ฺ) เป็นเครื่องหมายใต้พยัญชนะ, ใช้กับ 80 คอลัมน์

- banner บูตเปลี่ยนเป็น "BASIC ไทย version 1.0" (ไทย = TIS-620 $DE $B7 $C2)
- ปุ่ม " (Shift+') ตอนภาษาไทยให้โค้ด $DA (ฺ พินทุ -- ตามตารางคีย์บอร์ดของ ROM ต้นฉบับ) แต่ glyph $DA ใน
  ฟอนต์ต้นฉบับวาดจุดไว้ชิดล่างของช่อง ดูเหมือน "." -- แก้ glyph ใน assets/font_raw.bin ให้จุดอยู่บนสุดของช่อง
  (แบบเดียวกับ ุ ู) PRINTON วางไว้แถว+1 จึงอยู่ใต้พยัญชนะพอดี (อยู่ใน LOWER_VOWEL_TABLE ตั้งแต่ 9.19)
- 80 คอลัมน์ (MSX2/2+ SCREEN 0 WIDTH 80): NAMETAB_ADDR รองรับแล้วตั้งแต่ 9.19 แต่ SCREEN/WIDTH เรียก
  INITXT ซึ่งโหลดฟอนต์ระบบทับฟอนต์ไทย (เป็นทุกโหมด ไม่ใช่แค่ 80 คอลัมน์) -- เพิ่ม FONT_CHECK: เช็ค 2 ไบต์
  ของ glyph ก ใน VRAM ตอนเริ่มรอคีย์ (CHGE_HOOK) และก่อนพิมพ์อักษรโค้ด >= $80 (PRINTHOOK) ถ้าไม่ใช่ของเรา
  โหลดฟอนต์ไทยคืน (เฉพาะ SCREEN 0/1)
- ยืนยัน (`tests_919/t_w80.tcl`): MSX2 และ MSX2+ หลัง `SCREEN 0:WIDTH 80` ฟอนต์ไทยกลับมาเอง, เก็บ "สวัสดีปู่"
  ครบ, LIST วางสระบน/ล่าง/วรรณยุกต์ตรงคอลัมน์ในแถวกว้าง 80, Shift+' ได้ $DA อยู่ใต้ ก, cursor ไทยถูกต้อง;
  เทสอื่นทั้งหมดผ่านเหมือนเดิม

### 9.25 บรรทัดยาวเกิน 1 แถวขณะ PRINTON (ต่อแถวแบบ 3 ชั้น)

BIOS ตัดบรรทัดที่เกินความกว้างไปแถว+1 (= แถวสระล่างตอน PRINTON) และตั้ง LINTTB[แถว]=0 -- ตอนนี้:
- **ต่อแถว**: PRINTHOOK จำไว้เมื่อตัวปกติลงคอลัมน์สุดท้าย (WRAP_PENDING) แล้ว PRINT_RESYNC (ตอน CHPUT ถัดไป
  หรือก่อนรอคีย์) ย้าย cursor ไปแถว+3 (LINK_NEXT: scroll จอขึ้นถ้าแถวต่อ+แถวสระล่างของมันไม่พอ) บันทึกการต่อแถว
  ใน LINTTB ด้วย marker $50|DOWN(1)|UP(2) -- BIOS เห็นเป็น "ไม่ต่อ" (ไม่เอาแถวสระมาปนเป็นข้อความ) แต่ค่านี้
  เลื่อนตามจอเวลา BIOS scroll เอง
- **สระ/วรรณยุกต์ของตัวคอลัมน์สุดท้าย** (พิมพ์หลังต่อแถวแล้ว): ถ้าให้ BIOS วาดที่คอลัมน์สุดท้าย BIOS จะตัดบรรทัด/
  scroll จอเอง -> GHOST_CHECK วาดเองด้วย WRTVRM แล้วให้ BIOS วาด "ตัวหลอก" ที่ cursor จริง จำตัวเดิมไว้เขียนคืน
- **Enter**: INLIN_REBUILD ย้อนหาแถวแรก (marker DOWN ที่แถว-3) แล้วอ่านทุกแถวข้อความ (ห่างกัน 3) รวมสระของแต่ละแถว;
  แถวที่กด Enter BIOS เขียน LINTTB ทับ -> ดูการต่อจาก UP ของแถว+3 แทน แล้วใส่ marker คืน; LF ไปต่อใต้แถวสุดท้าย
  ของบรรทัด (กด Enter บนแถวไหนของบรรทัดก็ได้)
- **BS/DEL** ข้ามแถว (DELCOL ดึงช่องแรกของแถวต่อทั้ง 3 ชั้นขึ้นมา), BS ที่ต้นแถวต่อลบตัวท้ายแถวก่อน
- **INS**: BIOS เลื่อนแถวกลางเองไม่รู้จัก 3 ชั้น/การต่อแถว และแถวเต็มจะ "แทรกแถวใหม่" ทำ layout พัง -> รับโหมด INS
  มาทำเอง: CHGE_HOOK เห็น INSFLG ที่ BIOS กลับค่าตอนกด INS แล้วสลับ THAI_INS แทน ตั้ง INSFLG=0 ให้ BIOS พิมพ์ทับ
  เสมอ; INSCOL เลื่อนทั้ง 3 ชั้นไปขวาข้ามแถวต่อ ตัวที่ล้น (พร้อมสระของมัน) ไปต้นแถวต่อ ล้นแถวสุดท้ายก็ต่อแถวใหม่
- **ลูกศร**: ขึ้น/ลง ทีละ 3 แถว (อยู่บนแถวข้อความเสมอ), ซ้าย/ขวาข้ามแถวต่อไปแถว-3/+3 (ตั้ง CSRY ล่วงหน้าให้
  การเลื่อน 1 ช่องของ BIOS ไปลงที่ที่ถูก)
- ยืนยัน (`tests_919/t_cont2.tcl` ghost/enter2/bs/del/ins/prog/bottom) ทั้ง MSX1, MSX1 expanded, MSX2, MSX2+
  (SCREEN 1 WIDTH 29 ตัดคนละที่แต่โปรแกรมที่เก็บได้ตรงกันทุกไบต์), รวมถึงต่อแถวตอนอยู่แถวล่างสุด (scroll) และ
  PRINT จากโปรแกรมที่ยาวเกินแถววนซ้ำจนจอ scroll -- เทสเดิมทั้งหมดผ่าน (t_edit ปรับให้กดขึ้นทีละ 3 แถว)
- RAM ใหม่ $FD48-$FD54 (SLTWRK ต่อจากเดิม)

### 9.26 cursor ภาษาไทย = รูปเดียวกับอังกฤษ แต่กระพริบ (แทนกรอบสี่เหลี่ยมของ 9.21)

- รูป cursor ทั้งสองภาษาเป็นของ BIOS (ทึบ / INS ขีดล่าง) -- ภาษาไทยต่างแค่กระพริบ
- H.DSPC/H.ERAC ใช้แค่จำว่า "cursor รอคีย์แสดงอยู่" (CUR_WAITING) ไม่แทนตัวใต้ cursor อีกแล้ว
- hook ใหม่ **H.TIMI ($FD9F)**: ทุก BLINK_FRAMES (20) frame ถ้าเป็นภาษาไทยและ cursor รอคีย์แสดงอยู่ (SCREEN 0/1,
  ไม่มีงานวาดป้าย function key ค้าง) สลับ glyph 255 ระหว่าง "กลับสีแบบ BIOS" (ติด) กับ "glyph ปกติของตัวใต้
  cursor จาก CURSAV" (ดับ) -- ส่งต่อ hook เดิมเสมอ (disk ROM ใช้ H.TIMI) โดยคืนทุก register รวม A (VDP status)
- สลับเป็นอังกฤษตอนกำลังดับ -> วาดติดคืนทันที (CUR_REDRAW ตอนกดปุ่มสลับ และ TIMI_HOOK เช็คซ้ำ)
- บั๊กที่เจอระหว่างทำ: CUR_SAFE เช็ค SCREEN ด้วย `cp 2 / ccf / ret nc` กลับด้าน -> ไม่กระพริบเลย แก้เป็น `cp 2 / ret nc`
- ยืนยัน (`tests_919/t_blink.tcl`): อังกฤษ glyph 255 = FF.. คงที่, ไทยสลับ 00../FF.. เป็นจังหวะ, สลับกลับอังกฤษ
  ติดค้าง, THAIOFF คืน H.TIMI เป็น C9 ครบ 5 ไบต์ -- MSX1, MSX1 expanded, MSX2, MSX2+
- `tests_919/t_cursor.tcl` (เขียนใหม่): เก็บ glyph 255 หลายครั้งต่อสถานะ -- อังกฤษเห็นค่าเดียว, ไทยเห็น 2 ค่า
  (กลับสี/ปกติ) ทั้งบนช่องว่าง บนตัว b และโหมด INS (กลับสีเฉพาะแถว 5-7)

### 9.27 ย้าย RAM ของ ROM ออกจาก SLTWRK ไปบล็อกที่จองผ่าน HIMEM

เดิมตัวแปรทั้งหมดอยู่ $FD09-$FD5B = SLTWRK ของ **slot อื่น** (0-x ถึง 2-x) ถ้าเครื่องจริงมี ROM ที่ใช้ช่องนั้น
(เช่น disk interface) จะชนกัน -- เปลี่ยนเป็นวิธีมาตรฐานของ cartridge (`src/workram.asm`):
- INIT (WORK_ALLOC): ถ้าช่อง SLTWRK ของ slot เราเอง (page 1: SLTWRK + primary*32 + secondary*8 + 2) ยังเป็น 0
  ลด HIMEM ($FC4A) ลง WORK_SIZE (83) ไบต์ ล้างเป็น 0 แล้วเก็บ address ไว้ในช่องนั้น (INIT ถูกเรียกซ้ำได้ -> จองครั้งเดียว)
- ทุกจุดเข้า (INIT, STATEMENT, BOOT_HOOK, และ hook ทุกตัว: KEYC/CHPU/CHGE/PINL/INLI/DSPC/ERAC/TIMI) เป็น
  wrapper: `push ix / call GET_IX / call ..._BODY / pop ix` -- ตัวแปรทุกตัวเป็น offset อ้างแบบ `(ix+ชื่อ)`
- address จริงของ backup hook (PREV_*) คำนวณด้วย IX_HL/IX_DE; TIMI_HOOK ส่งต่อ hook เดิมด้วยการวาง address
  ปลายทาง (บล็อก+PREV_TIMI) บน stack แล้ว RET (ทุก register รวม IX ของผู้ถูกขัดจังหวะคืนครบ)
- ลบ dead code KEYC_TRAMPOLINE เดิมทิ้ง; ตอนนี้ ROM ไม่เขียน RAM นอก hook มาตรฐาน/ตัวแปรระบบที่ documented
  กับบล็อกของตัวเองเลย
- **FIX_FILES** (ต่อท้าย WORK_ALLOC): BASIC จัด file buffer (FILTAB/FCB) ใต้ HIMEM ไว้แล้ว *ก่อน* เรียก INIT ของ
  cartridge ($7CCC -> scan slot $7D14, ไม่คำนวณใหม่หลัง scan -- ตรวจจาก disassembly MSX1 และ routine เดียวกันที่
  $7E6B บน MSX2/2+) ถ้าแค่ลด HIMEM บล็อกจะทับ buffer ของไฟล์ #1 (F32D-F37F = 83 ไบต์ท้าย FCB#1) ->
  OPEN ... AS #1 เขียนทับ PREV_TIMI แล้วค้าง -- จึงคำนวณ FILTAB/MEMSIZ/STKTOP/ตาราง FCB/NULBUF ใหม่ตามสูตรเดียวกับ
  MAXFILES= จากตัวแปรระบบที่ documented (ไม่เรียก routine ภายในของ BASIC) -- SP ไม่ต้องย้ายเพราะ BASIC ตั้ง stack
  ใหม่จาก STKTOP หลัง scan เอง; ยืนยันว่าค่าที่ได้ตรงกับที่ BASIC คำนวณเองเมื่อสั่ง `MAXFILES=1` ทุกตัว
- ผลที่เห็น: "Bytes free" ลดลง 83 ไบต์ (28815 -> 28732)
- ยังไม่ได้ทดสอบกับเครื่องที่มี disk ROM (openMSX ในเครื่องทดสอบไม่มี disk ROM) -- disk ROM ใช้ช่อง SLTWRK ของ slot
  ตัวเอง จึงไม่ชนกับเราแล้ว และถ้า disk ROM ลด HIMEM ต่อหลังเรา ก็จัด buffer ใหม่จาก HIMEM ปัจจุบันตามปกติ
- ข้อจำกัด: `CLEAR n,addr` ที่ addr สูงกว่าบล็อก ทำให้ BASIC ใช้พื้นที่ทับได้ (เหมือน cartridge ทั่วไปที่จองผ่าน HIMEM)
- เทสอ่านตัวแปรผ่าน `wv` (หา slot จาก H.KEYC) หรือ `var` (ช่อง SLTWRK ที่ชี้ HIMEM) ใน tests_919/lib.tcl
- ยืนยัน: tests_919 ทั้งหมด (t_* 11 ตัว + t_cont2 7 กรณี) บน 4 เครื่อง ผลตรงกับรอบก่อนย้าย RAM ยกเว้นบรรทัด Bytes free
  และรูป cursor (กระพริบ) -- legacy regression ผ่านทั้งหมด ไม่มี FAIL

### 9.28 ฟอนต์ใหม่จากผู้ใช้

- assets/font_raw.bin แทนด้วยไฟล์ msxfont.bin ของผู้ใช้ (2048 ไบต์ ใช้ 2040 ไบต์แรก = glyph 0-254,
  glyph 255 เป็นของ cursor BIOS ไม่โหลดทับ) -- ต่างจากเดิมที่ ฺ ($DA) และวรรณยุกต์ $E8-$EC ย้ายไปชิดล่างของช่อง

### 9.29 ใส่ disk ROM แล้วบูตไม่ขึ้น prompt (บั๊กจาก 9.27)

ผู้ใช้รายงานบน blueMSX -- จำลองได้บน openMSX ด้วย disk ROM ของ blueMSX (PHILIPSDISK.rom, WD2793) ทั้ง MSX1/MSX2/2+
พบ 2 สาเหตุ:
1. **FIX_FILES ลด STKTOP แต่ไม่ย้าย stack** -- disk ROM ที่ init ทีหลังย้ายหน่วยความจำโดยคัดลอก stack ช่วง
   SP..STKTOP ($5F02-$5F19 ใน disk ROM) ถ้า SP อยู่เหนือ STKTOP ใหม่ จำนวนไบต์ติดลบ -> ldir ทับทั้ง RAM -> ค้าง
   แก้: FIX_FILES ย้าย stack ลงเท่ากับที่ STKTOP ลด (วิธีเดียวกับ disk ROM) และตั้ง SAVSTK
2. **disk ROM ตัวหลักใช้ $F1C9-$F37F ตายตัว** เป็นพื้นที่ระบบของ DOS (ล้างเป็น 0 ตอน INIT แล้วค่อยลด HIMEM
   $1BF จากค่าปัจจุบัน -- $57A9-$57C3) ถ้า ROM เราอยู่ slot ก่อน disk แล้วจองจาก $F380 บล็อกจะทับตัวแปร DOS ->
   บูตได้แต่ Disk BASIC พัง (SAVE/LOAD = "Bad file number", FILES = "Syntax error")
   แก้: ตอนจอง ถ้า HIMEM ยังเป็น $F380 และมี disk ROM ใน slot อื่น (DISK_PRESENT: อ่านผ่าน RDSLT หา "AB" +
   JP ที่ $4010/$4013/$4016/$4019/$401C ตาม jump table ของ disk driver MSX-DOS -- ตรวจแล้วกับ Philips,
   Panasonic, National, Microsol, MSX-DOS 2.3, Sunrise IDE, Beer IDE) ให้จองใต้ $F1C9 แทน
   ราคา: เฉพาะกรณี ROM เราอยู่ slot ก่อน disk -- Bytes free หายเพิ่ม ~440 ไบต์ (ช่วงที่ disk ROM กันไว้แต่ไม่ได้ใช้)
- ลองทางเลือกจองผ่าน H.STKE (หลัง init ครบ) แล้ว -- ใช้ได้แต่ disk ROM ถือว่า "H.STKE ถูก hook = cartridge ขอ
  ควบคุมเครื่อง" แล้วข้ามการบูต MSX-DOS / AUTOEXEC.BAS ($59CE) จึงไม่ใช้
- ยืนยัน: MSX1/MSX2/MSX2+ x (ROM ก่อน disk, disk ก่อน ROM): บูตขึ้น "BASIC ไทย", SAVE/LOAD โปรแกรมภาษาไทย,
  OPEN/PRINT#/INPUT# ไฟล์ #1, FILES ถูกต้อง, MEMSIZ = FILTAB-2 ทุกกรณี, cursor ไทยกระพริบ + ส่งต่อ H.TIMI ให้
  disk ROM (THAIOFF คืน hook ของ disk ถูกต้อง) -- ชุดทดสอบเดิมบนเครื่องไม่มี disk ผลไม่เปลี่ยน

### 9.30 ฟีเจอร์ที่ขาดเทียบกับต้นฉบับ (กลุ่ม A + B)

ไล่จาก disassembly ของ MSXTHA102 v1.02 (hook table ที่ $40A6: CHPU/CHGE/PINL/QINL/INLI/KEYC/READ/LPTO/$FEC6)
- **A1 GRAPH+A..Z = คำสั่ง BASIC** (+SHIFT = ชุดที่สอง) ข้อความจากตารางต้นฉบับ $4F32/$4FF2 (`src/kwtables_data.asm`)
  ทำงานเมื่อ THAION ไม่ว่าพิมพ์ไทยหรืออังกฤษ, CTRL+GRAPH ปล่อยผ่าน, GRAPH+ปุ่มอื่นยังเป็นอักษรกราฟิกตามปกติ
- **A2 SELECT สลับ PRINTON/PRINTOFF + ล้างจอ** (ต้นฉบับ $512E/$515C) -- INLIN ส่งรหัส $18 มาที่ CHPUT,
  PRINTHOOK จับแล้วเรียก SELECT_TOGGLE; PLOCKON ล็อกไว้ได้; เปิด PRINTON แล้วเริ่มแถว 2 (มีแถวสระบน),
  ตั้ง FSTPOS ใหม่ และใน direct mode ตั้ง INLIN_ACTIVE ตามโหมดใหม่ ให้บรรทัดที่กำลังพิมพ์ประกอบสระกลับเข้า BUF ได้
- **A3 ไม่ทำ**: ต้นฉบับกลับเป็นอังกฤษเองเมื่อกด Enter ใน direct mode ($50F6-$510C) แต่ผู้ใช้เคยรายงานอาการนี้
  ว่าเป็นบั๊ก (บั๊กจริงข้อ 12 รอบสอง) จึงคงพฤติกรรมปัจจุบัน (SET_INPUT_MODE แยกไว้แล้ว เปิดได้ง่ายถ้าต้องการ)
- **A4 CALL SYSTEM** พิมพ์ข้อความเวอร์ชัน (PRINT_BANNER ใช้ร่วมกับตอนบูต)
- **A5 กด CODE ค้างตอนเปิดเครื่อง** = ไม่เปิดระบบไทย (เช็คทั้งตอน INIT -> BOOT_SKIP และตอน "Ok" แรก)
  หมายเหตุ: MSX2+ ที่ใช้ทดสอบ (BIOS ญี่ปุ่น) เองมีพฤติกรรมพิเศษเมื่อกด KANA ค้างตอนบูต -- เป็นของ BIOS ไม่ใช่ ROM เรา
- **B CALL ANSTR/TNSTR/MLSTR(<สตริง>,<ตัวแปร$>)** (ต้นฉบับ $43E4-$44FB -- wiki อธิบายคลาดเคลื่อน):
  ANSTR = เลขไทย->อารบิก, TNSTR = อารบิก->เลขไทย, MLSTR = ตัดสระบน/ล่าง/วรรณยุกต์/glyph ผสม (ตาราง $55A0)
  ใช้ routine ภายใน BASIC ผ่าน CALBAS แบบต้นฉบับ (FRMEVL $4C64, GETYPR $5597, FRESTR $67D3, STRCPY $6611,
  CHRGTR $4666, PTRGET $5EA4, SNERR $4055, TMERR $406D) -- ตรวจแล้วว่า code เหมือนกันทุกไบต์บน MSX1/2/2+
  ผลพักที่ BUF+3 ก่อนจองที่ (การจองอาจเก็บขยะ string) -- ทดสอบวน 500 รอบด้วย CLEAR 60 ผลถูกต้อง
- WORK_SIZE เพิ่มเป็น 85 ไบต์ (BOOT_SKIP, SF_KIND)
- เทส: tests_919/t_a13 (GRAPH คำสั่ง + Enter), t_select, t_a45 (SYSTEM + CODE ค้าง), t_strfn -- ผ่าน 4 เครื่อง;
  ชุดเดิมผลไม่เปลี่ยน; disk ROM 2 ลำดับ slot ผ่าน

## 9. รายการบั๊กเดิมที่เวอร์ชันนี้ต้องไม่มี

- [x] MSX1 boot hang (เดิมแก้ใน v3 — จะไม่เกิดเพราะไม่ใช้ internal INITXT address เลย)
- [x] MSX2 "กด F1-F5 แล้วตัวอักษรเพี้ยน + ต้องกด key 2 ที" (DSPFNK bug — จะไม่เกิดเพราะเขียน F-key label เอง)
- [x] MSX2 character corruption ทั่วไป (VRAM address ผิด generation — แก้ด้วย section 5)
- [x] MSX2+ stack-search hijack เขียนทับ stack ผิดตำแหน่ง (ไม่ใช้เทคนิคนี้เลยในเวอร์ชันใหม่)
- [x] `CALL PRINTON` (โหมดไทย 3 ระดับ) ใช้ไม่ได้บน MSX1 (ต้นฉบับมีบั๊กที่ path ผสมระหว่าง MSX1/2 — เวอร์ชัน
      ใหม่ไม่มี branch แยก generation ในจุดวาดตัวอักษรเลย ตามที่อธิบายใน section 5 จึงควรใช้ได้ทั้งคู่โดย
      ธรรมชาติ — **ต้องทดสอบยืนยันตอน Phase 2**)
